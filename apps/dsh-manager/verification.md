# 交付验证

## Issue 01：原生窗口、安装与启动

2026-09-06，结论 passed。环境 Windows 11（10.0.26100）、Go 1.26.8 windows/amd64、本机 Node.js。固定审查点 d2915ca，审查包含本 issue 的完整 WIP。

- `go vet ./...` 通过，gofmt 完成。
- `go test -p 1 '-coverprofile=coverage.out' ./...` 全部通过；`go tool cover '-func=coverage.out'` 总 statement 覆盖率 85.9%，包含 GUI、Windows adapter、入口代码。
- 原生消息循环测试覆盖配置显示/保存/错误、启动/重启/停止、日志更新、布局、单实例激活与注销。
- 进程测试验证受管子进程端口归属、外部端口拒绝、进程树停止、取消和退出码；安装测试验证失败不提升、损坏安装恢复、无归属目录拒绝清理；管理测试验证主动停止取消重启、点击顺序、认证地址与日志脱敏。
- `DSH_SMOKE_ROOT=<专用目录> go test -v -timeout 15m -run TestRealDSHInstallationAndLifecycle ./internal/manager` 通过：官方 npm 安装 DSH 0.1.2-rc.1，隔离 profile 和工作目录，测试端口 61035，认证后 HTTP 200，停止后端口释放。上游启动令牌不写入报告。
- 首次真实探测失败的原因是上游根页面要求令牌和 cookie 握手；修复为解析受管进程的本机地址，CookieJar 验证就绪，浏览器使用同一认证地址，日志隐藏令牌；回归与真实重验均通过。
- `./build.ps1` 构建单 exe 成功，当前大小 6,989,312 字节。
- `./scripts/measure.ps1` 在独立用户数据目录采样：首个启动 114.61 ms，后续五次 57.40/60.68/68.91/62.02/63.56 ms；工作集 18.88–19.22 MiB，60 秒空闲累计 CPU 时间 0.0 秒（系统采样精度内）。该测量针对管理器窗口，不包含 DSH；不是操作系统冷启动基准。
- Standards 与 Spec 双 agent 审查通过；已修复重启取消竞态、损坏安装不可重试、DPI 最小尺寸及最小化布局重入。

Windows 10 兼容性尚未在 Windows 10 实机验证。托盘保活和自动更新属于后续 issue，本阶段未声明交付。

## Issue 02：托盘、保活与登录自启

2026-09-06，结论 passed。固定审查点 2566a9f，执行环境与 Issue 01 相同。

- gofmt、`go vet ./...` 通过；`go test -p 1 -timeout 45s '-coverprofile=coverage.out' ./...` 全通过，总 statement 覆盖率 85.0%。
- 可控时钟与真实 Node.js 子进程验证退避序列 1/2/4/8/16/30/30 秒、稳定 60 秒后重置、取消待恢复、关闭保活、HTTP 503 不触发重启。
- 原生窗口验证关闭入托盘服务继续运行、双击托盘恢复、登录后台启动路径、退避时停止按钮可用、配置保存失败恢复勾选、快速选项请求末次生效。
- 登录项 adapter 在独立测试注册表键验证创建、路径引用、幂等删除及保留其他值；测试未修改正式用户 Run 登录项，也未注销用户。
- Standards/Spec 双轴审查通过；修复了退避时停止按钮禁用、复选框异步顺序与失败恢复问题。
- Windows 真实登录/注销未做破坏当前会话的手工验证，以注册表契约、后台入口与注销窗口消息测试覆盖对应边界。自动更新属于 Issue 03。

## Issue 03：更新、回退与最终交付

2026-09-06，结论 passed。固定审查点 4794024；Windows 11 10.0.26100、Go 1.26.8 windows/amd64、Node.js 24.19.0，原生窗口按本机 200% DPI 渲染。

- gofmt 无待格式化文件，`go vet ./...` 通过；`go test -p 1 -timeout 60s '-coverprofile=coverage.out' ./...` 全通过，总 statement 覆盖率 85.9%，包含全部生产包及 main。
- 更新测试覆盖运行中准备不停止、保活保持旧版、显式启动应用 Pending、成功版本记录、失败回退与自动抑制、手动重试、停止覆盖下载后的重启、更新失败不取消原保活、取消候选不误标失败、6 小时调度、关闭自动更新、退出取消网络、HTTP 503 错误可见。
- 仓库验证安装锁等待可取消、部分安装不提升、清理保留当前/上次成功/待应用以及未知归属目录，拒绝越界删除。清理只在更新事务安装结束后执行，避开服务启停路径。
- 双轴审查均 passed，0 个未解决发现。已修复安装锁阻塞停止、清理旧快照误删待应用版本、失败的手动更新破坏保活意图三项问题，并补充回归。
- `./build.ps1` 成功，最终 `dist/dsh-manager.exe` 为 7,065,088 字节（6.74 MiB），单 Windows GUI exe。SHA256：`1B4E24A654E85D7B005145311964E111AF3DA950BC82B29DE4CA63DCB7565639`。
- 真实 DSH 0.1.2-rc.1 再验通过：专用 profile/目录/端口 63279，认证后 HTTP 200，停止后端口释放。没有接管现有服务，没有修改全局 DSH 安装。
- `./scripts/measure.ps1 -Screenshot ./dist/window.png` 的 60 秒记录在 `dist/performance.json`：首次窗口就绪 116.40 ms，后续五次 65.33/86.31/79.64/73.06/76.81 ms；工作集 18.78–21.59 MiB；60 秒空闲 CPU 累计 0.0 秒（系统采样精度内）。全部满足窗口 <1 秒、工作集 <40 MiB、60 秒 CPU <0.6 秒的预算。此指标只包含管理器，空闲时关闭自动下载、未启动 DSH；不是系统冷启动基准。
- 原生窗口截图已检查，控件、中文标签与日志区域无裁切；截图采集器使用线程 DPI awareness 获取物理尺寸，`-IdleSeconds 1` 另行重拍完整截图，不覆盖上述 60 秒性能记录。GUI 自动化同时覆盖选项、托盘、配置、服务动作与最小窗口尺寸。

限制：未在 Windows 10 实机验证；没有真实注销当前用户或重新登录。相关 Windows 消息、用户 Run 项与后台入口由自动化测试验证。程序版本回退不撤销上游数据迁移。最终 EXE 不内嵌 Node.js，运行需系统 Node.js 与 npm。
