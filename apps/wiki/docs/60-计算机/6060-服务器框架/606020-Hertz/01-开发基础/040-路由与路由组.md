---
id: aef78e04-aa3a-419f-9d26-baaa9ee1eeff
---

# 路由与路由组

## 路由组成

- method: GET、POST、PUT、PATCH、DELETE 等 HTTP 方法;
- path: 静态片段、命名参数和通配参数组成的匹配规则;
- Handler chain: 路由组与路由中间件加上最终 Handler;

```go
api := h.Group("/api/v1")
api.GET("/articles", listArticles)
api.POST("/articles", createArticle)
api.GET("/articles/:id", getArticle)
api.PUT("/articles/:id", updateArticle)
api.DELETE("/articles/:id", deleteArticle)
```

## REST 映射

| 意图     | 方法与路径             | 成功状态 |
| -------- | ---------------------- | -------- |
| 查询列表 | `GET /articles`        | 200      |
| 查询单项 | `GET /articles/:id`    | 200      |
| 创建     | `POST /articles`       | 201      |
| 全量更新 | `PUT /articles/:id`    | 200      |
| 删除     | `DELETE /articles/:id` | 204      |

## 组织原则

- 路由组: 表达版本、资源域和共享中间件; 不承载业务逻辑;
- 注册位置: 每个领域提供小型 `Register` 函数，main 只做组合;
- 路径参数: 表达资源身份; 过滤、排序和分页放在 query;
- 版本策略: 只在发生不兼容契约变化时增加 `/v2`;

## 匹配检查

- 固定路径优先: 避免参数路由遮蔽特殊动作;
- 方法明确: 不用 `Any` 代替清晰的资源语义;
- 404: 没有路径匹配;
- 405: 路径存在但方法不允许; 是否返回 405 取决于 Engine 配置;
