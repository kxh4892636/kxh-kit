---
id: b644a469-e457-4172-88ec-2a9039df89c7
---

# 代理与重试

## 如何区分正向与反向代理？

- forward proxy: Client 通过代理访问外部目标; 常用于出口控制和审计;
- reverse proxy: Hertz 接收请求并转发到上游; 常用于网关、聚合和迁移;
- transparent concern: 代理仍需明确 Host、scheme、Header、超时和流式语义;

## 如何实现受控的反向转发？

```text
client → Hertz proxy → upstream
       ← status/header/body ←
```

- 路由重写: 明确原路径到上游路径的映射;
- Header: 删除 hop-by-hop Header，追加规范的转发信息;
- body: 大请求和响应使用流式转发并限制大小;
- 错误: 上游不可达与上游 5xx 分开记录;
- 安全: 不允许用户任意控制目标地址，防止 SSRF 和开放代理;

## 如何判断请求能否重试？

- 幂等操作: GET、HEAD 和具备幂等键的写请求更适合重试;
- 可重试错误: 应用可把临时建连失败、502/503/504 或 429 纳入策略；Hertz 不会仅因这些状态自动重试，需配置 `SetRetryIfFunc`;
- 不可重试: 参数错误、认证失败、权限失败和多数业务冲突;
- body 可重放: 流式或一次性 body 未缓存时不能安全重试;

## 如何限制重试的总成本？

```text
总 deadline = 第一次尝试 + 退避 + 后续尝试 + 响应余量
```

- 次数: `WithMaxAttemptTimes` 包含首次调用，默认 1 表示不重试；要重试两次应设为 3;
- backoff: 默认 DelayPolicy 延迟为 0；需要显式组合 BackOffDelayPolicy 与 RandomDelayPolicy 才有退避和 jitter;
- Retry-After: 在总预算内尊重上游提示;
- 跨层: 只允许明确的一层负责重试，避免次数相乘;

## 如何防止重试放大故障？

- retry storm: 配合限流、熔断和总重试预算;
- 写重复: 使用幂等键和服务端去重;
- 指标: 记录原始请求数、尝试数、最终结果和重试原因;
- 日志: 只在最终失败或策略异常时提升级别，避免每次尝试刷屏;

## 需要核对具体用法时查哪里？

- 官方入口: [正向代理和反向代理](https://github.com/cloudwego/cloudwego.github.io/blob/38744fc12990bb900765d0235c171a80c9fa2af9/content/zh/docs/hertz/tutorials/basic-feature/proxy.md)、[重试](https://github.com/cloudwego/cloudwego.github.io/blob/38744fc12990bb900765d0235c171a80c9fa2af9/content/zh/docs/hertz/tutorials/basic-feature/retry.md);
