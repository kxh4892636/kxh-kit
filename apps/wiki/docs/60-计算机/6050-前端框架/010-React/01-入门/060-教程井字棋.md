---
id: f11da39f-389f-45e2-90b9-34c0a58e5c35
---

# 教程井字棋

通过井字棋学会哪些核心 React 技术？如何提升 state、保存历史、实现时间旅行？

## 核心练习

- 组件拆分: Board, Square;
- props 传值: 把点击位置和当前值传给子组件;
- state 提升: 把棋盘状态放到父组件 Board;
- 不可变性: 创建新数组而不是修改原数组;

```jsx
function Board() {
  const [squares, setSquares] = useState(Array(9).fill(null));
  function handleClick(i) {
    const next = squares.slice();
    next[i] = "X";
    setSquares(next);
  }
  // ...
}
```

## 不可变性为什么重要

- 便于 React 检测变化并重新渲染;
- 便于实现撤销/时间旅行;
- 避免意外修改历史数据;

## 保存历史

- 状态提升到顶层 Game 组件;
- 保存 `squares` 数组的历史: `history = [{squares}, ...]`;
- 当前步骤 `currentMove`, 点击历史可跳转;

```jsx
const [history, setHistory] = useState([Array(9).fill(null)]);
const [currentMove, setCurrentMove] = useState(0);
const currentSquares = history[currentMove];
```

## 时间旅行

- 用 `slice(0, currentMove + 1)` 截断未来;
- 渲染历史按钮列表, 点击 `setCurrentMove(move)`;
- 用 `key` 标识每一步;

## 胜负判定

- 预先定义胜利线组合;
- 每步后检查当前棋盘是否满足任一组合;
- 显示获胜者或平局;
