---
id: b924e39a-c9f8-43fd-95ae-456948621475
---

# React Router 路由模式

React Router 有哪些路由模式？如何选择？

## 安装

- v7 推荐使用 `react-router` 包;
- 旧项目常见 `react-router-dom`, v7 已合并;

```bash
npm install react-router
```

## BrowserRouter

- 基于 History API;
- URL 路径干净, 推荐用于 Web 应用;
- 需要服务器支持回退到入口页;

```jsx
import { createRoot } from "react-dom/client";
import { BrowserRouter } from "react-router";

createRoot(document.getElementById("root")).render(
  <BrowserRouter>
    <App />
  </BrowserRouter>,
);
```

## HashRouter

- 使用 URL hash 管理路由;
- 不需要服务器配置;
- 不推荐用于正式产品;

```jsx
import { HashRouter } from "react-router";
```

## MemoryRouter

- 路由状态保存在内存;
- 适合测试和非浏览器环境;

```jsx
import { MemoryRouter } from "react-router";
```

## StaticRouter

- 用于服务端渲染;
- 传入当前 location 生成静态 HTML;

```jsx
import { StaticRouter } from "react-router/server";
```
