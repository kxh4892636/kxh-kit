---
id: ab6ff7e3-ed26-5d3f-b205-17dc1b65b52b
---

# 函数声明

## 语法格式

```go
// add 接收两个 int 参数并返回一个 int
func add(a int, b int) int {
	return a + b
}
```

## 参数类型简写

- parameter type elision: 相邻参数类型相同时, 可只在最后一个参数后声明类型;

```go
// a, b int 表示 a 和 b 都是 int
func add(a, b int) int {
	return a + b
}
```
