---
id: 19e316e9-d936-45e5-93f7-355119806625
---

# 包语义

## 包与集合有什么区别？

- 集合: 同一个元组只能出现一次;
- 包: 同一个元组可以多次出现;
- 关系模型: 纯关系代数默认按集合处理;
- SQL: 表默认按包处理, 同一行可以重复出现;

## 包上的运算与集合上的运算有什么不同？

- 独立个体: 包的运算把每个元组 (含重复项) 视为独立个体;
- 不去重: 运算结果不必去除重复元组;
- 可增殖: 结果中可以出现多个重复的元组;
- 代价: 结果的元组数取决于重复个数, 不能只按"有无"判断;

## 为什么需要包语义？

- 计数: 同一个值出现几次必须能表达, 例如某演员出演了几部电影;
- 统计: 求和、平均等聚集操作按重复个数累加;
- 默认行为: SQL 默认保留重复, 需要去重时显式使用 `DISTINCT`, 见 [重复与聚集](../030-SQL/080-重复与聚集.md);
- 对应操作: 包上的扩展操作见 [包的扩展操作](./100-包的扩展操作.md);

（source: 010-关系数据模型.md §包）
