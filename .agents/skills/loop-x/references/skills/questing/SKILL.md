---
name: questing
description: 拷问计划、决策或想法直到共同理解；当用户要求 stress-test、grill，或另一 skill 需要清空决策 frontier 时使用。
---

# Questing

把当前想法映射为 **design tree**：每个待定决策是一个节点，依赖它的决策位于下游。**frontier** 是前置已经确定、现在即可回答的全部节点。

## Rounds

1. 从用户目标、已有答案和现场事实建立 design tree，标出事实节点与决策节点。所有当前可见分支均已进入树时完成。
2. agent 通过文件、工具或并行 sub-agents 查明事实节点；用户只处理需要 judgement 的决策节点。事实来源可复核、未知事实已转化为正在进行的检索或明确 blocker 时完成。
3. 在一轮中询问整个 frontier。每个问题编号，说明它改变的下游边界，并给出有立场的推荐答案：

   ```text
   ❓ **Q1 — {标题}**：{问题与互斥选择}

   ➡️ {推荐答案及关键理由}
   ```

   同一轮问题互不依赖；依赖本轮答案的节点留在下一轮。用户回答完当前全部问题时，本轮完成。

4. 用答案更新 tree，显式记录已确定决策、被排除分支与新出现的节点，再计算下一轮 frontier。把握超过 90% 且不会改变用户意图的节点由 agent 回答并说明依据。

重复 rounds，直到每个可达分支都有结论，frontier 为空，未知假设为零或已成为带解除条件的 blocker。向用户复述共同理解并取得确认后，本 skill 完成；随后才由调用者采取实现或写入动作。
