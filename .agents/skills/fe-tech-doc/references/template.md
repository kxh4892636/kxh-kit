# {{技术文档名称}}

对应的所有代码仓库位置: apps/xxx; apps/yyy; ...

## 数据源

新增/更新/删除的数据接口; 每个数据接口, 单独一个 ### 标题 {{数据接口名称}};

### {{数据接口名称}}

新增/更新/删除的数据接口的 idl;
```idl
struct GetOverviewByVersionResponse {
    ...
    36: optional ScoreRichText ScoreProtectTopPcTitle //首屏pc标题
    37: optional ScoreRichText ScoreProtectTopAppTitle //首屏app标题
}
```

新增/更新/删除的数据接口的代码变更位置: path1; path2; ...

## 功能模块

根据产品文档和代码仓库, 将产品文档的若干需求划分为若干相互独立, 相互正交的功能模块, 保证每个功能模块可以<important>互不影响, 独立实现, 并行执行</important>;
使用[功能模块名称-平台类型]的格式, 分隔不同的平台类型(如pc, app, h5);

### {{功能模块名称-平台类型}}

功能模块的代码变更位置: path1; path2; ...
功能模块使用的技术栈: TypeScript, React, scss/tailwindcss, zustand, auxo, arco-mobile, react-query...

#### UI

##### 设计稿

figma 链接: figma link 链接, 供 figma mcp 使用
{{figma链接}}, 在产品文档中获取, 若不存在保留占位符, 用户自行补充;

技术文档撰写阶段, 必须添加下述文案, 保证 agent 能够调用 figma mcp 生成对应的前端组件
<important>请根据 figma 设计稿 {{figma链接}}, 生成对应的前端组件</important>;

##### 组件布局

读取产品文档中组件图片示意图或者用户描述, 生成组件布局, 辅助 figma 生成对应的前端组件
组件布局 = 组件内部的排版规则，决定组件结构, 组件排列, 组件样式等;

#### 代码设计

关注组织结构, 属性设计, 事件设计, 状态管理设计, 插槽设计, 边界/异常处理, 性能优化等;

#### 交互逻辑

关注用户操作、界面反馈、视图变化; 包括事件处理, 视图状态控制, 用户反馈, 组件间交互等;

#### 数据逻辑

关注数据来源、怎么处理、怎么存储、怎么使用; 包括数据请求, 数据状态管理, 数据转换与处理, 数据持久化与缓存, 业务规则, 数据流转等;

#### 埋点逻辑

主动上报用户行为与页面信息; 包含事件埋点, 性能埋点, 错误埋点, 用户信息埋点等;

#### 外部静态资源

外部静态资源: 例如图片, 图标, 字体等;
{{外部资源链接}} 列表, 在产品文档或者 figma 中获取, 若不存在保留占位符, 用户自行补充;