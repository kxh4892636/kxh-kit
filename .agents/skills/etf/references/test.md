# Test Principles

本文件维护 ETF 项目的前后端 BDD/TDD 原则；`verification.md` 只负责具体命令、联调和验收路径。

## 总原则

- 测试资产必须靠近被验证的业务模块，避免需求、验收步骤和回归入口只散落在对话、计划文档或临时记录中。
- 每次需求迭代先维护本次需求自己的测试或验收流程；只有明确通过后，才把稳定内容合并进模块级回归入口。
- 前端以 BDD 为主，验证用户可观察行为。
- 后端 TDD 原则暂时只保留占位，后续补充。
- 跨端需求按契约顺序推进：先确认 proto/API 语义，再分别维护前后端测试入口，最后做联调验收。

## 前端 BDD

- 前端 BDD 测试用例使用 Markdown 维护，验收执行走真实浏览器 E2E 路径。
- 新增或重构后的 `features/*` 模块必须在模块内创建 `test/` 目录，例如 `src/features/market-dashboard/test/`。
- 每个模块的 `test/index.md` 维护该模块经过多次需求迭代后的最新稳定验收流程，作为当前模块的回归入口。
- 每个需求单独创建 `test/yyyy-mm-dd-xxx.md` 保存本次需求的验收流程；日期使用创建文档当天的本地日期，`xxx` 使用简短、可读的 kebab-case 需求名。
- 需求开发时先更新对应 `test/yyyy-mm-dd-xxx.md`，记录目标、前置条件、操作步骤、验收断言、失败定位和执行结果。
- 只有当某个需求的真实浏览器 E2E 验收明确通过后，才把对应的稳定流程合并更新到 `test/index.md`；未通过、待确认或临时调试步骤不要写入 `index.md`。

## 前端 BDD 文档模板

`features/*/test/yyyy-mm-dd-xxx.md` 至少包含：

- 目标：本次需求要证明的用户结果。
- 前置条件：后端、前端、测试数据、浏览器视口和登录态等。
- 操作步骤：用户在真实页面中的动作。
- 验收断言：页面文本、控件状态、URL、请求结果、图表/截图、错误提示等可观察结果。
- 失败定位：失败时先检查的接口、状态、日志、DOM 或截图线索。
- 执行结果：执行时间、通过/失败结论、遗留问题；未通过时不得合并到 `index.md`。

`features/*/test/index.md` 只保留稳定回归流程，不记录一次性调试过程。

## 后端 TDD

待补充。
