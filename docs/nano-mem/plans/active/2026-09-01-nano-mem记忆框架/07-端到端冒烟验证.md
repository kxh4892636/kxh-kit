---
status: completed
blocked_by: ["06", "08"]
---

# 端到端冒烟验证

## 交付

产品级验证：打包安装（npm pack → 全局安装或 tarball 安装）后，`nm --help`/`--version`、记忆全命令、遗忘状态机、self skill 安装与 README 用法全链路冒烟通过；README 示例命令与真实行为逐项一致；领域校验通过。

## 范围

- `npm pack` 产物安装到隔离环境（或 `npm i -g <tarball>`），验证 bin `nm` 与 `files`（dist/skills/README/LICENSE）齐全。
- 冒烟脚本（`script/smoke.mjs` 或手工记录）：初始化 → add（中文/英文）→ search（命中与排序）→ use（多次，验证 rank 上升）→ list/state 过滤 → delete → gc（dry-run + 执行）→ self skill install/check/update/uninstall → 退出码与 `--json` 契约。
- 全量单测 + `vp check` + 领域校验（`check-domain.mjs`）通过。
- README 示例逐条复现（与 05/06 的文档一致）。

## 范围外

- 发布到 npm registry（本 issue 只做本地打包/安装验证）。
- 性能压测与超大规模库（单用户规模假设见 ADR-0001）。

## 直接依赖

- 06: self skill 管理实现与包布局；消费其安装/卸载命令做端到端验证。
- 05: 文档作为冒烟断言来源（README 示例与真实行为一致）。

## 验收

- [ ] `npm pack` 后 tarball 安装成功，`nm --version` 与包版本一致；`nm --help` 输出全部命令。
- [ ] README「快速开始」示例从头到尾一次跑通（含 self skill install → skill 出现在 `.agents/skills/nano-mem/`）。
- [ ] 高频使用 → 排名上升；久不用 → 休眠/已删的完整链路过一遍（时间模拟或手工阈值验证）。
- [ ] `pnpm --filter @kxh4892636/nano-mem test` 与 `check` 全绿；`node .agents/skills/loop-x/script/check-domain.mjs .` 通过。

## 上下文

- [spec](../../../docs/nano-mem/plans/active/2026-09-01-nano-mem记忆框架/spec.md) 工作环境与契约
- [story US-001..US-007](../../../docs/nano-mem/plans/active/2026-09-01-nano-mem记忆框架/story.md)
- README（05 产出）与 [ADR-0001/0002/0003](../../../docs/nano-mem/adr)

## 下一步

决策已澄清：/code-delivery

## 交付记录

- 交付物：端到端验证全链路 + 修复：SKILL.md「全线盘点」措辞与默认分区语义对齐（commit `1b81924`；A：feat/nano-mem 累计 01-08 全部提交）。
- 验证证据（A 最终门禁）：test 123/123 passed；覆盖率 95.33/90.00/98.01/96.09（≥80）；`vp check` pass（22 files）；build 61.22 kB。
- 打包安装：`npm pack` → `kxh4892636-nano-mem-0.1.0.tgz` → `npm i -g` 成功（prefix C:\nvm4w\nodejs）；`nm --version` → 0.1.0；`nm --help` 输出 8 记忆命令 + self 命令组。
- 冒烟（临时库，cwd=nm-smoke-agent）：add 中文/英文 3 条 → `nm search 记忆` 命中 #1 score 0.8266（rel 0.733/strength 1.0）→ use good×2 → reps 4/难度 2.11→4.73 → stats（总数/状态/FSRS 概览）→ gc --dry-run 零副作用 → `list --state all --agent *` 跨分区盘点。
- skill 安装到根目录：`nm self skill install`（默认 target `<cwd>/.agents/skills`）→ `C:\...\kxh-kit\.agents\skills\nano-mem\{SKILL.md,.nano-mem-managed.json}`；`nm self skill check --name nano-mem` → current；skill 已出现在会话可用 skill 目录（DSH 内建一层形态发现）。
- README 快速开始示例逐条复现通过；领域校验 node .agents/skills/loop-x/script/check-domain.mjs . 通过。
