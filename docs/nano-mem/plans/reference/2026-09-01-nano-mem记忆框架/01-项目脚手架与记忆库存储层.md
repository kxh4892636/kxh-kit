---
status: completed
blocked_by: []
---

# 项目脚手架与记忆库存储层

## 交付

`@kxh4892636/nano-mem` 包骨架就绪（bin `nm` 指向 `dist/main.mjs`，`vp pack`/`vp test`/`vp check` 可跑），记忆库存储层（schema、CRUD、FTS5 同步、确定性去重、CJK 字符切分预处理）实现并有单测锁定——后续 all CLI 命令的预制基础（Make the change easy）。

## 范围

- `packages/nano-mem` 包初始化：package.json（name/bin/files/engines>=24/scripts/devDeps catalog）、tsconfig、vitest 配置、`.gitignore`。
- `src/store.ts`：memories 表 + memories_fts（fts5 unicode61，rowid = memories.id）schema；add/get/list/delete/update-timestamps；同 agent+run_key+text_hash 去重；FTS 写入/删除同步；`src/split.ts` CJK 字符切分 + lowercase 归一（写入与查询同构）。
- 单测（vitest，覆盖率 ≥80%）：schema 约束、去重、FTS 命中中文 2 字词、CRUD 往返、切分函数。
- 不做：CLI 命令、FSRS 集成（issue 02）、检索评分与状态机（issue 04）、self（issue 06）。

## 直接依赖

- 无（根 issue，prefactoring）。

## 验收

- [ ] `pnpm --filter @kxh4892636/nano-mem test` 通过且覆盖率 ≥80%（lines/statements）。
- [ ] `pnpm --filter @kxh4892636/nano-mem check` 通过；`vp pack src/main.ts` 产出 `dist/main.mjs`（占位入口可运行）。
- [ ] 测试证明：中文文本「记忆信息很关键」经归一后，查询「记忆」与「信息关键」均命中（FTS5 + bm25 可取得分）。
- [ ] 测试证明：同 agent 同文本重复 add 返回既有 id，记录数不变；不同 run 允许重复。

## 上下文

- [spec](../../../docs/nano-mem/plans/active/2026-09-01-nano-mem记忆框架/spec.md) 实施决策「存储 schema」
- [ADR-0001](../../../docs/nano-mem/adr/0001-存储与检索采用sqlite与fts5无向量模型.md)
- [CONTEXT 记忆库](../../../docs/nano-mem/CONTEXT.md)
- 实测：node:sqlite FTS5/bm25 OK（v24.19）；trigram 已排除

## 下一步

决策已澄清：/code-delivery

## 交付记录

- 交付物：`packages/nano-mem` 包骨架（package.json/tsconfig/vitest.config/LICENSE/README 占位）+ `src/main.ts` 占位 + `src/split.ts`（CJK 切分）+ `src/store.ts`（schema/CRUD/FTS5 同步/去重）+ 测试（split 5 + store 11）；commit `701e233`（worktree 分支），A（feat/nano-mem）合并 `1fba92c`。
- 验证证据：`pnpm --filter @kxh4892636/nano-mem test`：A 集成 31/31 passed；覆盖率 statements/branches/functions/lines = 93.02/91.93/100/93.1（≥80）；`vp check` pass（11 files formatted、0 lint/type errors）；`vp pack src/main.ts` → `dist/main.mjs`，`node dist/main.mjs` → `nano-mem 0.1.0`（exit 0）。
- 验收点证据：中文「记忆信息很关键」查询「记忆」「信息关键」均命中且 bm25 为有限负值；同 agent 同文本重复 add 返回既有 id、记录数不变；不同 run 允许重复；delete 后 FTS 查不到（软删除 trashed）。
