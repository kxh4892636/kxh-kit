---
status: pending
blocked_by: ["01", "02"]
---

# 安装与真机验证

## 交付

`@kxh4892636/pi-nested-skill` 已安装进本机 pi；一次真实输入可在任意位置插入并展开多个嵌套 skill，且嵌套 skill 已被宿主发现（`$` 能解析到它们），留下可复现证据。

## 范围

- 执行 `pi install <abs path>/packages/pi-nested-skill`，确认 `~/.pi/agent/settings.json` 的 `packages` 包含该本地路径。
- 用 `pi -p` 做真机验证：输入含两个嵌套 skill 的 `$` 引用与一个未知名，从会话 jsonl 核对展开结果。
- 记录安装与卸载方式（README / 交付记录），不改 pi 上游。

## 直接依赖

- 01：嵌套发现；消费其 `resources_discover` 返回的 `skillPaths`。
- 02：`$` 补全与 `input` 展开；消费其 `src/reference.ts`、`src/suggest.ts`。

## 验收

- [ ] `pi list` 或 `~/.pi/agent/settings.json` 显示已安装本地包
- [ ] `pi -p` 一条输入含 `$questing` 与 `$code-spec`：会话 jsonl 的用户消息出现两个 `<skill name=...>` 块，`location` 指向 `.agents/skills/nano-flow/references/skills/*/SKILL.md`
- [ ] 同一输入含 `$definitely-not-a-skill`：该 token 在用户消息中原样保留
- [ ] 顶层 skill 引用 `$nano-flow` 仍可展开，且会话无同名 collision 告警
- [ ] 嵌套 skill 出现在宿主可用技能集合（`$` 能解析即证明其已被 `resources_discover` 注册）
- [ ] 交付记录登记安装命令、验证命令、证据路径与卸载方式

## 上下文

- [spec](spec.md)
- [ADR-0001](../../../adr/0001-嵌套skill以扩展接缝交付.md)
- pi 包安装：`.../pi-coding-agent/docs/packages.md`
- pi 会话格式：`.../pi-coding-agent/docs/session-format.md`
- 样例嵌套 skill：`.agents/skills/nano-flow/references/skills/{questing,code-spec}/SKILL.md`（本地）

## 下一步

/code-delivery

## 交付记录

待完成登记。
