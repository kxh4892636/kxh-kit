---
status: pending
blocked_by: [01]
---

# 模型与模板管理

## 交付

`anki-cli model` 子命令组可用：查询 Model 的字段/模板/CSS，创建自定义 Model，更新模板与样式，增删改名重排字段。

## 范围

做：modelNames、modelFieldNames、modelStyling、modelTemplates、createModel（自定义 fields + 模板 + CSS）、updateModelStyling、updateModelTemplates、addModelField（指定位置插入）、removeModelField（需 `--yes`）、renameModelField、repositionModelField。

不做：笔记内容操作。

## 直接依赖

- 01：消费其 AnkiConnect client、输出归一化与命令分发骨架。

## 验收

- [ ] 单测（mock AnkiConnect）覆盖 11 个工具，含 removeModelField 未带 `--yes` 被拒绝；
- [ ] 真实 Anki 冒烟：createModel 创建测试 Model → 更新模板/CSS → 字段增改重排 → 验证 modelTemplates/modelStyling 输出。

## 上下文

- spec：../spec.md
- 参考实现：`.temp/anki-mcp-server/src/mcp/primitives/essential/tools/`（model 系）

## 下一步

/implement
