---
status: pending
blocked_by: [01]
---

# 复习流程

## 交付

`anki-cli review` 子命令组可用：同步 AnkiWeb、取到期卡片、按状态过滤卡片、展示卡片正反面、评分并调度下次复习；卡片内容按其自身模板渲染（反向卡、cloze 方向正确）。

## 范围

做：sync、get_due_cards（deck 过滤）、get_cards（状态过滤：due/new/learning/suspended/buried）、present_card（展示 question/front）、rate_card（Again/Hard/Good/Easy）；卡片渲染与 HTML 清洗参照上游 `extractRenderedCardContent`。

不做：交互式复习会话（以一次性子命令串行完成一轮复习）。

## 直接依赖

- 01：消费其 AnkiConnect client、输出归一化与命令分发骨架。

## 验收

- [ ] 单测（mock AnkiConnect）覆盖 5 个工具，含模板渲染方向（反向卡/cloze）；
- [ ] 真实 Anki 冒烟：sync → get_due_cards → present_card → rate_card 走完一轮，卡片调度状态变化可见。

## 上下文

- spec：../spec.md
- 参考实现：`.temp/anki-mcp-server/src/mcp/primitives/essential/tools/`（review 系）、`src/mcp/utils/anki.utils.ts`

## 下一步

/implement
