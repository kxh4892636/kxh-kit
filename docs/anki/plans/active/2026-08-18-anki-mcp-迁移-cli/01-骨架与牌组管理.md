---
status: pending
blocked_by: []
---

# 骨架与牌组管理

## 交付

`anki-cli` 可安装执行；`anki-cli deck list|stats|create|change` 四个命令端到端可用，输出 JSON。

## 范围

做：

- 包骨架：`packages/anki-cli`（`@kxh4892636/anki-cli`，ESM，vp pack 构建，bin `anki-cli`）；
- AnkiConnect client：`invoke<T>(action, params)`，Node 内置 fetch，read-only 拦截（WRITE_ACTIONS）、超时、apiKey，行为参照上游 `src/mcp/clients/anki-connect.client.ts`；
- 命令分发层：commander 分组子命令、全局 flag（`--anki-connect` / `--read-only`）、输出归一化（成功 JSON → stdout，错误 JSON → stderr + 非零退出）；
- deck 组 4 个工具：listDecks（可选 per-deck 统计）、deckStats、createDeck（`Parent::Child`，最多 2 级）、changeDeck。

不做：其他工具组；卡片渲染。

## 直接依赖

（无——根 issue）

## 验收

- [ ] 单测（mock AnkiConnect）覆盖 client 错误处理、read-only 拦截与 deck 组 4 命令；
- [ ] 真实 Anki 冒烟：`anki-cli deck list` 返回牌组 JSON；`deck create anki-cli-smoke` 成功后清理。

## 上下文

- spec：../spec.md
- 参考实现：`.temp/anki-mcp-server/src/mcp/clients/anki-connect.client.ts`、`src/mcp/primitives/essential/tools/`（deck 系）

## 下一步

/implement
