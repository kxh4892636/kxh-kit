---
status: pending
blocked_by: [01]
---

# 统计

## 交付

`anki-cli stats` 子命令组可用：全集合聚合统计（含 per-deck 分解）与复习历史分析。

## 范围

做：collection_stats（per-deck 分解 + 全集 card-state 计数）、review_stats（时间模式、保留率、连续学习记录；省略 deck 时全集聚合）。

不做：单 deck 统计（deckStats 属 01）。

## 直接依赖

- 01：消费其 AnkiConnect client、输出归一化与命令分发骨架。

## 验收

- [ ] 单测（mock AnkiConnect）覆盖 2 个工具，统计口径与上游一致（counts 块镜像 deck 浏览器、states 块按真实状态计数）；
- [ ] 真实 Anki 冒烟：`anki-cli stats collection` 与 `stats review` 返回统计 JSON。

## 上下文

- spec：../spec.md
- 参考实现：`.temp/anki-mcp-server/src/mcp/primitives/essential/tools/`（stats 系）、`src/mcp/utils/stats.utils.ts`

## 下一步

/implement
