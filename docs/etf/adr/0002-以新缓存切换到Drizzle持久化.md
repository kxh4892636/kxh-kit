# 以新缓存切换到 Drizzle 持久化

ETF 使用 Drizzle ORM 与 SQLite，SQL migrations 管理新缓存数据库，行情模块通过 store 隔离持久化细节。此决策替代源项目 ADR 0002 的 GORM 实现选择，保留事务写入与行情策略分离，不为假设中的第二种数据库预建适配框架。

本次不迁移旧 GORM 文件，源数据库保留，使用新数据目录避免覆盖；代价是旧缓存中超出上游当前返回窗口的历史不保证可恢复。用户已确认该取舍，不将缓存重建等同于完整历史迁移。
