---
status: pending
blocked_by: ["02"]
---

# 行情看板迁入与完整回归

## 交付

从 kxh-kit 启动完整 ETF 看板，证券切换、K 线、均线与虚拟均线等现有操作可用，后端使用 Node。

## 范围

迁入 apps/etf-dashboard，统一依赖和构建配置；以 Hono hc/TanStack Query 替换 Connect 与生成类型，保留业务界面和图表逻辑。迁移前端单测、浏览器组件测试与全部五组 E2E，Node 编排只管理自己的服务进程。完成启动/配置/缓存说明、根目录门禁接入和交付记录；不导入嵌套仓库、技能、Go 或 protobuf 工具链。

## 直接依赖

- 02：消费完整证券/日线 HTTP 契约、后端 AppType、可启动的 Node 服务及缓存行为；01 的基础能力经 02 传递，不重复列为直接依赖。

## 验收

- [ ] 全新安装后按文档启动前后端，默认行情、证券切换、图表缩放拖拽/tooltip、均线和虚拟均线保持现有可观察行为。
- [ ] 源纯逻辑和浏览器组件回归迁移通过，前端仅类型引用后端契约，浏览器构建不包含数据库或服务启动模块。
- [ ] 五组 E2E（默认行情、核心交互、窄屏、停服恢复、虚拟均线）在受控数据与受管 Node 服务下通过；停服恢复不可通过复用外部服务而跳过。
- [ ] E2E 使用独立端口和数据，结束时回收自身 Node 进程与临时资源，不停止用户的服务或修改源数据库。
- [ ] 两个应用的类型检查、测试与构建通过，检查迁入代码无 Go/Connect/protobuf 运行依赖；完成改动检查与 Standards/Spec 审查，记录实际提交。
- [ ] 交付文档说明新缓存、旧数据保留和历史窗口限制；按准入已确认的集成方式将验证后的应用合入 kxh-kit。

## 上下文

- [执行规格](spec.md)
- [领域语言](../../../CONTEXT.md)
- [接口决策](../../../adr/0001-前后端共同迁移为类型化接口.md)
- [缓存决策](../../../adr/0002-以新缓存切换到Drizzle持久化.md)
- [回归决策](../../../adr/0003-保留代码化行情回归.md)

## 下一步

/code-delivery；先完成 Plan 级 /dev-gate，按直接依赖顺序领取。

## 交付记录

尚未实施；完成登记前填写实际交付物、验证命令与结果、审查结论和提交。
