---
status: accepted
---

# 子仓克隆由工作区配置定位

`loopx workspace` 把普通浅克隆直接放在工作区配置的 `path`，不再使用默认外置目录或 `workspace.local.yaml` 记录本机路径；额外 worktree 均基于该子仓克隆生成，并在每次生成时显式指定目标路径。这以可版本控制、可直接观察的单一路径契约取代了外置克隆与工作区 worktree 的双路径模型，并取代 [ADR-0004](0004-子仓克隆存储使用浅克隆.md)；保留其普通浅克隆与基准分支选择。
