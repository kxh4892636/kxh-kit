---
status: accepted
---

# 子仓克隆由工作区配置定位

`nnf workspace` 使用 `git clone --depth 1 --branch <基准分支>` 把普通浅克隆直接放在工作区配置的 `path`；额外 worktree 均基于该子仓克隆生成，并在每次生成时显式指定目标路径。这形成可版本控制、可直接观察的单一路径契约；浅克隆把磁盘与网络成本限制在基准分支的最新快照，更新使用 fetch 与 fast-forward-only。
