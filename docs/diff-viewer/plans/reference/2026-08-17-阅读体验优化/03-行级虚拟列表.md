---
status: completed
blocked_by: []
---

# 行级虚拟列表

## 交付

打开含数万行变更的单个文件 (lockfile 重写、大型新增/删除文件), 首屏与滚动不再卡死: 只有视口附近的行挂载到 DOM, unified 与 split 两种布局同样受界。

## 范围

做:

- 引入 `@tanstack/react-virtual` (已批准的新依赖), 在文本 diff 渲染链路 (TextDiffViewer / DiffChunk / SideBySideDiffChunk) 实现行级 windowing; 评论行、展开按钮等变高行由虚拟器动态测量。
- 新增/删除文件的全量行 (现 `useExpandedLines.ts:411` 强制全渲染处) 同样纳入 windowing。
- 保留导航契约: 键盘光标、评论跳转、文件树点击依赖行 DOM id (`file-{fileIndex}-chunk-{chunkIndex}-line-{lineIndex}`, domHelpers.ts:8-9), 虚拟化后以"先确保目标行挂载再滚动"编排维持可用 (先例: useLazyDiffRendering 的 ensureFilesRenderedUpTo + rAF 重试)。

不做: 文件级卸载 (04 的范围); Markdown/Notebook/Image 专用 viewer 的虚拟化; 语法高亮机制本身改造。

## 直接依赖

(无)

## 验收

- [x] 组件测试: 巨型 fixture (数万行) 下挂载的 DOM 行数有界 (视口 + overscan 上限)。
- [x] 键盘导航/评论跳转能定位到当前未挂载的行 (先挂载后滚动), 组件测试或 e2e 证明。
- [x] 既有 e2e (diff-render 等) 与 `pnpm ready` 适用门禁全绿。

## 交付物与证据

- 交付: `65c0deb`、`21bcbf4`、`f28e75d`。引入 `@tanstack/react-virtual`; `virtualization/` module 统一滚动容器、动态测量、spacer、行模型与挂载后 DOM-id 重试; `TextDiffViewer` 提供跨 chunk windowing, unified/split 的巨型单 chunk 再使用行 windowing; 词级差异改为按挂载行惰性计算。
- 结构验收: 20,000 行单 chunk unified/split 只挂载视口附近行; 200×100 行多 hunk 文件同时挂载 chunk <10、DOM 行 <1,000、展开按钮 <10; 跨 chunk 跳转可从未挂载的第 150 个 chunk 定位到目标行; 定向测试 8/8。
- 完整验证: typecheck 通过; diff-viewer 全量单测 869 passed / 1 skipped; renderer/main build 通过; Electron e2e 12/12。
- `pnpm ready` 偏差: 本次变更的 12 文件 scoped `vp check` 全绿; 全仓 `vp check` 仅被基点已存在的 `skills/loop-x/tdd/mocking.md` 格式问题阻断; 跳过格式后的全仓 lint/typecheck 为 0 error、15 条既有 warning。测试与构建已分别完整执行并通过。
- code review: Spec 轴复查 0 项; Standards 的命名、目录、英文注释、增量返回类型和重复虚拟管线已修复。剩余为基点已存在的两个巨型 diff 渲染组件超过 389 行; 本次已将 `DiffChunk.tsx` 从高点 626 收拢至 572 (低于基点 577), split 主组件回到基点规模附近，完整拆分不纳入本 issue。

## 上下文

- 渲染链路与成本事实见 [spec.md](spec.md)「问题」与「实施决策」
- 滚动容器约定: `apps/diff-viewer/src/client/constants/navigation.ts:7` (`main.overflow-y-auto`)

## 下一步

已完成
