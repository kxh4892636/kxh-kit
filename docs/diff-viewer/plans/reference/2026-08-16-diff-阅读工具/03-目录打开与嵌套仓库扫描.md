---
status: completed
blocked_by: ["01"]
---

# 目录打开与嵌套仓库扫描

## 交付

打开本地目录，递归识别其中所有 git 仓库（含仓中仓、submodule），以父子层级展示仓库列表，扫描过程有进度反馈。

## 范围

做：

- 目录选择对话框与打开流程。
- 扫描器：发现 `.git` 即记为仓库条目并继续向其内部递归（仓中仓为父子条目）；submodule 作为子仓库条目；默认跳过 `node_modules`/`dist` 等重型目录与 `.git` 内部；深度上限 8；异步扫描 + 进度反馈。
- 侧栏仓库树（父子层级），勾选仓库加入视图。

不做：多仓库文件树（04）、远程扫描（06）。

## 直接依赖

- 01：消费其 Electron 骨架与渲染层。

## 验收

- [x] 打开含嵌套仓库（仓中仓 + submodule）的目录，全部仓库以父子层级列出，`node_modules` 未被遍历，扫描期间有进度指示。

## 交付物与证据

- 交付物：`src/main/repo-scan/repo-scanner.ts` 递归扫描器（仓中仓父子条目、gitfile 识别 submodule、重型目录与 `.git` 内部跳过、深度上限 8、逐目录进度事件）；`workspace:pick-directory`/`workspace:scan`/`workspace:scan-progress` IPC 与 preload bridge 扩展；`POST /api/active-repository` 激活仓库切换；`src/client/repo-tree/` 侧栏面板（Open Folder、进度指示、父子层级树、勾选）；`createNestedRepoFixture` e2e 基建。
- 合回 commit：`114014b`（单 commit），fast-forward 合入 main。
- 验证证据：单测/组件测试 58 文件 680 passed + 1 skipped（新增 17：扫描器 8 + api-router 3 + 面板 6）；e2e 5/5 passed（`repo-scan.spec.ts` 两条即验收的自动化等价证据）；`pnpm ready` worktree 复跑 exit 0、合回后 main 复验 exit 0。
- code review：Standards 轴无硬性违规（存量目录文件数超上限非本 diff 新增；判断项均接受——e2e 观测钩子 `DIFF_VIEWER_SCAN_DELAY_MS` 有注释约束、`fetchRevisions` catch 为既有行为平移）；Spec 轴无缺失，5 项声称取舍核实 4 项属实可接受。
- 接受偏差（内容冻结前记录）：
  - 勾选状态存于 renderer 面板内（04 需要时再上提）；取消勾选最后一个仓库时 diff 保持现状（归 04）；
  - 进度事件逐目录一条 IPC 未节流（深度 8 + 跳过约束下规模可控）；
  - **（更正 lane 汇报描述）** 打开零仓库目录的实际行为：主视图静默保留旧 diff，仅面板显示 "No repositories found"，并非走 error 全屏；04 做多仓库视图时建议把面板提到 error/loading 早退分支之外；
  - e2e fixture 的 submodule 用 gitfile 等价落盘布局（避免 Windows sh/file 传输崩溃），git 经 gitfile 工作已由 e2e 真实命令验证。

## 上下文

- [spec.md](spec.md)

## 下一步

/implement
