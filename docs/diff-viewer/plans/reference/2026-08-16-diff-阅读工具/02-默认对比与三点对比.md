---
status: completed
blocked_by: ["01"]
---

# 默认对比与三点对比

## 交付

打开仓库即呈现有意义的默认 diff：当前分支与远程默认分支的三点对比，无需手工选择 base/target。

## 范围

做：

- 默认对比 = 当前分支 vs 远程默认分支的三点对比；远程默认分支判定顺序：`origin/HEAD` symref → `origin/main` → `origin/master` → 第一个远程分支。
- 降级链：无远程或 detached HEAD 时，默认对比降级为未提交改动 vs HEAD。
- branch 间对比的三点（默认）/ 两点切换选项。

不做：对比矩阵的其他形态（branch/commit/staged/working-tree 选择能力由 fork 自带，01 已落地）。

## 直接依赖

- 01：消费其 GitDiffParser 数据层与 fork 自带的对比选择 UI。

## 验收

- [x] 处于 feature 分支的仓库打开后，默认展示当前分支与远程默认分支的三点 diff。
- [x] 无远程的仓库打开后，默认展示未提交改动 vs HEAD。

## 交付物与证据

- 交付物：`resolveInitialSelection` 完整版（四级远程默认分支判定 + 降级链）、`getCurrentBranch`、`getOriginDefaultBranch` 重写（单次 `for-each-ref`）、DiffQuickMenu「默认对比」预设（三点/两点可逆切换，fork 改动 #4）、`createFixtureRepoWithOrigin` 测试基建。
- 合回 commit：`8445c6c`（主体）、`31e23b0`（门禁加固 + 审查判断项），fast-forward 合入 main。
- 验证证据：单测 56 文件 663 passed + 1 skipped；e2e 3/3 passed（`default-compare.spec.ts` 两条即验收的自动化等价证据）；门禁加固后**连续 3 次 `pnpm ready` exit 0**（每轮先清 vp 缓存保证完整执行）；合回后 main 上 `pnpm ready` 复验 exit 0。
- code review：Standards 轴无硬性违规（判断项 1 处接受、1 处已在 `31e23b0` 简化）；Spec 轴无缺失，4 项声称偏差逐条核实属实且可接受（含 `getOriginDefaultBranch` 重写修复的上游 simple-git 误判 bug）。
- 接受偏差（内容冻结前记录）：
  - 无远程的干净仓库打开默认展示空 diff（降级链字面行为，旧 `HEAD^..HEAD` 可从 Quick Menu「Previous commit」到达），UX 优化留待后续；
  - `pnpm ready` 并行负载 flaky 的根因为超时撞线而非 git spawn 失败，加固手段 = vitest testTimeout 30s/20s + `maxWorkers: 8` + testing-library `asyncUtilTimeout: 10s` + loop-kit `cli.test.ts` testTimeout 30s；
  - detached HEAD 降级仅有单测无 e2e（验收未要求）。

## 上下文

- [spec.md](spec.md)

## 下一步

/implement
