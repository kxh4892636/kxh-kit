---
status: completed
blocked_by: []
---

# 评论持久化与一键复制

## 交付

评论持久化到 userData 的 JSON（按 仓库+对比 为键，不污染仓库），重启应用不丢；一键复制全部评论为可直接粘贴给 AI 的 Markdown。

## 范围

做：

- fork 评论存储从 localStorage 替换为 userData 文件存储。
- 评论锚点 = 仓库 + 文件路径 + 行号 + side（new 为主，支持 old）+ 可选代码片段快照。
- 一键复制格式：Markdown 列表，每条含 `文件:行号`、引用代码块、评论正文。

不做：评论导出为文件、跨设备同步。

## 直接依赖

- 01：评论 UI 与行/范围锚点机制来自 fork，本 issue 替换其存储层与格式化输出。

## 验收

- [x] 在 diff 行/范围写评论，重启应用后评论仍在。
- [x] 一键复制得到符合格式的 Markdown（每条含 `文件:行号`、代码块、正文）。

## 交付物与证据

- 交付物：`src/main/comment-persistence.ts`（userData JSON 持久化，按 仓库+对比 为键）、`src/utils/comments-markdown.ts`（Markdown 格式化）、`src/main/comment-sessions.ts` 与 `api-router.ts` 持久化接线、`src/client/hooks/useDiffComments.ts` 改走 API、`e2e/comments-persistence.spec.ts`（2 条新 e2e）、`e2e/isolated-user-data.ts`（e2e 隔离 userData 夹具）。
- 合回：fast-forward 至 `b305dc1`（feat(diff-viewer): 05 评论持久化与一键复制，18 文件 +1169/-119）。
- 验证证据：729 单元/组件测试通过；e2e 8/8 通过（含新增持久化 2 条）；合回后 main `pnpm ready` exit 0。
- 验证环境：Windows 11 + Node 22 + pnpm 11，本地 `pnpm ready`（typecheck + lint + 全部测试 + 构建）。
- code review 结论：Standards 无硬性违规；判断项均接受——api-router 三处 `commentStoreFor→hydrate→persisted` 序列可提取（轻微，不改）、`persisted: Promise` 语义以注释钉住（接受）、`src/main/` 23 文件与 `src/utils/` 14 文件超 13 上限属存量+同域加剧（留后续归并）；新增 `src/utils/comments-markdown.ts` 位于 vendored 区，判定为可接受放置（不改上游存量、kebab-case 领域名）。
- 接受偏差：复制格式去 diff 头（每条约简）、不迁移旧 localStorage 数据、`DIFIT_USER_DATA_DIR` 环境变量进入生产路径、写盘失败仅 log 不阻塞 UI、`useLocalComments.ts` 作为 dead code 留存。

## 上下文

- [spec.md](spec.md)

## 下一步

已完成
