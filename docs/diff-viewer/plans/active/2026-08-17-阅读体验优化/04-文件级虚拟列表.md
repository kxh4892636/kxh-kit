---
status: pending
blocked_by: ["03"]
---

# 文件级虚拟列表

## 交付

浏览含数百个文件的 diff 时, 远离视口的文件块被卸载, DOM 总量不随滚动/跳转累积; 文件树点击与键盘导航仍可跳转任意文件。

## 范围

做:

- 将现 `useLazyDiffRendering` 的"只挂不卸"改为 windowing: 远离视口的文件块卸载, 回到附近时重新挂载; 每文件粘性头部 (DiffViewerHeader) 行为保持。
- 文件树点击/键盘跨文件导航的"先确保挂载再滚动"编排保持可用 (现有 ensureFilesRenderedUpTo + rAF 重试语义保留)。
- 复用 03 建立的 react-virtual 集成与滚动容器接线, 不引入第二套虚拟化机制。

不做: 行级 windowing (03 已完成); 文件树本身 (FileList) 的虚拟化。

## 直接依赖

- 03: 消费其 react-virtual 集成、滚动容器 (`main.overflow-y-auto`) 接线与"先确保挂载再滚动"导航编排契约, 避免两套虚拟化机制并存。

## 验收

- [ ] 组件测试: 多文件 fixture 下滚动到底部后, 远离视口的文件块不在 DOM 中。
- [ ] e2e: 文件树点击靠后的文件可正常跳转定位; 既有 e2e 与 `pnpm ready` 全绿。

## 上下文

- 现状: `apps/diff-viewer/src/client/hooks/useLazyDiffRendering.ts` (首批 8 文件, IntersectionObserver 挂载, 从不卸载)
- [spec.md](spec.md)

## 下一步

/implement (待 03 完成后领取)
