---
status: in_progress
blocked_by: ["02"]
---

# 真实通道切换冒烟与文档同步

## 交付

真实 npm 通道切换的可复核证据，以及领域语言同步：`update` 只准备版本、`start` 才切换运行版本的行为在真实包上被验证。

## 范围

隔离环境下用真实 `@deepseek-ai/dsh` 验证 `--tag` 通道切换与回退；按需收录「发布通道」到 `docs/dsh/CONTEXT.md` 并运行 `check-domain`；补打包安装后的 CLI 证据与仓库回归。冒烟发现本机配置源（npmmirror）缺 alpha 依赖包，按用户指示追加「源缺少该包/版本时改用 npm 官方源重试一次」的兜底（`src/runtime/versions.ts`），故本项包含这一处生产代码改动。

## 直接依赖

- 02：冒烟验证的是 02 的通道切换行为；消费其 CLI 契约（`update`、`start --tag`）与 `status.tag`、`status.prepared`。

## 验收

- [x] 隔离 `LOCALAPPDATA` 与独立端口下：`start` 以 `latest`（0.1.2-rc.1）运行 → `update --tag alpha` 安装 0.1.5-alpha.1 且运行版本与 PID 不变 → `start` 切到 0.1.5-alpha.1 → `update --tag latest` → `start` 回到 0.1.2-rc.1。
- [x] 冒烟前后 `snapshot(3080)` 的端口所有者一致，未触碰运行中的实例。
- [x] 通道记忆在真实命令序列中生效：省略 `--tag` 的 `update` 沿用上次通道。
- [x] `pnpm pack` 产物安装到独立目录后 `dsh-alive --help` 与 `dsh-alive update --tag next` 可用。
- [x] `docs/dsh/CONTEXT.md` 收录「发布通道」并 `check-domain.mjs .` 通过。
- [x] 仓库回归 `pnpm exec vp run -r --no-cache test` 通过。

## 上下文

- [执行契约](spec.md)。
- [前置](02-移除自动更新与显式update命令.md)。
- 领域文档：[docs/dsh/CONTEXT.md](../../../CONTEXT.md)。

## 下一步

/code-delivery；等待 02 completed。

## 交付记录

交付物：`src/runtime/versions.ts` 的源兜底（`OFFICIAL_REGISTRY` + 仅对 `ETARGET`/`E404`/`No matching version` 类错误改用 npm 官方源重试一次）；`src/runtime/versions.test.ts` 三处用例（回退成功、两次都失败保留两段原因、网络故障不回退）；README「版本与发布通道」补源兜底说明；`docs/dsh/CONTEXT.md` 收录「发布通道」；spec 更新源兜底决策与上游事实；真实冒烟脚本 `.flow/dsh-alive-channel-smoke.mjs`。

验证证据：真实 npm 通道切换冒烟（隔离 `LOCALAPPDATA` + 独立端口 65504 + 隔离 `DSH_HOME`）依次通过——`start --tag latest` 运行真实 0.1.2-rc.1（pid 18384）→ `update --tag alpha` 安装真实 0.1.5-alpha.1（经官方源兜底）且 `version`/`pid` 不变、`state.json` 记 `{0.1.5-alpha.1, alpha}` → 裸 `update` 沿用 alpha → `start` 切到真实 0.1.5-alpha.1（pid 17168，HTTP 401 未认证首页）→ `update --tag latest` 准备 0.1.2-rc.1 → `start` 回到真实 0.1.2-rc.1（pid 25920）→ `stop` 停止；3080 端口所有者冒烟前后同为 pid 14324。打包安装：`pnpm pack` 产物安装到独立目录后 `.bin` 仅有 `dsh-alive`，`--help` 列出 `start`/`update`/`stop`/`status`/`logs`，`update --port 59123 --tag next` 返回 `{state:"stopped", tag:"next", prepared:"0.1.2-rc.1"}` 且 `state.json` 记为 `{"version":"0.1.2-rc.1","tag":"next"}`。包门禁：`check` 通过、`test` 47 项通过、`test:coverage` statements 91.48%、branches 87.59%、functions 89.18%、lines 92.79%；`check-domain.mjs .` 通过；仓库回归见下方验证记录。

## 验证记录

- 上游事实（本项冒烟取证）：本机 `npm config get registry` = `https://registry.npmmirror.com`；`npm view @deepseek-ai/dsh-fs-local@0.1.5-alpha.1 version --json` 在该源返回 `E404`，加 `--registry=https://registry.npmjs.org` 返回 `"0.1.5-alpha.1"`；`npm view @deepseek-ai/dsh dist-tags --json`（官方源）为 `latest=0.1.2-rc.1`、`next=0.1.2-rc.1`、`alpha=0.1.5-alpha.1`。首次冒烟在无兜底时以 `ETARGET No matching version found for @deepseek-ai/dsh-fs-local@^0.1.5-alpha.1` 失败，运行中的实例与 `state.json` 均未被改动——这正是追加源兜底的依据。
- 兜底边界：网络故障（`ECONNREFUSED` 等）不回退，避免等待翻倍并保留真实网络错误；离线用例因此仍快速回退到缓存版本。
- 仓库回归：最终一次 `pnpm exec vp run -r --no-cache test` 12 任务全部通过（exit 0）。此前两次未通过均非最终代码问题：第一次在实现进行中运行，`versions.test.ts` 与半完成的改动不一致；第二次在 `packages/nano-flow` 的 `flow.test.mjs` 上因 5 秒默认超时失败（1143/1144），该包单独运行 1144/1144 通过，与上一轮交付记录的并行负载抖动一致。
- 包内全量运行曾出现一次 `instance.test.ts` 退避用例偶发失败（`Expected: "backoff" Received: "running"`），伴随并发的真实 npm 安装负载；单文件运行与随后连续两次全量运行均 47/47 通过。未修改该既有用例，如实记录为观察到的时序抖动。
