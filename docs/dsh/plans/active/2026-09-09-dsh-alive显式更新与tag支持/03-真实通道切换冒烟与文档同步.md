---
status: pending
blocked_by: ["02"]
---

# 真实通道切换冒烟与文档同步

## 交付

真实 npm 通道切换的可复核证据，以及领域语言同步：`update` 只准备版本、`start` 才切换运行版本的行为在真实包上被验证。

## 范围

隔离环境下用真实 `@deepseek-ai/dsh` 验证 `--tag` 通道切换与回退；按需收录「发布通道」到 `docs/dsh/CONTEXT.md` 并运行 `check-domain`；补打包安装后的 CLI 证据与仓库回归。不改 `packages/dsh-keep-alive/src` 的行为。

## 直接依赖

- 02：冒烟验证的是 02 的通道切换行为；消费其 CLI 契约（`update`、`start --tag`）与 `status.tag`、`status.prepared`。

## 验收

- [ ] 隔离 `LOCALAPPDATA` 与独立端口下：`start` 以 `latest`（0.1.2-rc.1）运行 → `update --tag alpha` 安装 0.1.5-alpha.1 且运行版本与 PID 不变 → `start` 切到 0.1.5-alpha.1 → `update --tag latest` → `start` 回到 0.1.2-rc.1。
- [ ] 冒烟前后 `snapshot(3080)` 的端口所有者一致，未触碰运行中的实例。
- [ ] 通道记忆在真实命令序列中生效：省略 `--tag` 的 `update` 沿用上次通道。
- [ ] `pnpm pack` 产物安装到独立目录后 `dsh-alive --help` 与 `dsh-alive update --tag next` 可用。
- [ ] `docs/dsh/CONTEXT.md` 收录「发布通道」并 `check-domain.mjs .` 通过。
- [ ] 仓库回归 `pnpm exec vp run -r --no-cache test` 通过。

## 上下文

- [执行契约](spec.md)。
- [前置](02-移除自动更新与显式update命令.md)。
- 领域文档：[docs/dsh/CONTEXT.md](../../../CONTEXT.md)。

## 下一步

/code-delivery；等待 02 completed。

## 交付记录

完成登记前填入并保留：交付物与验证证据链接。
