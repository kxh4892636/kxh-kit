---
status: pending
blocked_by: []
---

# 依赖声明与文档对齐

## 交付

两个插件的 DSH 相关声明与实际运行的 `latest` 发布通道（`0.1.5-rc.1`）一致：peer 范围改 `^0.1.5-rc.1`，上游 opt-in 工具包 `@deepseek-ai/dsh-tool-session-query` 也随之对齐并留在 `dependencies`；两个 README 与 ADR-0003 的版本字面量同步，文档不再与声明矛盾。

## 范围

- 做什么：`packages/dsh-nested-skill/package.json` 的 peer `@deepseek-ai/dsh-fs`、`@deepseek-ai/dsh-skill` 改 `^0.1.5-rc.1`；`packages/dsh-session-manager/package.json` 的 peer `@deepseek-ai/dsh-llm`、`@deepseek-ai/dsh-tools` 与其 `dependencies` 的 `@deepseek-ai/dsh-tool-session-query` 改 `^0.1.5-rc.1`；两个 README 的兼容段落更新为 `0.1.5-rc.1`；`docs/dsh/adr/0003-内容搜索经上游opt-in工具与索引启用交付.md` 的正文与 Consequences 版本字面量同步为 `^0.1.5-rc.1`（决策本身不变）。
- 不做什么：不改 `@deepseek-ai/cordis`（`^4.0.2`）与 `@deepseek-ai/schemastery`（`^3.18.2`）范围；不 bump 包版本号、不产出 tarball、不发布 npm；不新增 ADR、不改 `docs/dsh/CONTEXT.md`；不放宽为跨 minor 的开放范围；不改工具实现或 `cordis.patch.yml`。

## 直接依赖

- 无。

## 验收

- [ ] 两个 `package.json` 中所有 `@deepseek-ai/dsh-*` 声明均为 `^0.1.5-rc.1`，`cordis`/`schemastery` 范围未变
- [ ] 两个 README 的兼容段落写明 DSH `0.1.5-rc.1`（`latest` 发布通道）
- [ ] ADR-0003 正文与 Consequences 的版本字面量为 `^0.1.5-rc.1`，决策与备选未改
- [ ] `node .agents/skills/nano-flow/scripts/check-domain.mjs .` 不因本轮改动新增违例
- [ ] `vp check` 与两个包 `vp test src` 全绿
- [ ] `pnpm install --lockfile-only` 或等价 dry-run 解析出 `@deepseek-ai/dsh-tool-session-query@0.1.5-rc.1`（不改工作区安装状态）

## 上下文

- [spec](spec.md)（问题 3、4 与「实施决策」的版本范围条目）
- [ADR-0003 内容搜索经上游 opt-in 工具与索引启用交付](../../../adr/0003-内容搜索经上游opt-in工具与索引启用交付.md)
- [取证：0.1.2-rc.1 → 0.1.5-rc.1 兼容性](../../../../../.flow/dsh-plugin-0.1.5-audit/report.md)（第 5 节）
- 上游工具包 `0.1.5-rc.1` 的 peer 要求：`.temp/dsh-0.1.5-new/package/package.json`
- [领域语言](../../../CONTEXT.md)（`发布通道`、`DSH 插件`）

## 下一步

/code-delivery

## 交付记录

- **交付物**：`packages/dsh-nested-skill/package.json`（peer `dsh-fs`/`dsh-skill` → `^0.1.5-rc.1`）、`packages/dsh-session-manager/package.json`（peer `dsh-llm`/`dsh-tools` → `^0.1.5-rc.1`，`dependencies` 的上游工具包 → **精确** `0.1.5-rc.1`）、两个 README 的兼容段落与安装形态说明、`docs/dsh/adr/0003` 正文与 Consequences 的版本字面量。提交见本分支 HEAD（提交号随后续 docs 提交变化，权威登记在 Flow receipt）。
- **验证证据**：
  - 版本解析：`npm view "<pkg>@<declared range>" version` 实测 `@deepseek-ai/dsh-fs|dsh-skill|dsh-tools|dsh-llm` 的 `^0.1.5-rc.1` 均解析到 `0.1.5-rc.2`（npm 已发布 rc.2），而宿主是 rc.1，故上游 opt-in 工具包改用精确 `0.1.5-rc.1`；`npm view @deepseek-ai/dsh-tool-session-query@0.1.5-rc.1 version` = `0.1.5-rc.1`。
  - 通道事实：`@deepseek-ai/dsh` dist-tags `latest=0.1.5-rc.1`、`next=0.1.5-rc.2`（rc.2 于 2026-09-10T14:57Z 发布，晚于宿主启动）；rc.2 的 peer 要求 `^0.1.5-rc.2`，与本机宿主 rc.1 不兼容——精确钉版避免混装。
  - 门禁：`vp check` 与 `vp test src` 在两个包全绿（nested-skill 37 项、session-manager 74 项）；`node .agents/skills/nano-flow/scripts/check-domain.mjs .` 通过。
  - 残留范围检查：`packages/**` 下仅 `dsh-opencode-session`（非本轮范围）仍声明 `0.1.2-rc.1`，已登记到 spec「待定」。
- **未覆盖**：profile 是否真的解析出新的工具包版本由 Issue 04 的重装证明（本 Issue 只对齐声明）。
