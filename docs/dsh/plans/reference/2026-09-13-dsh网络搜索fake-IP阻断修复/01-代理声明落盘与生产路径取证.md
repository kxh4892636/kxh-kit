---
status: completed
blocked_by: []
---

# 代理声明落盘与生产路径取证

## 交付

1. harness home `C:\Users\kxh\.dsh\.env` 含两行代理声明，DSH 启动环境层把 `HTTPS_PROXY` 解析为来自该文件的 `user-env` 层。
2. 生产路径对照证据：无该文件时 `proxyRouteFor` 为直连并复现 `WEB_BLOCKED_URL`；有该文件时路由为 `proxied: true` 且 `HttpFetchProvider.fetch` 对 `https://www.weather.com.cn/` 返回 200 与页面正文。
3. 3080 实例重启使配置生效，端口自检可达（401 未认证首页属正常）。
4. ADR-0005 落盘。

## 范围

- 写入并复核 `~/.dsh/.env`；用 `.cache/env-layer-probe.mjs` 走 DSH 自身的 `loadLayeredEnv` / `installProxyFromEnvironment` / `HttpFetchProvider` 取证。
- 用脱离父进程的辅助脚本（`.cache/restart-dsh.ps1`）重启实例并自检端口；新增 ADR-0005 与领域文档校验。
- 不做：改上游包或 SSRF 校验、改 mihomo 配置、把 `.env` 纳入仓库、更换 DSH 版本。

## 直接依赖

- 无（根节点）。权威输入为 [spec.md](spec.md) 的「问题」「方案」「实施决策」与 [.flow 取证记录](../../../../../.flow/quest/2026-09-13-dsh网络搜索fake-IP阻断修复.md)。

## 验收

- [x] `C:\Users\kxh\.dsh\.env` 内容为两行代理声明，地址 `http://127.0.0.1:7890`（`.cache/exec-evidence.txt` 第 1 节）。
- [x] 删除 `.env` 的对照运行输出 `HTTPS_PROXY entry: undefined`、`route: {"proxied":false}` 与 `WEB_BLOCKED_URL` 原文；恢复后输出 `"source":"user-env"`、`"path":"C:\\Users\\kxh\\.dsh\\.env"`、`proxied":true`、`fetch ok: {"status":200,"chars":100000}` 与标题 `天气网`（同文件第 2、3 节）。
- [x] `docs/dsh/adr/0005-fake-ip解析经代理声明修正.md` 存在，共 15 行（< 144 线上限）。
- [x] 领域文档校验：本 Plan 无违例（校验器在本工作区仅报出与本工作无关的既有违例 `docs/common/plans/active/2026-09-13-鹈鹕骑自行车svg动画: Plan 至少需要 story.md 或 spec.md`）。
- [x] 3080 实例重启后端口自检可达，且重启后的端到端抓取复核已闭合：见「交付记录」的重启证据与重启后复核。

## 上下文

- [spec.md](spec.md)（问题、方案、实施决策、工作环境）
- [ADR-0005](../../../adr/0005-fake-ip解析经代理声明修正.md)
- 取证脚本：`.cache/proxy-probe.mjs`、`.cache/env-layer-probe.mjs`（工作区根下，非交付物）
- 报错来源与代理策略代码位置见 spec「上下文」段

## 下一步

/code-delivery

## 交付记录

- **配置**：`C:\Users\kxh\.dsh\.env`（harness home 层，DSH 启动器唯一受理代理名的落盘位置）写入 `HTTP_PROXY=http://127.0.0.1:7890` 与 `HTTPS_PROXY=http://127.0.0.1:7890` 两行；未纳入仓库（用户级运行时配置）。
- **证据（对照实验，生产代码路径）**：`.cache/exec-evidence.txt` 四节——① `.env` 内容；② 删除 `.env` 时 `loadLayeredEnv('dsh')` 返回 `HTTPS_PROXY entry: undefined`、`proxyRouteFor` 为 `{"proxied":false}`、`HttpFetchProvider.fetch` 抛 `WebError WEB_BLOCKED_URL URL hostname "www.weather.com.cn" resolves to a non-public IP address`（原报错逐字复现）；③ 恢复 `.env` 后同一调用返回 `{"value":"http://127.0.0.1:7890","source":"user-env","path":"C:\\Users\\kxh\\.dsh\\.env"}`、`{"proxied":true,"proxy":"http://127.0.0.1:7890"}`、`fetch ok: {"status":200,"chars":100000}`、标题 `天气网`；④ 首轮独立复现脚本显示本机 DNS 答案 `198.18.1.197`。
- **取证脚本**：`.cache/env-layer-probe.mjs`（走 DSH 自身 `dsh-app-boot` + `dsh-http-proxy` + `dsh-web-fetch-http`，可用 `node .cache/env-layer-probe.mjs` 复跑）、`.cache/proxy-probe.mjs`（首轮定位）。两者位于 gitignore 的 `.cache/` 下，不随仓库分发。
- **领域文档**：[ADR-0005](../../../adr/0005-fake-ip解析经代理声明修正.md)（新增，15 行）。
- **生效动作**：`.cache/restart-dsh.ps1` 由脱离父进程的辅助进程在本次会话结束后重启 3080 实例（清掉从 DSH 会话继承的代理/`DSH_*` 标记，再 `dsh.cmd web --host 127.0.0.1 --port 3080 --no-open`），自检 `http://127.0.0.1:3080/` 可达后写入 `.cache/restart-dsh.log`。
- **重启证据**：辅助重启日志 `.cache/restart-dsh.log`——`2026-09-13T04:36:38 helper start`、`04:36:43 env scrubbed; HTTP_PROXY=`、`04:36:43 spawned dsh pid=25152`、`04:36:46 port reachable status=401`、`04:36:46 RESTART OK pid=25152 url=http://127.0.0.1:3080/`；第 2 行证明重启进程的代理不来自继承环境。
- **重启后复核（2026-09-13 补记，闭合 spec「待定」原第 1 项）**：① 清空 shell 内代理变量后复跑 `.cache/env-layer-probe.mjs`，得 `HTTPS_PROXY entry: {"value":"http://127.0.0.1:7890","source":"user-env","path":"C:\\Users\\kxh\\.dsh\\.env"}`、`route: {"proxied":true,...}`、`fetch ok: {"status":200,"chars":100000}`、标题 `天气网`——`.env` 仍是该代理名的落盘来源；② 当前 3080 实例（`node` pid 25208，2026-09-13T04:57:12 由 VS Code 集成终端拉起）上直接抓取 `https://www.weather.com.cn/` 返回 HTTP 200 与首页正文，用户可见的抓取能力已恢复。辅助重启的 pid 25152 其后已被该实例取代，两者都在进程启动时经 home 层读取 `.env`。
