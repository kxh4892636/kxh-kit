---
id: 60a23aa8-ba05-4158-9ae6-7621de6e82ed
---

# 复合类型

## array

### 基本概念

#### 概念

- array: 固定长度、元素类型相同的值序列;
- 长度: array 类型的一部分, `[3]int` 和 `[4]int` 是不同类型;
- 值语义: 赋值和传参会复制整个数组;

#### 语法格式

```go
func main() {
	var nums [3]int       // 固定长度数组; 元素零值为 0
	nums[0] = 10          // 数组索引从 0 开始
	println(nums[0], len(nums))
}
```

### 初始化

#### 语法格式

```go
func main() {
	a := [3]int{1, 2, 3}      // 显式长度数组字面量
	b := [...]int{1, 2, 3}    // 由元素数量推导数组长度
	c := [5]int{1: 10, 3: 30} // 指定索引初始化; 未指定元素为零值
	println(a[0], b[1], c[3])
}
```

### 数组等价

#### 概念

- array 可比较条件: 元素类型可比较;
- 数组相等: 长度相同、元素类型相同、每个元素相等;
- 类型差异: `[2]int` 和 `[3]int` 不是同一类型;

#### 语法格式

```go
func main() {
	a := [2]int{1, 2}
	b := [2]int{1, 2}
	println(a == b) // true
}
```

### 多维数组

#### 语法格式

```go
func main() {
	matrix := [2][3]int{{1, 2, 3}, {4, 5, 6}} // 2 行 3 列数组
	println(matrix[1][2])                     // 访问第 2 行第 3 列
}
```

## slice

### 基本概念

#### 概念

- slice: 指向底层数组一段连续元素的描述符;
- 长度: 当前可访问元素数量;
- 容量: 从起始位置到底层数组末尾的可扩展空间;

#### 语法格式

```go
func main() {
	var zero []int       // nil slice; len 和 cap 均为 0
	s := []int{1, 2, 3} // slice 指向底层数组的一段连续元素
	println(len(zero), cap(zero), len(s), cap(s))
}
```

### 创建方式

#### 语法格式

```go
func main() {
	a := []int{1, 2, 3}     // slice literal
	b := make([]int, 3)     // 长度为 3, 容量为 3
	c := make([]int, 0, 8)  // 长度为 0, 容量为 8
	println(len(a), len(b), cap(c))
}
```

### nil 和 empty slice

#### 概念

- nil slice: 值为 nil, 长度和容量为 0;
- empty slice: 值非 nil, 长度为 0;
- append: nil slice 和 empty slice 均可直接 append;

#### 语法格式

```go
func main() {
	var nilSlice []int
	emptySlice := []int{}
	println(nilSlice == nil)   // true
	println(emptySlice == nil) // false
}
```

### 切片表达式

#### 语法格式

```go
func main() {
	a := []int{0, 1, 2, 3, 4}
	b := a[1:4] // 取下标 1 到 3; 新旧 slice 共享底层数组
	b[0] = 10   // 修改共享元素; a[1] 同步变化
	println(a[1], len(b), cap(b))
}
```

### append

#### 语法格式

```go
func main() {
	s := []int{1, 2}
	s = append(s, 3)    // append 返回新 slice; 必须接收返回值
	s = append(s, 4, 5) // 容量不足时会分配新底层数组
	println(len(s), cap(s))
}
```

### copy

#### 语法格式

```go
func main() {
	src := []int{1, 2, 3}
	dst := make([]int, len(src))
	n := copy(dst, src) // 返回实际复制的元素数量
	println(n, dst[0])
}
```

## map

### 基本概念

#### 概念

- map: key-value 映射表;
- key 类型: 必须支持相等比较;
- value 类型: 任意 Go 类型;
- 零值: `nil`, 可读不可写;

#### 语法格式

```go
func main() {
	ages := map[string]int{"tom": 18} // map key 必须支持 == 和 !=
	println(ages["tom"])
}
```

### 创建和读写

#### 语法格式

```go
func main() {
	m := make(map[string]int) // 创建空 map
	m["a"] = 1                // 写入 key-value
	v, ok := m["b"]           // comma ok 区分 key 不存在和 value 零值
	delete(m, "a")            // 删除 key; key 不存在也安全
	println(v, ok, len(m))
}
```

### 遍历

#### 语法格式

```go
package main

import "fmt"

func main() {
	m := map[string]int{"a": 1, "b": 2}
	for k, v := range m {
		fmt.Println(k, v) // map 遍历顺序不保证稳定
	}
}
```

### 并发安全

#### 概念

- 内置 map: 多 goroutine 并发读写不安全;
- 保护方式: 使用 `sync.Mutex`、`sync.RWMutex` 或 `sync.Map`;
- 设计方式: 尽量让 map 归属单个 goroutine;

## struct

### 基本概念

#### 概念

- struct: 将多个字段组合成一个自定义类型;
- 字段名: 同一个 struct 内必须唯一;
- 零值: 每个字段为对应类型零值;

#### 语法格式

```go
type User struct {
	Name string // 导出字段; 包外可见
	Age  int
}
```

### 初始化和访问

#### 语法格式

```go
func main() {
	a := User{}                    // 零值初始化
	b := User{Name: "Tom", Age: 18} // 命名字段初始化; 推荐使用
	c := User{"Jerry", 20}          // 顺序初始化; 依赖字段顺序
	b.Age = 19                      // selector 访问和修改字段
	println(a.Name, b.Age, c.Name)
}
```

### 构造函数

#### 概念

- 构造函数: Go 没有内置 constructor 语法;
- 约定命名: 使用 `NewType` 普通函数创建并初始化值;
- 返回类型: 可按需求返回值或指针;

#### 语法格式

```go
func NewUser(name string, age int) *User {
	return &User{Name: name, Age: age} // 返回初始化后的 User 指针
}
```

### 嵌入字段

#### 概念

- embedded field: 只写类型名的匿名字段;
- 字段提升: 可通过外层 struct 直接访问嵌入字段成员;
- 组合复用: Go 使用嵌入表达组合关系;

#### 语法格式

```go
type Address struct {
	City string
}

type User struct {
	Name string
	Address // embedded field; 可通过 User 直接访问 Address 字段
}
```

### 空结构体

#### 概念

- `struct{}`: 不包含字段的结构体;
- 使用场景: set、只传递信号的 channel;

#### 语法格式

```go
func main() {
	seen := map[string]struct{}{} // struct{} 通常不占用额外存储
	seen["go"] = struct{}{}
	_, ok := seen["go"]
	println(ok)
}
```

## pointer

### 基本概念

#### 概念

- pointer: 保存变量内存地址的值;
- 零值: `nil`;
- nil 解引用: 运行时 panic;

#### 语法格式

```go
func main() {
	x := 10
	p := &x // & 获取变量地址
	*p = 20 // * 解引用指针并修改指向的值
	println(x)
}
```

### 指针参数

#### 语法格式

```go
func addOne(p *int) {
	*p = *p + 1 // 通过指针修改调用方变量
}

func main() {
	n := 1
	addOne(&n)
	println(n)
}
```

### new

#### 概念

- `new(T)`: 分配 T 的零值并返回 `*T`;
- `make`: 只用于 slice、map、channel;

#### 语法格式

```go
func main() {
	p := new(int) // 分配 int 零值并返回 *int
	*p = 10
	println(*p)
}
```

### uintptr

#### 概念

- uintptr: 可保存指针位模式的整数类型;
- GC 语义: uintptr 不被垃圾回收器当作指针追踪;
- 使用限制: 普通业务代码不应把 uintptr 当安全指针使用;

### 多级指针

#### 语法格式

```go
func main() {
	x := 1
	p := &x  // *int
	pp := &p // **int
	**pp = 2
	println(x)
}
```

### 指针限制

#### 概念

- 指针算术: Go 不支持普通指针加减运算;
- 临时值取址: 不可对不可寻址的临时结果直接取地址;
- 安全边界: 需要底层地址运算时通常涉及 `unsafe`;

## 自定义类型

### 类型定义

#### 概念

- type definition: 基于已有类型定义新类型;
- 新类型: 与底层类型不是同一类型;
- 底层类型: 决定新类型的存储和基础操作;

#### 语法格式

```go
type UserID int64 // 基于 int64 定义新类型

func main() {
	var id UserID = 100
	println(id)
}
```

### 类型别名

#### 概念

- type alias: 为已有类型创建别名;
- 使用场景: 迁移 API、提供语义化名字;
#### 语法格式

```go
type Byte = uint8 // Byte 与 uint8 完全相同
```
