# Nano Mem

Nano Mem is a local agent-memory CLI. The package exposes one binary, `nm`, and emits stable JSON for agent callers.

```powershell
nm --help
nm --version
```

Memory, search, lifecycle, skill-management, and self-update commands are delivered incrementally by the active Nano Mem Plan.
