{
  "name": "@kxh4892636/loop-kit",
  "version": "0.2.0",
  "description": "Install the Kxh Kit agent rules, domain guide, and Loop Kit skills.",
  "license": "MIT",
  "bin": {
    "loop-kit": "dist/cli.mjs"
  },
  "files": [
    "dist",
    "LICENSE",
    "README.md"
  ],
  "type": "module",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "vp pack && node dist/create-snapshot.mjs",
    "check": "vp check",
    "coverage": "vp run build && c8 --include=dist/*.mjs --exclude=dist/create-snapshot.mjs --all --reporter=text --reports-dir=node_modules/.cache/c8-report --temp-directory=node_modules/.cache/c8-temp --check-coverage --lines=80 --functions=80 --statements=80 --branches=80 vp test tests/cli.test.ts",
    "prepack": "vp pack && node dist/create-snapshot.mjs",
    "test": "vp run build && vp test"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@typescript/native": "catalog:",
    "c8": "catalog:",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
