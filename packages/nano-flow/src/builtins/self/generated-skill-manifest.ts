import type { ManagedSkill } from "./skill-catalog";

export const generatedSkills = JSON.parse(
  '[{"name":"nano-flow","version":"0.0.2","contentHash":"69a660f976a5d144328cce383e274e0b46ba3a93c07322345d6862fd55410be8","files":[{"path":"extensions/hooks.json","content":"{\\n  \\"schema_version\\": 1,\\n  \\"hooks\\": [\\n    {\\n      \\"match\\": [\\"dev-gate\\"],\\n      \\"message\\": \\"任何准入判断前先完整读取 `<nano-flow-skill-root-dir>/extensions/QUESTIONS.md` 和 `<nano-flow-skill-root-dir>/extensions/workflows/README.md`，以用户输入和当前上下文作为已有答案，选择并询问全部相关问题。问题集未清空时结论为 `not ready`。\\"\\n    },\\n    {\\n      \\"match\\": [\\"code-delivery\\"],\\n      \\"message\\": \\"交付过程中遇到 block 卡点， 请优先在 `<nano-flow-skill-root-dir>/extensions/workflows/README.md` 和对应业务域 workflow 中寻找可能的解决方法.\\"\\n    }\\n  ]\\n}\\n"},{"path":"extensions/QUESTIONS.md","content":""},{"path":"extensions/skills/code-spec/SKILL.md","content":"---\\nname: code-spec\\ndescription: 应用本仓库的代码规范；当编写、修改或审查代码时使用。\\n---\\n\\n逐个核对本次触及的目录、文件、函数和外部边界；每条适用规则均已满足或命中「全局例外」时，规范检查完成。\\n\\n## 目录层级\\n\\n- 将 `/src` 视为`level-1`, 若 `/src` 不存在, 则视为项目根目录(`/`).\\n- 目录层级禁止超过 4 层目录, `/level-4` 下只能包含文件, 即 `/level-1/level-2/level-3/level-4/index.ts` .\\n- 一个目录下的文件数量不超过 13 个, 超过时根据领域合并为文件夹.\\n\\n## 命名规范\\n\\n- 文件夹和文件的名称使用 `kebab-case`;\\n- 文件夹和文件应按实际内容使用领域名称(如 `tab-group-state.ts`, `terminal-cleanup.ts`), 禁用 `helpers`, `utils`, `common`, `components`, `hooks` 等不传递信息, 易沦为杂物堆的泛化名称(如 `tabs-helpers.ts`, `terminal-utils.ts`). 若只能想到此类名称, 通常说明文件职责过多需拆分, 或应按函数的操作对象命名.\\n- `level-2` 允许使用 `libs`, `utils`, `config`, `components`, `hooks` 等泛化名称.\\n\\n## 代码文件\\n\\n- 单个文件不超过 610 行, 单个前端组件不超过 389 行, 单个函数不超过 89 行, 超过时重构, 优先按职责和复用边界进行重构;\\n- 默认使用命名导出/导入, 除非需要默认导出/导入;\\n- 注释只写说明性注释, 解释代码背后的原因, 约束, 取舍和风险; 禁止用注释复述代码正在做什么或如何执行; 注释使用中文; 增量代码, 存量改动代码按需补充注释(非测试代码).\\n\\n## 风险边界\\n\\n- 网络请求, SDK 调用, 文件系统等外部边界必须有错误处理.\\n- 外部边界数据默认不可信; 内部流转数据默认可信, 无须 schema/validator.\\n  - 客户端: 默认 HTTP 响应数据不可信, 递归使用 `?` 可选链(TypeScript), 避免空指针异常; 默认用户输入数据不可信, 使用项目 schema/validator.\\n  - 服务端: 默认 RPC/HTTP 请求数据不可信, 使用项目 schema/validator.\\n\\n## 测试\\n\\n- 测试文件与实现文件放置于同级目录, E2E 测试可以使用单独的 tests 目录;\\n\\n## TS\\n\\n- 函数使用箭头函数, 不使用普通函数, 函数参数和返回值必须有类型注解.\\n- 禁止使用 `enum`, 使用 `const = {} as const` 替代, 需要值联合类型时, 从 const 对象派生.\\n- 禁止使用 `any`, 确有无法收窄的数据类型, 使用 `type ISafeAny = any`.\\n\\n## CSS\\n\\n- 优先使用 Tailwind CSS, 不存在则使用 SCSS/LESS/CSS Modules;\\n\\n## React\\n\\n### 组件模板\\n\\n```typescript\\n// 组件属性使用接口声明, 组件属性数量 <= 8 个, 超过重构组件\\ninterface MyComponentProps {prop1: string; prop2: number;}\\n// 使用箭头函数组件和命名导出;\\nexport const MyComponent: React.FC<MyComponentProps> = (props: MyComponentProps) => {\\n  // 组件属性使用 `props` 定义,组件内部使用解构赋值获取属性\\n  const { prop1, prop2 } = props;\\n  // 组件内部按功能逻辑块组织,最后返回 UI,通用功能逻辑放置于顶部\\n  return (<Component />);\\n};\\n```\\n\\n## 全局例外\\n\\n- 工具链约定的文件夹和文件;\\n- 自动生成的文件夹和文件;\\n- 领域 skill 的优先级高于该 skill;\\n"},{"path":"extensions/skills/code-test/mocking.md","content":"# 何时 Mock\\n\\n只在 **system boundaries** 进行 Mock:\\n\\n- External APIs(payment, email 等).\\n- Databases(有时需要, 优先使用 test DB).\\n- 时间/随机性.\\n- File system(有时需要).\\n\\n不要 mock:\\n\\n- 你自己的 classes/modules.\\n- 内部协作者.\\n- 任何你控制的事物.\\n\\n## 为 Mockability 进行设计\\n\\n在 system boundaries 上, 设计易于 mock 的 interfaces:\\n\\n**1. 使用 dependency injection**\\n\\n传入 external dependencies, 而不是在内部创建它们:\\n\\n```typescript\\n// 易于 mock\\nfunction processPayment(order, paymentClient) {\\n  return paymentClient.charge(order.total);\\n}\\n\\n// 难以 mock\\nfunction processPayment(order) {\\n  const client = new StripeClient(process.env.STRIPE_KEY);\\n  return client.charge(order.total);\\n}\\n```\\n\\n**2. 优先使用 SDK-style interfaces, 而不是 generic fetchers**\\n\\n为每项外部操作创建具体 function, 而不是创建一个包含条件逻辑的 generic function:\\n\\n```typescript\\n// GOOD: 每个 function 都可以独立 mock\\nconst api = {\\n  getUser: (id) => fetch(`/users/${id}`),\\n  getOrders: (userId) => fetch(`/users/${userId}/orders`),\\n  createOrder: (data) => fetch(\\"/orders\\", { method: \\"POST\\", body: data }),\\n};\\n\\n// BAD: Mocking 要求 mock 内部存在 conditional logic\\nconst api = {\\n  fetch: (endpoint, options) => fetch(endpoint, options),\\n};\\n```\\n\\nSDK approach 意味着:\\n\\n- 每个 mock 返回一种特定 shape.\\n- 测试准备中没有条件逻辑.\\n- 更容易看出测试使用了哪些 endpoints.\\n- 每个 endpoint 都有 type safety.\\n"},{"path":"extensions/skills/code-test/SKILL.md","content":"---\\nname: code-test\\ndescription: 在实现代码完成后编写和评估测试；当 feature、bug fix 或既有代码需要 unit/integration tests 或覆盖率时使用。\\n---\\n\\n# Code Test\\n\\n本 skill 承担 `implementation → tests` 中的 tests 阶段：先以已完成的生产代码和 spec 为对象，再编写能证明行为正确、能识别错误实现的测试。\\n\\n探索 codebase 时，从 `CONTEXT-MAP.md` 定位所触及业务域并读取它们的 `CONTEXT.md`，使测试名称和 interface vocabulary 与项目的 domain language 一致，并遵守相关业务域的 ADRs。\\n\\n## 流程\\n\\n1. 使用 `/dev-gate` 已确认的质量门禁、public interfaces、critical paths 和 seams，负责其中的测试类门禁。\\n   - 直接调用本 skill 时，在写测试前写下受测 seams 并与用户确认。\\n   - interface 形态仍未确定时，使用 `/code-design` 的 module、interface、depth 和 seam vocabulary 先确定边界。\\n2. 在生产代码完成后编写测试。测试通过 public interfaces 验证 observable behavior，覆盖 spec 中的成功路径、失败路径和关键边界；expected values 来自 spec、worked example 或 known-good literal 等独立事实源。\\n3. 先运行受影响的单个测试文件。失败时根据 public contract 判断是测试还是实现错误；任何生产代码修正都会使本阶段重新从受影响测试开始。\\n4. 按仓库已有配置运行 coverage。\\n5. 记录测试命令、通过结果和 coverage report。只有下方门禁全部成立时，本 skill 才完成并进入 `/verifying`。\\n\\n## 硬门禁\\n\\n- 受影响测试和完整 test suite 均通过。\\n- **测试覆盖率 ≥ 80%。** 统计范围以仓库配置为事实源；未配置时至少包含本次修改的全部生产代码。报告同时给出 statements、branches、functions、lines 时，每一项都必须达到 80%；只给出单一总指标时，该指标必须达到 80%。\\n- 缺少 coverage tooling、报告或可复现命令时，结论是 `blocked`。不得通过缩小统计范围或编写无行为断言的测试来抬高指标；仓库定义的更严格门禁优先。\\n\\n## 测试原则\\n\\n良好的测试读起来像 specification。代码内部结构可以完全改变，只要 observable behavior 不变，测试就应继续通过。需要判断示例质量时读取 [tests.md](tests.md)；外部 seam 需要 mock 时读取 [mocking.md](mocking.md)。\\n\\n**seam** 是执行测试的 public boundary：你可以在该 interface 上观察行为，而无需深入内部。测试只位于预先商定的 seams 上，使投入集中在 critical paths 和复杂逻辑，而不是内部结构或任意 edge cases。\\n\\n## Anti-patterns\\n\\n- **Implementation-coupled** - mock internal collaborators、测试 private methods，或通过 side channel 验证。识别信号：重构时行为没有变化，测试却失败。\\n- **Tautological** - assertion 使用与代码相同的方式重新计算 expected value，因此它由构造保证通过，永远无法与代码产生分歧。\\n- **Metric gaming** - 只执行代码却不验证行为、缩小统计范围或堆砌无行为断言的测试。识别信号：指标提高，但测试仍无法区分一个相关的错误实现。\\n"},{"path":"extensions/skills/code-test/tests.md","content":"# 良好与糟糕的 Tests\\n\\n## 良好的 Tests\\n\\n**Integration-style**: 通过真实 interfaces 进行测试, 而不是 mock 内部部分.\\n\\n```typescript\\n// GOOD: 测试 observable behavior\\ntest(\\"用户可以使用有效 cart 结账\\", async () => {\\n  const cart = createCart();\\n  cart.add(product);\\n  const result = await checkout(cart, paymentMethod);\\n  expect(result.status).toBe(\\"confirmed\\");\\n});\\n```\\n\\n特征:\\n\\n- 测试用户/调用方关心的行为.\\n- 只使用 public API.\\n- 能经受内部重构.\\n- 描述 WHAT, 而不是 HOW.\\n- 每个 test 只有一个 logical assertion.\\n\\n## 糟糕的 Tests\\n\\n**Implementation-detail tests**: 与内部结构耦合.\\n\\n```typescript\\n// BAD: 测试 implementation details\\ntest(\\"checkout 调用 paymentService.process\\", async () => {\\n  const mockPayment = jest.mock(paymentService);\\n  await checkout(cart, payment);\\n  expect(mockPayment.process).toHaveBeenCalledWith(cart.total);\\n});\\n```\\n\\n危险信号:\\n\\n- Mock internal collaborators.\\n- 测试 private methods.\\n- 对 call counts/order 作出断言.\\n- 行为没有变化, 测试却在重构时失败.\\n- 测试名称描述 HOW, 而不是 WHAT.\\n- 通过 external means 验证, 而不是通过 interface 验证.\\n\\n```typescript\\n// BAD: 绕过 interface 进行验证\\ntest(\\"createUser 保存到 database\\", async () => {\\n  await createUser({ name: \\"Alice\\" });\\n  const row = await db.query(\\"SELECT * FROM users WHERE name = ?\\", [\\"Alice\\"]);\\n  expect(row).toBeDefined();\\n});\\n\\n// GOOD: 通过 interface 验证\\ntest(\\"createUser 使 user 可被检索\\", async () => {\\n  const user = await createUser({ name: \\"Alice\\" });\\n  const retrieved = await getUser(user.id);\\n  expect(retrieved.name).toBe(\\"Alice\\");\\n});\\n```\\n\\n**Tautological tests**: Expected value 重述 implementation, 因此测试由构造保证通过.\\n\\n```typescript\\n// BAD: 使用与代码相同的方式重新计算 expected value\\ntest(\\"calculateTotal 对 line items 求和\\", () => {\\n  const items = [{ price: 10 }, { price: 5 }];\\n  const expected = items.reduce((sum, i) => sum + i.price, 0);\\n  expect(calculateTotal(items)).toBe(expected);\\n});\\n\\n// GOOD: Expected value 是独立的 known literal\\ntest(\\"calculateTotal 对 line items 求和\\", () => {\\n  expect(calculateTotal([{ price: 10 }, { price: 5 }])).toBe(15);\\n});\\n```\\n"},{"path":"extensions/workflows/README.md","content":""},{"path":"FLOW.md","content":"# Flow 运行协议\\n\\n本文件定义 `/nano-flow` 如何驱动 `flow.mjs`；主流程中的其他 skill 不触发该脚本。脚本返回的 JSON 是当前步骤、租约与状态的运行态事实源。脚本命令及 options 以现场帮助为准：\\n\\n```powershell\\nnode <nano-flow-skill-root-dir>/scripts/flow.mjs --help\\n```\\n\\n所有命令从工作区根目录执行。首次使用一个命令前查看其帮助；保留成功结果中的 `plan`、`session`、`issue`、`next_skill`、`next_action` 与 `message`，后续调用原样复用对应标识和消息。\\n\\n## 进入与恢复\\n\\n- 主流程：`/to-story -> /quest-with-domain -> /to-issues -> /dev-gate -> /code-delivery`。\\n- Flow 只执行一次 `enter-plan`，由 `/nano-flow` 以 `--entry` 传入用户确认的 `/to-story` 或 `/quest-with-domain`。\\n- `--mode` 可选 `manual`（默认）或 `auto`：`/dev-gate` 之前的步骤完成后，manual 询问用户再推进，auto 直接自动推进。模式随 Plan 持久化，重新进入时传入即切换。\\n- `--plan` 是本轮稳定标识。需要进入 `/to-issues` 时，它必须是工作区内的实际 Plan 路径；跳过 `/to-issues` 时只要求它在工作区内唯一且稳定。\\n- 已有运行态使用 `status` 查明当前位置，再按返回值恢复。\\n- 命令成功且当前调用者持有唯一有效租约、返回的 `next_skill` 与预期入口一致时，进入完成。\\n\\n## Receipt chain\\n\\n`next_skill` 或 `next_action` 是唯一获准的下一步：\\n\\n1. 完整读取并执行返回的 skill；action 则等待其前置 skill 交付控制权。\\n2. 只在该 skill 的完成标准真实成立后，使用 `record-plan` 或 `record-issue` 登记结果和至少一项可核查证据。\\n3. 登记成功后丢弃旧的 next 值，只执行新返回值。\\n\\n`/to-story` 完成后直接进入 `/quest-with-domain`。`/quest-with-domain` 完成后必须进入 `/to-issues`，由它根据任务是否需要可恢复的 issue graph 登记 `completed` 或 `skipped`。\\n\\n`delivering_direct` 返回 Plan 级 `/code-delivery`；`delivering_issues` 由 `claim-issue` 返回 Issue 级 `/code-delivery`。\\n\\n`/code-delivery` 进入后先登记 `started` 并保留返回的 `commit` action；代码、`/code-test`、`/verifying` 与 `/code-review` 是它返回前的内部门禁，不另记 Flow receipt。全部门禁仍适用于当前 diff 后，才执行并登记 `commit=committed`。\\n\\n每条 receipt 都引用本轮真实产物或结果。证据存在、与当前目标和 diff 对应，且足以复核声明时，本步骤完成。\\n\\n## Issue 推进\\n\\n- 仅当 `/to-issues=completed` 且 Plan 到达 `delivering_issues` 后，以 `claim-issue` 领取直接依赖均已完成的 issue；首次领取保留脚本生成的 issue session。\\n- 一个 session 串行推进自己领取的 issue。不同 session 可以并行领取互不阻塞的 issue；同一 issue 只有一个有效租约。\\n- 交付前，issue 的「交付记录」包含交付物与验证证据；脚本据此允许完成状态与 commit receipt。\\n- 所有 issue 完成后，Plan 自动进入 `completed`；再以 `sync-plan` 刷新派生视图，并按 [`DOMAIN.md`](references/DOMAIN.md) 处理 Plan 生命周期。\\n\\n## 租约、阻塞与恢复\\n\\n- 较长操作用 `heartbeat-plan` 或 `heartbeat-issue` 续租；主动暂停用 `release-plan` 或 `release-issue`。\\n- 租约过期后用 `claim-plan` 或 `resume-issue` 接管。\\n- 真实障碍用 `block-issue` 记录原因和可判定的解除条件，并释放租约。\\n- 异常退出或移动 Plan 后用 `sync-plan` 从 issue frontmatter 重建 spec 派生视图。\\n\\n写命令失败时保留现场，依据错误信息恢复前置条件。\\n\\n## 持久状态\\n\\n`.flow/state/YYYY-MM-DD-state.json` 按本地日期保存当日阶段、租约、游标与 receipt。不读取无日期前缀或其他日期的状态文件。\\n\\nPlan 的 `phase` 是显式执行阶段：\\n\\n```text\\nplanning\\n  ├─ /to-issues=skipped + /dev-gate=ready\\n  │      ↓\\n  │  delivering_direct ── commit=committed ─→ completed\\n  │\\n  └─ /to-issues=completed + /dev-gate=ready\\n         ↓\\n     delivering_issues ── all issues completed ─→ completed\\n```\\n\\n`cursor` 只表示当前阶段内的位置，切换阶段时归零；`lease` 与阶段正交。Plan 运行态直接保存为 `{ phase, cursor, lease, receipts, issues }`。\\n\\nissue frontmatter 的 `status` 是持久化 Issue 执行状态的事实源，spec 状态与 Issue 表是派生视图：\\n\\n```text\\npending -> in_progress -> completed\\n              |\\n              +-> blocked -> in_progress\\n```\\n\\n`blocked` 必须带障碍与解除条件；`completed` 必须带交付物与验证证据，后续修正以新 issue 表达。Plan 的 `active -> reference | archived` 生命周期与执行状态正交。\\n\\n`flow.mjs` 约束顺序与状态，不证明证据真实性。只有可复核证据与登记结果一致时，Flow 步骤才算完成。\\n"},{"path":"references/DOMAIN.md","content":"# 领域文档\\n\\n本文件是工作区领域文档布局、定位、命名和 Plan 生命周期的单一事实源。\\n\\n## 探索前先定位领域\\n\\n1. 阅读工作区根目录 `CONTEXT-MAP.md`，确定即将处理的业务域及其关系。\\n2. 阅读对应业务域的 `docs/{domain-name}/CONTEXT.md`。\\n3. 阅读该业务域中与当前工作相关的 `adr/`。\\n\\n跨业务域工作覆盖所有相关业务域，并读取 `CONTEXT-MAP.md` 中的关系。任一文件不存在时静默继续；术语或决策得到确认后，由 `/quest-with-domain` 按需创建或维护。\\n\\n## 布局\\n\\n领域内容只创建在工作区根目录的 multi-context 布局中；本约束文件随 `/nano-flow` 分发：\\n\\n```text\\n/\\n├── CONTEXT-MAP.md\\n└── docs/\\n    ├── common/\\n    │   └── CONTEXT.md\\n    └── {domain-name}/\\n        ├── CONTEXT.md\\n        ├── adr/\\n        │   └── 0001-中文决策名.md\\n        └── plans/\\n            ├── active/\\n            │   └── YYYY-MM-DD-中文工作名/\\n            │       ├── story.md\\n            │       ├── spec.md\\n            │       └── 01-中文标题.md\\n            ├── reference/\\n            └── archived/\\n```\\n\\n- `CONTEXT-MAP.md` 只索引业务域及其关系；跨域关系只定义一次。\\n- `docs/common/` 承载多个业务域共同贡献的领域术语和 ADR。\\n- `docs/{domain-name}/CONTEXT.md` 只承载该业务域的 glossary，不承载 spec 或实现细节。\\n- `docs/{domain-name}/adr/` 承载该业务域拥有的 ADR；跨域决策只选择一个 owner，其他域使用链接引用。\\n- `docs/{domain-name}/plans/` 承载 `/to-story` 和 `/to-issues` 维护的 Plan。\\n- `domain-name` 使用稳定、简短的 kebab-case；新增业务域时同步创建 `CONTEXT.md` 并加入 `CONTEXT-MAP.md`。\\n\\n## 文档约束\\n\\n- `docs/` 下的 ADR、Plan 文件名与正文使用中文；约定名 `CONTEXT.md`、`spec.md`、`story.md`、稳定 ID、代码标识符和 `domain-name` 除外。\\n- `CONTEXT.md` 行数、`adr/*.md` 数量与单个 ADR 行数的上限由 `check-domain` 脚本强制，以其输出为准。\\n- ADR 使用域内连续编号 `0001-中文决策名.md`、`0002-中文决策名.md`。\\n- 超过任一上限时，按领域边界合并、拆分或移除沉积内容，使文档重新满足上限。\\n\\n## Plan 生命周期\\n\\nPlan 目录位置表达活性和参考价值：\\n\\n```text\\n新建 ──> active ──完成且仍有参考价值──> reference ──失去参考价值──> archived\\n                  └─废弃、过时或不再有用──────────────────────> archived\\n```\\n\\n- `active/`：当前正在推进；新 Plan 一律创建于此。\\n- `reference/`：全部 issue 已完成，且内容仍有参考价值。\\n- `archived/`：已废弃、已过时或不再有用；内容冻结，不再更新，也不再作为权威来源。\\n\\nPlan 生命周期与 Plan 内的执行状态正交：目录位置表达活性与价值，`spec.md` 和 issue frontmatter 的 `status:` 表达执行进度。移动 Plan 目录不修改 `status:`；未完成但被废弃的 Plan 可以保留原状态进入 `archived/`。\\n\\n## 领域语言与决策\\n\\n- 命名 Issue、测试、假设和重构提案时，使用对应 `CONTEXT.md` 中的 canonical term，不使用 glossary 明确排除的同义词。\\n- 所需概念尚未收录时，先判断它是否只是项目未使用的语言；确认存在领域缺口后，由 `/quest-with-domain` 维护 glossary。\\n- 输出与现有 ADR 冲突时显式指出冲突及重新讨论的理由，不静默覆盖。\\n"},{"path":"references/skills/code-delivery/SKILL.md","content":"---\\nname: code-delivery\\ndescription: 按已确认的 spec 或 issue 交付代码；当 `/dev-gate` 已给出 `ready` 时使用。\\n---\\n\\n# Code Delivery\\n\\n以 `/dev-gate` 确认的执行契约为边界，以同一基线中的质量门禁为证据要求。未满足前置时先运行 `/dev-gate`。\\n\\n1. **实现**：读取 `/code-spec`，按照 spec/issues 完成工作；\\n2. **测试**：遵循 `/code-test`，根据质量门禁新增测试逻辑；\\n3. **验收**：执行 `/verifying`，根据质量门禁验证工作；\\n4. **审查**：调用 `/code-review` 完成双轴审查；\\n5. **提交**：获得用户授权后，为完成内容创建 commit。\\n\\n基线漂移则返回 `/dev-gate`，更新权威入口并重新确认。\\n"},{"path":"references/skills/code-design/DEEPENING.md","content":"# Deepening\\n\\n如何根据依赖安全地 deepen 一组 shallow modules. 本文假定使用 [SKILL.md](SKILL.md) 中的 vocabulary - **module**, **interface**, **seam**, **adapter**.\\n\\n## Dependency categories\\n\\n评估 deepening 候选项时, 对它的依赖进行分类. category 决定如何跨越 seam 测试 deepened module.\\n\\n### 1. In-process\\n\\n纯计算, in-memory state, 无 I/O. 始终可以 deepen. 合并 modules, 直接通过新 interface 进行测试. 不需要 adapter.\\n\\n### 2. Local-substitutable\\n\\n拥有本地 test stand-ins 的依赖(Postgres 使用 PGLite, 文件系统使用 in-memory filesystem). 如果存在 stand-in, 就可以 deepen. 运行 test suite 时, 使用 stand-in 测试 deepened module. seam 是 internal 的, module 的 external interface 上没有 port.\\n\\n### 3. Remote but owned (Ports & Adapters)\\n\\n跨越网络边界的自有服务(microservices, internal APIs). 在 seam 上定义 **port**(interface). deep module 拥有逻辑, transport 以 **adapter** 形式注入. 测试使用 in-memory adapter. 生产环境使用 HTTP/gRPC/queue adapter.\\n\\n### 4. True external (Mock)\\n\\n不受你控制的第三方服务(Stripe, Twilio 等). deepened module 将外部依赖作为注入的 port 接收. 测试提供 mock adapter.\\n\\n## Seam discipline\\n\\n- **One adapter means a hypothetical seam. Two adapters means a real one.** 至少两个合理 adapters(通常是生产环境 + 测试)使 port 成为真实 seam；single-adapter 形态保持 inline.\\n- **Internal seams vs external seams.** 测试专用 variation 保持为 implementation 私有的 internal seam；调用方需要替换的 variation 才进入 external interface.\\n\\n## Testing strategy: replace, don\'t layer\\n\\n- 一旦 deepened module 的 interface 上有了测试, shallow modules 上的旧 unit tests 就变成浪费, 删除它们.\\n- 在 deepened module 的 interface 上编写新测试. **interface is the test surface**.\\n- 测试通过 interface 对可观察结果作出断言, 而不是对内部状态作出断言.\\n- 测试应该能经受内部重构. 它们描述行为, 而不是 implementation. 如果 implementation 变化时测试必须变化, 它测试的范围就越过了 interface.\\n"},{"path":"references/skills/code-design/DESIGN-IT-TWICE.md","content":"# Design It Twice\\n\\n当用户想为选定的 deepening 候选项探索备选 interfaces 时, 使用此 parallel sub-agent pattern. 它基于 \\"Design It Twice\\" - 你的第一个想法不太可能是最好的.\\n\\n使用 [SKILL.md](SKILL.md) 中的 vocabulary - **module**, **interface**, **seam**, **adapter**, **leverage**.\\n\\n## 流程\\n\\n### 1. 界定 problem space\\n\\n派遣 sub-agents 前, 为选定的候选项编写一份面向用户的 problem space 说明:\\n\\n- 任何新 interface 都需要满足的 constraints.\\n- 它将依赖的内容, 以及它们所属的 category(参见 [DEEPENING.md](DEEPENING.md)).\\n- 用于落地 constraints 的粗略说明性代码草图. 它不是提案, 只是让 constraints 变得具体的方法.\\n\\n向用户展示这份说明, 然后立即进入第 2 步.\\n\\nconstraints、依赖类别、现有 coupling 与不可改变的 domain invariants 均已进入 brief，且说明性草图没有预设某个候选 interface 时，本步骤完成。\\n\\n### 2. 派遣 sub-agents\\n\\n并行派遣至少 3 个 sub-agents. 每个 sub-agent 都必须为 deepened module 产出一个**截然不同**的 interface.\\n\\n使用独立的 technical brief 提示每个 sub-agent(文件路径, coupling details, [DEEPENING.md](DEEPENING.md) 中的 dependency category, seam 后面的内容). 该 brief 独立于第 1 步面向用户的 problem-space 说明. 为每个 agent 指定不同的 design constraint:\\n\\n- Agent 1: \\"最小化 interface, 最多以 1-3 个入口为目标. 最大化每个入口的 leverage.\\"\\n- Agent 2: \\"最大化灵活性, 支持多种 use cases 和扩展.\\"\\n- Agent 3: \\"为最常见的调用方优化, 让默认情况变得简单.\\"\\n- Agent 4(如果适用): \\"围绕 ports & adapters 设计 cross-seam dependencies.\\"\\n\\n在 brief 中同时包含 [SKILL.md](SKILL.md) vocabulary 和相关业务域的 CONTEXT.md vocabulary, 让每个 sub-agent 都使用与架构语言和项目 domain language 一致的名称.\\n\\n每个 sub-agent 输出:\\n\\n1. Interface(types, methods, params, 以及 invariants, 顺序, 错误模式).\\n2. 展示调用方如何使用它的用例.\\n3. implementation 隐藏在 seam 后面的内容.\\n4. 依赖策略和 adapters(参见 [DEEPENING.md](DEEPENING.md)).\\n5. 权衡, leverage 在哪里高, 在哪里薄弱.\\n\\n至少三个方案在 interface shape 或 seam placement 上真正不同，每个方案都覆盖完整 brief 且能独立评估时，本步骤完成。\\n\\n### 3. 展示并比较\\n\\n依次展示 designs, 让用户逐个理解, 然后用文字比较它们. 按 **depth**(interface 上的 leverage), **locality**(变更集中的位置)和 **seam placement** 进行对比.\\n\\n比较后给出你自己的推荐: 你认为哪个 design 最强, 以及原因. 如果不同 designs 中的要素可以很好地组合, 提出 hybrid. 明确表达观点, 用户想要的是有力判断, 而不是一份菜单.\\n\\n用户能够沿同一组 constraints 比较所有方案，推荐方案的 depth、locality、依赖策略与代价均已明确，且最终选择或待定决策有清晰记录时，本流程完成。\\n"},{"path":"references/skills/code-design/SKILL.md","content":"---\\nname: code-design\\ndescription: 设计 deep modules 与可测试 seams；当任务需要收窄 interface 并集中复杂性、分类依赖、选择 adapter 或比较模块边界方案时使用。\\n---\\n\\n# Code Design\\n\\n目标是 **deep module**：调用方学习较小的 interface，获得较多行为；维护者把变化、知识与验证集中在一处。\\n\\n## Vocabulary\\n\\n在设计与评审中使用同一组词：\\n\\n- **Module**：任何同时拥有 interface 与 implementation 的事物，可以是函数、类、包或 vertical slice。_Avoid_: component、service、unit。\\n- **Interface**：调用方正确使用 module 必须知道的全部事实，包括类型、invariants、顺序、错误、配置与重要性能约束。_Avoid_: 只把 signature 或 TypeScript `interface` 当作全部 interface。\\n- **Implementation**：module 在 interface 后隐藏的行为与数据。\\n- **Depth**：interface 的 leverage；每学习一个单位 interface 可使用多少行为。interface 接近 implementation 复杂度时 module 是 shallow。\\n- **Seam**：无需修改调用点即可替换行为的位置，是 module interface 所在处。_Avoid_: boundary，避免与 bounded context 混淆。\\n- **Adapter**：在 seam 上满足 interface 的具体实现角色。\\n- **Leverage**：调用方从 depth 获得的复用收益。\\n- **Locality**：维护者把同一变化、bug 与验证保持在一处的收益。\\n\\n一个概念只有一个术语；命名、图示和方案比较均使用以上 vocabulary。\\n\\n## Design lens\\n\\n- **Depth 属于 interface**：更多 implementation lines 不会自动产生 depth；只有调用方需要理解的复杂度下降才会。\\n- **Deletion test**：移除 module 后，若复杂性在多个调用方重新出现，它提供了 locality；若只少一层转发，它是 shallow middle man。\\n- **Interface is the test surface**：调用方与测试穿过同一个 seam 观察行为。需要越过 interface 才能验证时，重新检查 interface 或 seam placement。\\n- **Hide policy, expose intent**：把顺序、重试、状态转换和依赖编排藏在 module 内，让调用方表达要得到的结果。\\n\\n评估每个候选 module：列出调用方必须知道的 interface、被隐藏的 complexity、预期 adapters、可观察结果，以及变化会集中在哪里。无法指出 leverage 或 locality 的候选保持 inline 或继续重构。\\n\\n## Testability\\n\\n- dependencies 从拥有 policy 的 module 外部注入；module 自己拥有其内部协作方式。\\n- 优先返回或暴露可观察结果，使测试通过 interface 判断行为。\\n- 新 interface 的 methods 与 parameters 只表达真实 use cases 和 invariants。\\n\\n每个新增或改变的 seam 都有真实 variation，每个测试都通过约定 interface 观察行为，且 implementation 细节没有泄漏到调用方时，设计检查完成。\\n\\n## Conditional references\\n\\n- 一组 shallow modules 因 dependencies 无法直接合并时，读取 [`DEEPENING.md`](DEEPENING.md)，按依赖类别选择 seam、adapter 与替换式测试策略。\\n- 关键 interface 存在多个合理形态且选择会显著影响 depth 或 locality 时，读取 [`DESIGN-IT-TWICE.md`](DESIGN-IT-TWICE.md)，并行生成不同约束下的设计再比较。\\n"},{"path":"references/skills/code-review/SKILL.md","content":"---\\nname: code-review\\ndescription: 双轴审查 fixed point 以来的 branch 或工作区 diff；当用户要求 review PR、branch、commit range 或 WIP 变更时使用。\\n---\\n\\n# Code Review\\n\\n**Standards** 与 **Spec** 是独立轴：代码可以忠实实现错误规范，也可以正确实现需求却违反仓库规则。使用两个并行 sub-agents 隔离 context，最后保持双轴报告。\\n\\n## 1. 固定审查范围\\n\\n解析用户给出的 commit、branch、tag 或 merge-base。用户未指定时，从执行契约或 PR/branch 上下文推断；仍有多个合理基准才询问。\\n\\n- 只审查已提交 branch：使用 `git diff <fixed-point>...HEAD`。\\n- 审查 staged 或 unstaged WIP：使用 `git diff <fixed-point>`，使 HEAD 后提交和当前工作区都进入范围。\\n\\n记录可复现的 diff 命令与 `git log <fixed-point>..HEAD --oneline`。fixed point 可解析、diff 非空、工作区变更是否纳入已明确时，本步骤完成。\\n\\n## 2. 定位两轴来源\\n\\nSpec 按顺序取自：用户指定路径；与 branch/feature 匹配的 `docs/{domain-name}/plans/`；用户确认的其他来源。用户确认不存在 spec 时，Spec 轴记为 `not available`。\\n\\nStandards 包括仓库中实际适用于 diff 的 `AGENTS.md`、`CONTRIBUTING.md`、编码规范与 `/code-spec`。完整读取 [`SMELL-BASELINE.md`](SMELL-BASELINE.md)；它只提供判断型 heuristics。\\n\\n每个变更文件都映射到适用 standards，每项 spec 要求都有可引用位置时，本步骤完成。\\n\\n## 3. 并行审查\\n\\n同时派遣两个 sub-agents，并给每个 agent 相同的 diff 命令、commit 列表和审查范围：\\n\\n- **Standards agent**：读取全部 standards 来源与 `SMELL-BASELINE.md`。按文件和 hunk 报告违反的仓库规则，并分别标注 baseline smell；每项引用代码位置与规则来源，区分硬规则和 judgement，不超过 400 字。\\n- **Spec agent**：读取全部 spec 来源。报告缺失或部分实现的 requirement、scope creep、以及行为存在但实现不符合要求的项；每项同时引用代码与 spec 位置，不超过 400 字。没有 spec 时不派遣该 agent。\\n\\n两个 agent 都覆盖完整 diff，且每项发现具备可复核位置、违反的规则或 requirement、影响与理由时，本步骤完成。\\n\\n## 4. 汇总\\n\\n在 `## Standards` 与 `## Spec` 下分别呈现发现，保持各 agent 的严重度与顺序；Spec 不可用时明确说明。最后分别给出每轴发现数量与该轴最高严重度问题，不跨轴生成总排名。\\n\\n双轴均有明确结论、每项 finding 可追溯、无发现的轴显式报告 pass 时，本 review 完成。\\n"},{"path":"references/skills/code-review/SMELL-BASELINE.md","content":"# Standards smell baseline\\n\\n这些 Fowler code smells 是 judgement heuristics，不是硬性违规。仓库明确认可的形态覆盖本 baseline；工具已机械强制的内容不进入人工 review。\\n\\n- **Mysterious Name**：名称没有揭示实际职责。重命名；无法诚实命名通常表示设计仍含混。\\n- **Duplicated Code**：相同逻辑形态出现在多个变更位置。提取共同变化的形态。\\n- **Feature Envy**：method 使用另一个 object 的数据多于自身数据。把行为移向它依赖的数据。\\n- **Data Clumps**：同一组 fields 或 params 总是共同出现。用一个有领域含义的 type 表达它们。\\n- **Primitive Obsession**：primitive 或 string 承担了重要领域概念。提供能守住其不变量的 type。\\n- **Repeated Switches**：针对同一类型的条件 cascade 反复出现。集中 dispatch 或使用 polymorphism。\\n- **Shotgun Surgery**：一项逻辑变化迫使多个位置分散编辑。把共同变化收拢到一个 module。\\n- **Divergent Change**：一个 module 因多个不相关原因变化。按变化原因拆分职责。\\n- **Speculative Generality**：diff 为 spec 未要求的未来需求增加 abstraction 或 hook。收缩到当前真实需求。\\n- **Message Chains**：调用方暴露长 navigation chain。让最靠近数据的 module 隐藏 traversal。\\n- **Middle Man**：module 主要只是继续 delegate。把 interface 交给真正拥有行为的 module。\\n- **Refused Bequest**：subclass 或 implementer 忽略大部分继承契约。改用更诚实的 composition 或 interface。\\n\\n每个 smell finding 必须引用具体 hunk，说明为何该 heuristic 在此处成立，并给出与仓库规则一致的改进方向。\\n"},{"path":"references/skills/dev-gate/SKILL.md","content":"---\\nname: dev-gate\\ndescription: 门禁进入实现前的执行基线；当 questing 完成或领域设计完成时使用。\\n---\\n\\n# Dev Gate\\n\\n本 skill 只交付 `ready` 或 `not ready`。`ready` 表示实现输入和执行边界已被用户明确确认。\\n\\n## 1. 核对准入产物\\n\\n确定本次实现只由一个权威入口驱动，并核对对应条件：\\n\\n- `/questing`：frontier 为空，用户已确认共同理解。\\n- `/quest-with-domain`：上述条件成立，已确认术语、关系和 ADR 均已落入领域文档。\\n\\n从现场文件和工具查明可检索事实；只把会改变执行边界的未决选择交给用户。权威入口唯一、其完成标准有可核查证据且未知项已清空时，本步骤完成。\\n\\n## 2. 建立基线\\n\\n从权威入口、工作区配置和用户约束形成三项基线：\\n\\n- **工作环境**：项目管理、本地环境、CI/CD 入口、运行环境及必需外部服务。\\n- **执行契约**：本轮范围、固定比较点、步骤顺序、停止点、交付物，以及是否包含 commit 等状态变更。\\n- **质量门禁**：`/code-test` 与 `/verifying` 的最小门禁集合；每项都带事实来源、执行入口和通过条件。\\n\\n现场没有声明的值保持为待确认决策，不把猜测写成基线。三项均完整且互不矛盾时，本步骤完成。\\n\\n## 3. 确认与交接\\n\\n向用户提交一张简短基线卡，逐项列出触发路径、工作环境、执行契约和质量门禁。用户修正时先更新对应权威文档，再重新提交受影响部分。用户明确确认基线卡时，本步骤完成。\\n\\n实现期间任一基线发生实质漂移时，本 gate 重新打开；更新受影响的权威文档并再次取得用户确认。\\n"},{"path":"references/skills/quest-with-domain/ADR-FORMAT.md","content":"# ADR 格式\\n\\nADR 位于唯一 owner 的 `docs/{domain-name}/adr/`，文件名按域内顺序使用 `0001-中文决策名.md`。目录在首份 ADR 出现时创建；跨域决策由其他域链接引用。\\n\\n## 资格门槛\\n\\n一项决策同时满足以下条件才成为 ADR：\\n\\n1. **Hard to reverse**：未来改变它有显著成本。\\n2. **Surprising without context**：只看实现无法理解为何这样选择。\\n3. **Real trade-off**：存在真实备选，并因明确理由选择其一。\\n\\n常见候选包括架构形态、跨域集成、带 lock-in 的技术选择、owner 与 scope 边界、刻意偏离常规路径的方案，以及代码不可见的外部约束。库的普通选用、易逆转偏好和没有备选的显然选择不形成 ADR。\\n\\n## 最小模板\\n\\n```md\\n# {决策的简短标题}\\n\\n{1-3 句话说明 context、决策与理由。}\\n```\\n\\nADR 可以只包含这个段落。下列内容仅在增加长期解释价值时添加：\\n\\n- `Status` frontmatter：`proposed | accepted | deprecated | superseded by ADR-NNNN`。\\n- `Considered Options`：被拒绝的备选值得未来读者记住。\\n- `Consequences`：存在不明显且重要的下游影响。\\n\\n创建前扫描 owner 域现有 ADR 的最大编号并递增 1。文件名、编号、owner、决策理由和引用均一致时完成。\\n"},{"path":"references/skills/quest-with-domain/CONTEXT-FORMAT.md","content":"# Context 格式\\n\\n本文件只定义领域 glossary 与 map 的文档形状；布局、归属与生命周期由 `<nano-flow-skill-root-dir>/references/DOMAIN.md` 负责。\\n\\n## 业务域 CONTEXT.md\\n\\n```md\\n# {Context 名称}\\n\\n{一到两句话说明该 context 的领域职责。}\\n\\n## Language\\n\\n**Order**:\\n{一到两句话定义它是什么}\\n_Avoid_: Purchase, transaction\\n\\n**Invoice**:\\n交付后发送给 customer 的付款请求。\\n_Avoid_: Bill, payment request\\n```\\n\\n- 一个概念选择一个 canonical term，把会造成歧义的同义词列入 `_Avoid_`。\\n- 定义说明概念是什么，保持一到两句话；规则与实现细节进入其他权威产物。\\n- 只收录当前业务域特有的语言；通用编程概念留在代码或工程文档。\\n- 自然形成多个概念簇时使用 subheading；单一 cohesive area 保持 flat list。\\n\\n## CONTEXT-MAP.md\\n\\n```md\\n# Context Map\\n\\n## Contexts\\n\\n- [Ordering](./docs/ordering/CONTEXT.md) - 接收并跟踪 customer orders。\\n- [Billing](./docs/billing/CONTEXT.md) - 生成 invoices 并处理 payments。\\n\\n## Relationships\\n\\n- **Ordering → Billing**: Ordering 发出 `OrderCompleted`；Billing 消费它并生成 invoice。\\n```\\n\\nmap 只承担索引与跨域关系；每项关系只记录一次。没有跨域关系时省略 `Relationships`。\\n\\n新业务域使用稳定、简短的 kebab-case `domain-name`，同时创建 `docs/{domain-name}/CONTEXT.md` 和 map entry，使每条链接可解析。\\n"},{"path":"references/skills/quest-with-domain/SKILL.md","content":"---\\nname: quest-with-domain\\ndescription: 维护领域模型：拷问设计并同步已确认的领域术语、跨域关系与 ADR；当讨论需要改变领域 context 文档或领域决策记录时使用。\\n---\\n\\n# Quest with Domain\\n\\n使用 `/questing` 的 rounds 与 frontier 打磨 domain model，并在结论成立的同一轮写入其权威位置。读取领域语言供其他任务使用不触发本 skill。\\n\\n## 1. 进入并定域\\n\\n完整读取 `<nano-flow-skill-root-dir>/references/DOMAIN.md`。从工作区根 `CONTEXT-MAP.md` 定位所有相关业务域，再读取对应 `CONTEXT.md` 与相关 ADR；归属仍是用户决策时提出带推荐答案的问题。\\n\\n相关业务域与跨域关系均已确定时，本步骤完成。\\n\\n## 2. 清空设计 frontier\\n\\n以现有 glossary 和 ADR 为约束运行 `/questing`：\\n\\n- 术语冲突或 overloaded 时，要求在现有 canonical term 与新概念之间作出明确区分。\\n- 关系或规则含糊时，用具体 scenario 与 edge case 暴露边界。\\n- 用户对现状的描述可由代码验证时，检查实际实现并把矛盾作为 frontier 问题。\\n- 环境事实由 agent 检索；trade-off 与领域选择由用户决定。\\n\\nfrontier 为空、代码与口头模型的矛盾已解决或明确记录、用户确认共同理解时，本步骤完成。\\n\\n## 3. 就地维护语言\\n\\n出现已确认的新业务域、术语或跨域关系时，读取 [`CONTEXT-FORMAT.md`](CONTEXT-FORMAT.md) 并在当轮更新：\\n\\n- 各产物的归属规则以 CONTEXT-FORMAT.md 为权威。\\n\\n每项确认内容恰好有一个权威位置，map 链接可解析，glossary 中的 canonical term 与 `_Avoid_` 词互不冲突时，本步骤完成。\\n\\n## 4. 记录有价值的 ADR\\n\\n出现架构形态、跨域集成、技术锁定、scope owner 或其他长期 trade-off 时，完整读取 [`ADR-FORMAT.md`](ADR-FORMAT.md)。按其中的资格门槛逐项判断；达标的决策写入唯一 owner 的 ADR，未达标的候选不创建文档。\\n\\n每个候选均有判断结果，所有达标决策只记录一次且理由与已确认 trade-off 一致时，本步骤完成。\\n\\n## 5. 验证并返回\\n\\n修改领域文档后，从工作区根执行：\\n\\n```powershell\\nnode <nano-flow-skill-root-dir>/scripts/check-domain.mjs .\\n```\\n\\n校验通过后，本步骤完成。\\n"},{"path":"references/skills/questing/SKILL.md","content":"---\\nname: questing\\ndescription: 拷问计划、决策或想法直到共同理解；当用户要求 grill/stress-test，或另一 skill 需要清空决策 frontier 时使用。\\n---\\n\\n# Questing\\n\\n持续拷问直到与用户达成共同理解。把讨论映射为 **design tree**：每个待定决策是一个节点，依赖它的决策位于下游；**frontier** 是前置已经确定、现在无需猜测即可回答的全部节点。\\n\\nsubagent 的选择先形成 **draft tree**；用户统一评价后，接受或修改的选择才进入 **confirmed tree**。事实由 agent 查明，决策最终由用户确认。\\n\\n## 1. 建树与取证\\n\\n从用户目标、已有答案和现场事实建立 design tree，区分事实节点与决策节点，并标明每个节点的前置依赖。通过文件和工具查找可获得的事实；需要继续探索的事实保持为未决前置，不转交用户回答。\\n\\n所有当前可见分支都已进入 tree；每个已知事实都有可复核来源，每个未知事实都是正在进行的探索或带解除条件的 blocker，每个决策节点都有明确前置时，本步骤完成。\\n\\n## 2. 收敛 draft tree\\n\\n1. **Frontier**：计算当前整个 frontier。每个节点使用在本次 quest 内唯一且递增的编号，列出互斥选择和前置问题，说明下游边界，并给出 agent 的推荐答案及关键理由。同一轮问题互不依赖；依赖本轮答案的节点留在下一轮。每个 frontier 节点恰好对应一个问题，且没有前置未决的问题混入本轮时完成。\\n2. **Subagent**：整个 quest 固定一个 subagent，负责未决事实探索和全部轮次的选择；用户只评价审阅文件，不逐轮回答。首次启动时提供目标、事实来源、约束、未决事实以及本轮全部问题与选项；后续轮次通过 follow-up 只补充 tree 变化、新事实和新 frontier。未决事实只让依赖它的节点等待，其余 frontier 继续回答。当前轮每题都收到一项选择及关键理由、可检索事实均有结果或仍是显式未决前置，且 subagent 身份未改变时完成；原 subagent 无法继续时，以其可恢复为解除条件形成 blocker。\\n3. **记录**：首次写入前完整读取 [`template.md`](template.md)，在工作区根创建或续写 `.flow/quest/YYYY-MM-DD-中文工作名.md`。日期使用首次创建时的本地日期，中文工作名从目标提炼；同一 quest 始终续写同一文件，同名文件属于其他 quest 时改用更具体的工作名。按模板把本轮全部问题与答案插入最终的「补充说明」之前。所有模板占位符已替换、每题的推荐与选择可比较、且「补充说明」仍是最后一节时完成。\\n4. **推进**：用 subagent 的选择更新 draft tree，记录已确定决策、被排除分支与新节点，再计算 frontier。本轮每项选择都已应用且下一轮 frontier 已完整计算时完成。\\n\\n重复本节；探索中的事实只暂停其下游，其他 frontier 继续推进。每个可达分支都有结论、frontier 为空、没有静默假设，且全部问题均已写入审阅文件时，draft tree 完成。此阶段只形成 draft。\\n\\n## 3. 统一评价\\n\\ndraft tree 完成后，向用户提供审阅文件路径，请用户一次性评价全部问题，并把所有问题均未涉及的信息写入末尾的「补充说明」。等待用户说明评价完成，再重新读取所有轮次并按依赖顺序重放：\\n\\n- 空白的「修改意见」确认 subagent 的选择，但只在该节点仍然可达且前置保持成立时生效。\\n- 非空的「修改意见」是用户的新答案或约束；能唯一确定答案时替换原选择，否则重新打开该节点。\\n- 上游修改使前置不成立时，其下游问题与答案退出 confirmed tree，即使修改意见为空。\\n- 非空的「补充说明」进入 design tree，按事实、约束或决策分类；HTML 注释不算内容。新事实沿用第 1 节的取证规则，只把 judgement 放入新 frontier。\\n\\n每个问题均已评价、全部反馈已纳入或因依赖失效而排除、新事实已查明或成为 blocker，且新 frontier 已完整计算时，本步骤完成。修改意见与补充说明均为空时，draft tree 整体转为 confirmed tree；任一项非空且产生新 frontier 时，返回第 2 节，由同一 subagent 进行新一轮选择。\\n\\n## 完成\\n\\n只有以下条件全部成立时，本 skill 完成：\\n\\n- design tree 的每个可达分支都已访问，frontier 为空，没有静默假设，未知事实均已查明。\\n- 审阅文件符合模板，包含全部问题、推荐答案、选择答案、双方关键理由、修改意见与补充说明，且「补充说明」位于末尾。\\n- agent 已复述 confirmed tree，用户明确确认已经达成共同理解。\\n\\n`.flow/quest` 审阅文件是本 skill 唯一直接写入的产物；用户确认前不执行实现，其他权威文档仍由调用者负责。\\n\\n仍有事实或 subagent blocker 时，保存现场与解除条件并暂停；该结果不满足本 skill 的完成条件。\\n"},{"path":"references/skills/questing/template.md","content":"# {中文工作名}\\n\\n## 第 {round} 轮\\n\\n### ❓ Q{question} — {标题}\\n\\n- 问题：{问题与互斥选择}\\n- 前置问题：{Q 编号或无}\\n- 下游边界：{该答案会改变什么}\\n- ➡️ 推荐答案：{agent 的推荐答案}\\n- 推荐理由：{关键理由}\\n- 选择答案：{subagent 的选择答案}\\n- 选择理由：{关键理由}\\n- 修改意见：\\n\\n### ❓ Q{question + 1} — {标题}\\n\\n- 问题：{问题与互斥选择}\\n- 前置问题：{Q 编号或无}\\n- 下游边界：{该答案会改变什么}\\n- ➡️ 推荐答案：{agent 的推荐答案}\\n- 推荐理由：{关键理由}\\n- 选择答案：{subagent 的选择答案}\\n- 选择理由：{关键理由}\\n- 修改意见：\\n\\n## 补充说明\\n\\n<!-- 填写所有问题均未涉及的用户补充说明；没有则留空。 -->\\n"},{"path":"references/skills/simplify/SKILL.md","content":"---\\nname: simplify\\ndescription: 对整个工作区（增量+存量）做简化审查与清理：删减冗余、复用既有实现、收敛复杂度、消除性能浪费；判断前参考全部领域 ADR 与 Plan。\\ndisable-model-invocation: true\\n---\\n\\n# Simplify\\n\\nSimplify 对整个工作区（或用户指定范围）的**增量与存量**代码做 cleanup pass：删减冗余、消除浪费，每个候选都由证据支撑。正确性审查属于 `/code-review`。\\n\\n贯穿全程的两个词：\\n\\n- **证据**：每条发现都给出 file:line、消费方证据与现状成本；说不出代价的「看起来复杂」是 nit，不是候选。\\n- **删减**：deletion over addition；成功的度量是代码变少，不是变多。\\n\\n## 1. 确定范围\\n\\n默认范围是整个工作区的 production 代码；用户可缩窄到业务域、目录或文件。存在未交付的增量（工作区或 branch diff）时，增量与存量纳入同一范围一并评判，不分开处理。记录范围边界与排除项（构建产物、依赖目录、自动生成物）；范围明确时，本步骤完成。\\n\\n## 2. 读取全部领域约束\\n\\n按 `<nano-flow-skill-root-dir>/references/DOMAIN.md` 的定位规则读取范围内触及的**所有**业务域：`CONTEXT-MAP.md` → 每个域的 `docs/{domain}/CONTEXT.md` → 该域**全部** `adr/` → 该域**全部** `plans/`；再读取适用的 `AGENTS.md` 与 `/code-spec`。\\n\\n- ADR 与 CONTEXT.md 是 **intentional design** 的权威记录：受保护的 seam、契约与术语不作为删减候选；简化主张与 ADR 冲突时，显式列出冲突与重议理由交给用户，不静默绕过。\\n- `active` 与 `reference` 的 Plan 是方向约束：候选不得砍掉尚未交付的 requirement，不恢复已确认路线明确排除的形态；`archived` 的 Plan 只作为「为何移除或废弃」的历史证据，不作为权威来源。\\n\\n范围内每个业务域的约束已读取、受保护设计已列出时，本步骤完成。\\n\\n## 3. 分区并行 survey\\n\\n把范围按业务域或顶层目录划分分区，派遣并行 subagents：每个 subagent 拿一个分区、第 2 步的完整约束清单和下面四个角度；用户指定 focus（复用/删减/深度/性能）时只派对应角度。范围窄（单目录以内）时不分区，直接执行；无 subagent 能力时在本 context 按分区顺序执行同一流程，并在汇报中说明。\\n\\n- **复用**：与既有实现重复；手写逻辑已有 stdlib、平台原生能力或已安装依赖的等价物。每条发现点名应复用的对象及其位置。\\n- **删减**：dead code、speculative generality、单一实现抽象、pass-through wrapper、冗余状态、复述代码的注释；同一逻辑的更短形态。\\n- **深度**：generic path 上为单一调用方打的 special case、叠在旧 workaround 上的 workaround、为绕过真实修复而加的 wrapper。深层修复只标记，不在本 pass 实施。\\n- **性能**：不必要的工作（冗余计算、重复读取或调用、N+1 访问）、可并行却串行的独立操作、热路径（启动、per-request）上的重活、过宽读取、无界增长或未清理的 listener/handle；先检查后操作改为直接操作并处理错误（TOCTOU）。并发化与访问模式重构按 RISKY 呈现。\\n\\n调查纪律（Chesterton\'s fence）：标记删除前用 `git blame`、消费方检索与第 2 步约束确认存在理由；理由不明标注 `confidence: low`，靠猜的发现直接丢弃。每个符号的消费方先分类：只有测试或文档消费且行为不承重的是强候选；存在 production 消费方的是 feature decision，不是 cleanup。第一个好候选不停止 survey。\\n\\n每条发现按统一格式报告：`file:line → 问题 → 现状成本 → 建议 | confidence: high/medium/low | risk: SAFE/CAREFUL/RISKY`。\\n\\n- **SAFE**：已证实不改变行为（无生产消费方的 dead code、未使用的 import）。\\n- **CAREFUL**：语义保持（extract、inline、局部重命名、拍平嵌套）。\\n- **RISKY**：触及公共契约、行为可能变化或命中受保护设计。\\n\\n每个分区都被全部角度完整覆盖、每条发现具备可复核位置与成本时，本步骤完成。\\n\\n## 4. 汇总与应用\\n\\n合并去重；静默丢弃证据不足的发现；冲突按「正确性 > 用户指定 focus > 可读性与复用 > 微优化」裁决，微优化只在已确认的热路径上压倒可读性；互斥且均可行时选改动更小者并备注另一方案。按 risk tier 应用：\\n\\n- **SAFE**：直接应用，完成后运行 targeted tests。\\n- **CAREFUL**：逐文件应用并运行该文件的 targeted tests，破坏即回退该条。\\n- **RISKY**：只呈现——风险、测试覆盖状态、建议跟进方式（新 issue 或 `/quest-with-domain`），不自动应用。\\n\\n用户要求只报告时全部呈现、不修改。修改范围限于发现本身及修复所需的最小邻域。所有 SAFE 与 CAREFUL 项已应用或回退、RISKY 项已逐条呈现时，本步骤完成。\\n\\n## 5. 验证与收口\\n\\n运行触及文件的 targeted tests 与仓库 linter/typecheck；任一修复破坏验证则单独回退并报告。有意保留的已知上限（全局锁、O(n²) 扫描、naive heuristic）按 `/code-spec` 写说明性注释，点名上限与升级触发条件。\\n\\n汇报按分区、角度与 tier 分组：已应用的修复、有意跳过及理由、净删除规模；inline 顺序执行时显式说明。验证通过、汇报完整时，本 skill 完成。\\n\\n## 边界\\n\\n- 不猎 correctness bug：survey 撞见的真实 bug 单独显著报告，不混入 cleanup 修复。\\n- 永不简化掉：`/code-spec` 风险边界要求的校验与错误处理、安全措施、用户明确要求的事物。\\n- 公共契约（导出名、API 路径、配置键）的改动一律 RISKY。\\n- Apply ≠ rewrite：本 pass 不重构范围之外的模块；深层修复经 RISKY 通道交给用户决定。\\n"},{"path":"references/skills/to-anki/agents/openai.yaml","content":"policy:\\n  allow_implicit_invocation: false\\n"},{"path":"references/skills/to-anki/SKILL.md","content":"---\\nname: to-anki\\ndescription: 将 Markdown 学习笔记,词表,现成问答或完形填空内容转换为 Anki 卡片.\\ndisable-model-invocation: true\\n---\\n\\n# Anki 制卡\\n\\n将用户资料转换为 Anki 卡片, 遵循流程 **对齐 → 解析 → 预览 → 确认 → 创建 → 核验**.\\n\\n## 1. 对齐批次\\n\\n从用户材料和当前环境中推断可验证的信息, 只询问会阻塞预览的缺项:\\n\\n- 源材料: 路径, 文件列表或用户直接提供的内容;\\n- 卡片类型: 学习笔记基础卡, 词汇卡, 问答卡或完形填空卡;\\n- 目标: 根牌组, 子牌组映射, 笔记类型和字段映射;\\n- 格式: 正反面格式, 来源链接, 标签及需要保留的 HTML;\\n- 重复策略: 默认跳过重复项并记录, 只有用户明确要求时才创建重复项;\\n\\n**完成标准：** 每张候选卡的来源, 目标牌组, 笔记类型, 字段映射, 格式, 重复策略都有明确值; 所有未验证信息均已标为假设.\\n\\n## 2. 解析源材料\\n\\n原材料按章节和语义知识点划分, 保留标题层级与来源行号\\n\\n所有候选卡都应满足:\\n\\n- **原子化** 一张基础卡聚焦一个主要回忆目标;\\n- **忠实** 答案只来自源材料, 补充内容必须单独标明;\\n- **可追溯** 记录来源, 或原始条目的稳定标识;\\n- **可复现** 相同输入和规则产生相同的牌组, 字段与卡片边界.\\n\\n### Markdown 学习笔记约定\\n\\n- 记录 markdown 文件可能包含的 YAML frontmatter 中的 `id`; 子牌组名取文件名移除数字前缀和 `.md` 后的部分, 例如 `010-Go-入门.md` → `Go-入门`; 默认一个 markdown 对应一个 anki 卡片;\\n- 默认配置: 根牌组以 `<topic>` 作为预览假设(用户未指定时); 笔记类型 `基础`, 字段为 `正面` 和 `背面`; 正面 `根牌组-章节名-知识点`; 背面 `<a href=\\"https://wiki.kongxiaohan.cn/<id>\\">跳转wiki</a>`;\\n- 只有链接基址与 `id` 同时存在时才生成链接; 缺少任一项时, 把背面格式列为待确认.\\n\\n**完成标准：** 每张候选卡都能追溯到一个源条目或明确的 Markdown 范围, 每个源条目恰好归入候选或排除之一.\\n\\n## 3. 生成强制预览\\n\\n每次预览分配递增版本号, 例如 `v1`, `v2`. 预览推荐格式:\\n\\n```markdown\\n预览版本:v1\\n候选卡片:57\\n笔记类型:基础(正面 → 正面,背面 → 背面)\\n\\n|   # | 来源           | 目标牌组 | 正面/填空文本 | 背面/答案      |\\n| --: | -------------- | -------- | ------------- | -------------- |\\n|   1 | 010-Go-入门.md | Go       | Go-Go-入门    | 统一 Wiki 链接 |\\n\\n超出建议行数的卡片及保持合并的理由\\n\\n所有假设,未验证项和可能的冲突\\n```\\n\\n**完成标准：** 表格行数与候选总数一致; 每个源条目恰好归入候选或排除之一; 仅凭预览即可复现待执行批次.\\n\\n## 4. 等待确认\\n\\n用户明确确认当前预览版本后, 才进入创建或导出. 用户提出修改, 源文件发生变化或任何字段映射改变时, 生成下一个预览版本并重新等待确认.\\n\\n**完成标准：** 当前版本获得明确确认, 且确认后批次内容未发生变化.\\n\\n## 5. 创建\\n\\n写入 Anki 一律使用 `nnf anki` 命令(经 AnkiConnect 操作本机 Anki), 不直接读写 Anki 数据库或 collection 文件; 具体命令与 option 以 `/nano-flow-cli` 的 Anki 路由和现场 `nnf anki --help` 为事实来源.\\n\\n创建并写入 anki 卡片:\\n\\n1. 创建缺失的目标牌组, 并按确认预览逐条或批量创建卡片;\\n2. 导出时, 按确认的字段顺序生成 UTF-8 TSV/CSV, 按格式引用含分隔符或换行的字段, 并原样保留 HTML;\\n3. 若实际模板, 字段或目标状态与预览不一致, 返回预览步骤生成新版本.\\n\\n**完成标准：** `创建数或导出记录数 + 跳过数 + 失败数 = 已确认候选数`, 且每个结果都能映射回预览中的唯一序号.\\n\\n## 6. 核验交付\\n\\n写入后, 从 Anki 重新读取目标笔记, 逐项核对牌组, 笔记类型和关键字段, 最终报告:\\n\\n- 已确认的预览版本和候选总数;\\n- 每个牌组的创建或导出数量;\\n- 笔记类型与字段映射;\\n- 创建, 跳过, 失败数量及原因;\\n- 核验方式, 结果和仍未解决的问题.\\n\\n**完成标准：** 从 Anki 回读的目标笔记在牌组, 笔记类型和关键字段上与已确认预览一致, 报告中的数量与原因均与回读结果相符; 不一致项已明确列出.\\n"},{"path":"references/skills/to-issues/SKILL.md","content":"---\\nname: to-issues\\ndescription: 长程任务的可恢复状态：持续维护 spec、设计 frontier 与 tracer-bullet issue 图；当工作需要跨会话推进，或单会话内仍需持久化检查点时使用。\\nargument-hint: \\"要持久化并推进什么长任务?\\"\\n---\\n\\n# To Issues\\n\\n完整读取 `<nano-flow-skill-root-dir>/references/DOMAIN.md`，以其中的领域布局与 Plan 生命周期为权威约束。\\n\\n## 判断是否跳过\\n\\n提供 FLOW 上下文时，如果工作是需持久化推进的长程任务，向用户推荐继续执行，反之推荐跳过，等待用户确认。\\n未提供 FLOW 上下文时，直接执行。\\n\\n## 建立或维护\\n\\n写入 spec 或 issue 前完整读取 [`TEMPLATE.md`](TEMPLATE.md), 以其中的模板和扩展规则为单一真源.\\n\\n1. **定域与查明事实**——从 `CONTEXT-MAP.md` 定位业务域, 工作落在**一个**域内; 跨域工作先在 map 中确认关系并选定主域, 或按域拆成多次拆分. 读取该域 `CONTEXT.md`, 相关 ADR / Workflow, 已有 story, spec, issue, commit 或 diff 等工作涉及产物.\\n2. **就地落盘**: 根据事实就地创建或更新 `spec.md` 与 `issue.md`;\\n   - 落盘内容是确认后的结论, 不是对话记录; 无对应小节的按模板扩展处理;\\n   - 使用**tracer-bullet 规则**创建 issue 文件;\\n3. **用户确认**：反复确认并迭代，直至通过；\\n4. **校验并交接**：从工作区根运行 `node <nano-flow-skill-root-dir>/scripts/check-domain.mjs .`。\\n\\ntracer-bullet 规则:\\n\\n- 以一个完整的用户结果或业务能力为边界, 只贯穿实现该能力所需的层级;\\n- 在直接依赖成立后, 可以独立实现, 交付和验收，每条直接依赖都说明原因和本 issue 消费的产物或契约;\\n- 用一个可独立判定的结果定义最小验收信号;\\n- 让每个 issue 更容易实现的 prefactoring 排最前——\\"Make the change easy, then make the easy change\\";\\n- 大范围机械重构是唯一例外: expand-contract 序列——expand → 按影响面分批 migrate, 每批一个 issue → contract 删除旧形态.\\n\\n**完成标准:** 工作范围无遗漏, 每项交付责任只有一个归属 issue; 依赖图至少有一个根 issue, 无环; 用户确认边界和依赖图正确；校验通过，全部 issue 为 `pending`；仅凭领域文档、Plan 与 Flow 运行态即可恢复工作\\n\\n## 推进 Plan\\n\\n1. 从 `plans/active/` 的 spec 派生表定位 `pending` 且直接依赖全部 `completed` 的 frontier；有多个候选时使用用户已确认的优先级。\\n2. 领取一个 issue，在同一 session 内交付或形成真实 blocked 结果；当前 issue 结束后才领取下一个。\\n3. 新发现的事实更新 spec；新发现的工作建立新 issue，尚不能精确表述的设计 frontier 进入 spec「待定」。当前 issue 的边界保持不变，符合 ADR 资格的长期 trade-off 由 `/quest-with-domain` 写入领域文档。\\n4. 每次暂停前同步 issue 状态、spec 派生视图与证据；恢复时只信任文件与运行态，不依赖先前对话。\\n5. 全部 issue 完成后与用户确认参考价值，把整个 Plan 移入 `reference/` 或 `archived/`，并重新运行领域校验。\\n\\n**完成标准:** 一个领取结果有且只有一个终态、状态与 spec 派生视图一致、证据链可复核时，本轮推进完成。\\n"},{"path":"references/skills/to-issues/TEMPLATE.md","content":"# Plan 文档模板\\n\\n## spec.md 模板\\n\\n```markdown\\n---\\nstatus: pending\\n---\\n\\n# {工作名}\\n\\n## 问题\\n\\n{用户要得到的结果, 已知约束}\\n\\n## 方案\\n\\n{保持在设计层级}\\n\\n## 已排除的备选\\n\\n- {方案}: {拒绝理由}\\n\\n## 实施决策\\n\\n{模块, 接口, schema, 契约等设计层级内容; 决策密度高的片段(state machine, schema, type shape)可内联并注明出处}\\n\\n## 工作环境\\n\\n{执行该工作所需的环境信息: 例如项目管理工具, 本地开发环境, CI/CD 流水线, 运行时环境, 三方服务等}\\n\\n## 范围\\n\\n{做什么}\\n\\n## 非范围\\n\\n{不做什么}\\n\\n## 待定\\n\\n{尚不能精确表述为 issue 的未决问题、已知选项或证据、恢复条件; 澄清后 graduate 为 issue}\\n\\n## 上下文\\n\\n{通过路径或 URL 引用已有产物: PRD, story, spec, ADR, workflow, commit, diff 等; 域内文档使用 ../../ 相对路径}\\n\\n## Issue\\n\\n| #   | Issue                  | 状态    | 阻塞于 | 下一步         |\\n| --- | ---------------------- | ------- | ------ | -------------- |\\n| 01  | [{标题}](01-{标题}.md) | pending | —      | /code-delivery |\\n```\\n\\n## issue 模板\\n\\n```markdown\\n---\\nstatus: pending\\nblocked_by: []\\n---\\n\\n# {标题}\\n\\n## 交付\\n\\n{用户可感知的结果}\\n\\n## 范围\\n\\n{做什么, 不做什么}\\n\\n## 直接依赖\\n\\n- {NN}: {原因}; 消费其 {产物或契约}\\n\\n## 验收\\n\\n- [ ] {可独立判定的最小结果}\\n\\n## 上下文\\n\\n- {通过路径或 URL 引用相关产物: PRD, story, spec, ADR, workflow, commit, diff 等; 域内文档一律 ../../ 相对路径}\\n\\n## 下一步\\n\\n{决策已澄清: /code-delivery; 仍需澄清: /quest-with-domain}\\n\\n## 阻塞记录\\n\\n{仅 status 为 blocked 时保留: 障碍与解除条件}\\n\\n## 交付记录\\n\\n{仅 status 为 completed 时保留: 交付物与验证证据链接}\\n```\\n\\n## 模板扩展\\n\\n模板章节是最小集合, 不是封闭集合. 允许根据工作的性质添加模板未固定的章节, 但新增章节必须**真正独特且必要**:\\n\\n- **独特**——内容无法归入任何现有章节, 语义不重叠;\\n- **必要**——缺少它, 该工作的执行或验收会缺失关键信息.\\n\\n两条同时满足才添加, 章节标题使用中文; 否则归入现有章节或省略.\\n"},{"path":"references/skills/to-note/agents/openai.yaml","content":"policy:\\n  allow_implicit_invocation: false\\n"},{"path":"references/skills/to-note/SKILL.md","content":"---\\nname: to-note\\ndescription: 将给定材料整理为高密度,可检索的 Markdown 知识笔记.\\ndisable-model-invocation: true\\n---\\n\\n# 知识笔记\\n\\n以**覆盖账本**守住知识完整性, 以 **语义概念** 和 **知识密度** 组织笔记, 以 **费曼技巧** 提高学习效率, 以 **电报体** 提高信息效率, 根据用户输入制作 Markdown 知识笔记;\\n\\n## 1. 建立覆盖账本\\n\\n完整读取源材料, 将每个有意义的内容单元登记为独立知识点;\\n为每个知识点记录来源位置, 语义相同的重复项合并为一个知识点, 并保留其全部来源位置;\\n\\n当每个源内容单元都已登记, 每个知识点都有来源位置, 且语义相同的知识点已合并时, 本步骤完成.\\n\\n## 2. 构造笔记组织结构\\n\\n一个笔记承载一个可独立学习的语义概念, 根据概念体系和知识边界进行分组;\\n基于原材料的知识密度, 决定笔记篇幅(与知识密度正比), 约束在 <= 144 行, 超过时按语义拆分, 无法再拆的连贯主题可以超出;\\n根据概念体系和知识边界, 可创建最多二级目录的树形结构;\\n保证末级目录的 markdown 文件数量 <= 13 个;\\n工作区已有笔记目录结构时, 笔记的组织结构与其对齐;\\n为覆盖账本中的每个知识点记录目标文件和目标标题;\\n\\n当覆盖账本中的每个知识点都恰好归入一个目标文件和标题, 且隶属于一个语义概念, 目录不超过二级且末级目录的 markdown 文件数量 <= 13 个, 超出行数上限的文件均有不可再拆的理由时, 本步骤完成.\\n\\n## 3. 写入笔记\\n\\n笔记先使用 **费曼技巧** 保证内容清晰易懂, 再使用 **电报体** 压缩表达; 两者冲突时, **费曼技巧** 优先.\\n新建笔记必须使用 [`template.md`](template.md), 将 `<uuid>` 替换为新的唯一 UUID; 修改现有笔记时保留原 UUID;\\n笔记顶部具有若干问题, 覆盖笔记所有知识点, 可以通过回答问题进行完整讲解和主动复习;\\n\\n### 费曼技巧\\n\\n- 简单教授: 以向初学者讲解为目的, 使用简单易懂的语言; 保留准确的专业术语, 并在首次出现时进行通俗解释;\\n- 修复盲点: 输出过程中, 不理解的地方, 需要反思/优化, 直至知识点清晰;\\n- 可以单纯根据笔记各级标题, 知道需要讲解的知识点内容;\\n\\n### 电报体\\n\\n- H1 仅用于文档主题; 正文按 H2 → ... 连续递进, 层级由知识关系决定;\\n- 普通文本使用电报体, 一个 bullet 承载一个独立知识点, 使用格式 `term: explanation;`, 富文本信息优先使用代码块\\\\表格\\\\公式\\\\图示等信息载体;\\n- 优先讲清概念与权衡, 需要精确记忆处使用额外文本补充;\\n\\n当覆盖账本中的所有目标文件均成功写入时, 本步骤完成.\\n\\n## 4. 验证笔记\\n\\n逐项核对覆盖账本, 确认每个源内容单元都映射到一个目标位置, 每个知识点只有一个主归属;\\n检查所有目标文件, 确认新建笔记的 UUID 均已替换且在工作区内唯一, 修改现有笔记的 UUID 保持不变, 各级标题可以独立展示笔记的知识结构;\\n执行工作区已有且适用的 Markdown 检查或文档构建命令;\\n\\n当覆盖账本既无未映射项, 也无重复主归属, 文件与 UUID 检查通过, 且所有适用的工作区检查成功时, 本步骤完成.\\n"},{"path":"references/skills/to-note/template.md","content":"---\\nid: <uuid>\\n---\\n\\n# 文档主题\\n\\n问题 1; 问题 2; ...\\n\\n## 概念\\n\\n- term: 最短完整解释;\\n\\n## 代码示例\\n\\n- term: 最短完整解释;\\n\\n```text\\n最小示例\\n```\\n"},{"path":"references/skills/to-prototype/agents/openai.yaml","content":"policy:\\n  allow_implicit_invocation: false\\n"},{"path":"references/skills/to-prototype/LOGIC.md","content":"# 逻辑原型\\n\\n一个自包含的单文件 HTML (**可分享 demo**), 让任何人通过点按钮驱动一个状态模型. 当问题关乎 **业务逻辑, 状态转换或数据形状** 时使用: 这类东西纸面上看着合理, 只有推过真实用例才摸得到不对劲的地方.\\n\\n因为只是一个文件, 无需安装任何东西, 你可以把它递给非开发者 (设计师, PM, 领域专家), 让他们亲手感受这个模型. 所以它说他们的语言, 不说代码的语言.\\n\\n## 什么时候是这个形态\\n\\n- \\"我不确定这个状态机能不能处理先 X 后 Y 的边界情况.\\"\\n- \\"这个数据模型真的能表达 …… 这种情况吗?\\"\\n- \\"写 API 之前, 我想先摸一摸它应该长什么样.\\"\\n- 任何有人想 **按按钮, 看状态变** 的场合.\\n\\n如果问题是 \\"这应该长什么样\\", 走错分支了, 使用 [UI.md](UI.md).\\n\\n## 流程\\n\\n### 1. 写下问题\\n\\n写代码之前, 先写下你在为哪个状态模型, 回答什么问题做原型. 一段话, 放在 demo 顶部 (放在可见的介绍区里, 而不只是注释). 回答了错误问题的逻辑原型是纯粹的浪费, 所以把问题写明, 之后才能核对 — 无论用户是现在看着, 还是离开后再回来 (AFK).\\n\\n### 2. 把逻辑隔离进可移植模块\\n\\n把真正的逻辑 (回答问题的那部分) 放在单个 `<script>` 块里, 写成一个小而纯的模块, 之后可以原样提起, 放进真实代码库. 它周围的页面是一次性的; 这个模块不是.\\n\\n正确的形状取决于问题:\\n\\n- **纯 reducer**: `(state, action) => state`. 动作是离散事件, 状态是单个值时合适.\\n- **状态机**: 显式的状态与转换. \\"此刻哪些动作甚至合法\\" 本身是问题的一部分时合适.\\n- **一组纯函数**, 作用于普通数据类型. 没有隐式的当前状态, 只有转换时合适.\\n- **一个有清晰方法面的 class 或模块**, 当逻辑确实拥有持续的内部状态时.\\n\\n选 _最适合所问问题_ 的形状, 而不是最容易接到页面上的形状. 保持纯粹: 不碰 DOM, 不碰 `document`, 按钮 handler 不伸进它内部. 页面调用它; 反方向没有任何流动. 这让原型在自身生命周期之外仍然有用: 问题有了答案, 验证过的 reducer / 状态机 / 函数组可以自行提进真实模块.\\n\\n### 3. 构建可分享的 HTML 文件\\n\\n一个文件, 纯 HTML/CSS/JS: 无框架, 无打包器, 无服务器, 全部内联, 双击即开, 经得起邮件转来转去. 任何人打开它就能跑.\\n\\n为非开发者而写. 每个标签都用 **领域语言**, 不用代码语言: 按钮和状态读起来像业务, 不像 reducer. 用平实的话解释正在发生什么; 文案写法遵循 [UI.md](UI.md) 的 \\"设计中的写作\\" 一节: 从用户一侧命名事物, 默认主动语态, 报错说清怎么修.\\n\\n自上而下, 层次清晰地布局:\\n\\n1. **标题和一句话说明** 这个 demo 让你探索什么 (第 1 步的问题).\\n2. **当前状态**: 全部相关状态渲染成可读的面板 (带标签的字段, 不是裸 JSON dump), 每次点击后重新渲染, 让变化可见. 有助于非开发者跟随时, 点出刚刚变了什么.\\n3. **自由摸索按钮**: 每个动作一个按钮, 始终可用, 任何人可以按任意顺序戳这个模型. 每次点击 dispatch 对应动作并重新渲染状态.\\n4. **引导式走查**: 一组 **场景**, 每个页签一个. 每个页签放一段平实语言的场景描述 (它设定的处境, 要留意什么), 下面是这个场景 **要按的按钮**, 按序排列. 每一步都是真按钮: 点击即执行该动作并前进到下一步. 开始一个走查会重置到已知的初始状态, 让场景每次都以同样方式运行.\\n\\n选那些展示别扭用例的场景 — 纸面上难以推理的那些: 快乐路径, 刁钻的边界情况, 一次理应非法的尝试.\\n\\n保持美观但克制: 干净的字体排印, 大方的间距, 一个点缀色. 不要动画, 不要噱头: 任何东西都不得与状态和按钮争夺注意力.\\n\\n### 4. 交出去\\n\\n把文件发给他们, 或当面打开. 他们得空会自己点完走查和自由摸索; 最有意思的时刻是他们说 \\"等等, 这不应该是可能的\\" 或 \\"咦, 我以为 X 会不一样\\" — 那些是 _想法_ 里的 bug, 而这正是全部意义. 他们想加新动作或新场景, 就加. 原型会演化.\\n\\n### 5. 捕获答案和原型\\n\\n原型回答了它的问题之后, 捕获答案, 然后按 [SKILL](SKILL.md) 所述捕获原型. 逻辑分支的对应关系: 验证过的 reducer / 状态机 / 函数组提进真实模块 (决策被吸收); HTML 外壳随原型一起去一次性分支, 作为 primary source 保存; 因为是自包含单文件, 在那里也永远可以随手重跑.\\n\\n## 反模式\\n\\n- **不要加测试.** 需要测试的原型已经不再是原型.\\n- **不要接真实数据库.** 除非问题 specifically 关乎持久化, 一律用内存状态.\\n- **不要泛化.** 没有 \\"万一以后要支持 X\\". 原型只回答一个问题.\\n- **不要把逻辑和页面糊在一起.** 纯模块引用了 DOM, `document` 或按钮 handler, 它就不再可提取. 页面保持为纯模块上的一层薄壳.\\n- **不要动用框架, 打包器或服务器.** 收件人双击打开的一个文件; React 应用或 dev server 会杀死 \\"可分享\\".\\n- **不要把 HTML 外壳送进生产.** 页面是为手工点按优化的. 背后的逻辑模块才是值得留的部分.\\n"},{"path":"references/skills/to-prototype/SKILL.md","content":"---\\nname: to-prototype\\ndescription: 构建一次性原型代码回答一个设计问题; 当需要验证状态模型或业务逻辑的手感, 或探索 UI 应该长什么样时使用.\\nargument-hint: \\"要回答的问题是什么? (逻辑手感还是 UI 外观)\\"\\ndisable-model-invocation: true\\n---\\n\\n# 原型\\n\\n原型是 **回答一个问题的一次性代码**. 问题决定形态.\\n\\n## 选择分支\\n\\n根据用户的输入和周围代码, 判断正在回答哪一类问题; 用户在场且问题含糊时直接询问:\\n\\n- **\\"这套逻辑 / 状态模型顺手吗?\\"** → [LOGIC.md](LOGIC.md). 构建一个可分享的单文件 HTML (自由摸索按钮 + 分页签的引导式走查), 把状态机推过纸面上难以推理的用例, 非开发者也能亲手驱动.\\n- **\\"这东西应该长什么样?\\"** → [UI.md](UI.md). 在同一条路由上生成数个截然不同的 UI 变体, 通过 URL search param 和底部浮动栏切换; 每个变体按其中的设计流程做出独特而刻意的视觉方向.\\n\\n两个分支产出的工件完全不同, 选错分支就浪费整个原型. 问题确实含糊且联系不上用户时, 按周围代码默认匹配 (后端模块 → 逻辑; 页面或组件 → UI), 并在原型顶部声明该假设.\\n\\n## 两个分支共用的规则\\n\\n1. **从第一天起就是一次性的, 并明确标记.** 原型代码放在它真正服务的位置旁边 (被原型的模块或页面附近), 让上下文显而易见; 命名上要让随手翻阅的人一眼看出这是原型而非生产代码. 一次性 UI 路由遵循项目已有的路由约定, 不要发明新的顶层结构.\\n2. **运行零门槛.** UI 原型从项目任务 runner 的一条命令启动: `pnpm <name>`, `python <path>`, `bun <path>` 等. 逻辑 demo 是用户双击即开的单个 HTML 文件. 无论哪种, 启动都不需要动脑.\\n3. **默认不持久化.** 状态住在内存里. 持久化正是原型要 _检验_ 的东西, 而不是原型应该依赖的东西. 问题明确涉及数据库时, 使用 scratch DB 或命名清晰为 \\"PROTOTYPE, 随手可删\\" 的本地文件.\\n4. **不做打磨.** 不写测试, 错误处理以 _能跑_ 为限, 不做抽象. 重点是快速学到东西.\\n5. **暴露状态.** 每个动作之后 (逻辑) 或每次变体切换时 (UI), 打印或渲染出全部相关状态, 让用户看到什么变了.\\n6. **完成后捕获.** 把验证过的决策折入真实代码, 然后把原型本身作为 **primary source** 捕获: 提交到一次性分支, 不进 main, 并在实现 issue 上留下指向该分支的上下文指针. 答案 (结论和它敲定的问题) 也捕获在 issue 或 commit 里. main 分支只保留验证过的决策.\\n"},{"path":"references/skills/to-prototype/UI.md","content":"# UI 原型\\n\\n在同一条路由上生成 **数个截然不同的 UI 变体**, 通过底部浮动栏切换. 用户在浏览器里来回翻看, 选定一个 (或从每个变体里各取一部分), 然后把其余的扔掉.\\n\\n如果问题关乎逻辑/状态而非外观, 走错分支了, 使用 [LOGIC.md](LOGIC.md).\\n\\n## 什么时候是这个形态\\n\\n- \\"这个页面应该长什么样?\\"\\n- \\"定下来之前, 我想先看几个 dashboard 方案.\\"\\n- \\"给设置页试一种不同的布局.\\"\\n- 任何用户原本要在脑子里对着三个模糊 mockup 纠结一天的场合.\\n\\n## 两种子形态: 强烈偏好子形态 A\\n\\nUI 原型 **贴着应用其余部分** 时才容易判断: 真实的 header, 真实的 sidebar, 真实的数据, 真实的密度. 孤立的一次性路由是真空环境: 每个变体单独看都挺好. 只要存在可以承载变体的现成页面, 默认子形态 A; 确实没有邻近的归宿时才用子形态 B.\\n\\n### 子形态 A: 挂在既有页面上 (首选)\\n\\n路由已存在. 变体渲染在 **同一条路由** 上, 由 `?variant=` URL search param 门控. 既有的数据获取, params 和鉴权全部保留, 只替换渲染. 这是默认选项; 除非有具体理由, 都选它.\\n\\n如果原型对应的东西还没有页面, 但 _自然会长在某页面内部_ (dashboard 的新区块, 设置页的新卡片, 既有流程的新步骤), 仍然是子形态 A: 把变体挂载进宿主页面.\\n\\n### 子形态 B: 新建页面 (最后手段)\\n\\n仅当被原型化的东西确实没有可栖身的现成页面时使用 (例如全新的顶层界面, 或无法合理嵌入任何地方的流程).\\n\\n遵循项目已有的路由约定创建 **一次性路由**, 不要发明新的顶层结构. 命名上要一眼看出是原型 (例如在路径或文件名中包含 `prototype`). 同样使用 `?variant=` 模式.\\n\\n采用子形态 B 之前先 sanity check: 真的没有任何现成页面可以嵌入吗? 空路由会隐藏设计问题, 而填充了真实内容的页面会暴露它们.\\n\\n两种子形态的底部浮动栏完全相同.\\n\\n## 设计原则\\n\\n以小型工作室设计负责人的心态对待每个变体: 这家工作室以给客户做出绝不会与任何人混淆的视觉识别而闻名. 客户已经否掉过模板感的提案, 花钱买的是独特的观点: 针对这个简报, 在配色, 字体和布局上做出深思熟虑, 有主张的选择, 并承担一处能自圆其说的真实美学冒险.\\n\\n### 立足于主题\\n\\n简报没有钉死产品或主题时, 动手设计前自己先钉死: 说出一个具体的主题, 它的受众, 以及这个页面唯一的职责, 并说明你的选择. 记忆中关于用户偏好的任何信息, 他们正在构建的东西的上下文, 或你以前做过的设计, 都可以用作提示. 独特选择来自主题自己的世界: 它的材料, 工具, 造物与行话. 全程用简报的真实内容和主题素材来构建.\\n\\n### 设计准则\\n\\nWeb 设计的 hero 是一个论点. 用主题世界里最有特征的事物开场, 形式不限: 一句标题, 一张图, 一段动画, 一个 live demo, 一个互动瞬间. 选择要刻意: 大数字配小标签, 辅助统计加渐变点缀是模板答案, 只有确实最优时才用.\\n\\n字体承载页面的个性. display 与 body 字体的搭配要刻意, 而不是任何项目都会伸手用的那几套; 设定清晰的字阶 (type scale), 字重, 字宽和间距都要有意图. 让字体处理本身成为设计中令人记住的部分, 而不是内容的中性运输工具.\\n\\n结构即信息. 结构性手段 — 编号, eyebrow, 分隔线, 标签 — 应编码内容里真实存在的东西, 而不是装饰. 许多泛泛的设计使用编号标记 (01 / 02 / 03), 但只有内容确实是序列时才合适, 例如真实的流程, 或顺序承载着读者所需信息的时间线. 采用编号标记这类选择之前, 先质疑它是否真的成立.\\n\\n刻意地使用动效. 想清楚动效在哪里, 以及是否服务于主题: 页面加载序列, 滚动触发的揭示, 悬停微交互, 环境氛围. 一个编排过的瞬间通常比散落的特效更有力; 按方向所需选择. 但有时少即是多, 过多的动画会贡献 \\"这设计是 AI 生成的\\" 的感觉.\\n\\n让复杂度匹配愿景. 极繁方向需要繁复的执行; 极简方向需要在间距, 字体和细节上的精确. 优雅就是把选定的愿景执行好.\\n\\n认真对待文案. 设计简报常常不包含真实内容, 文案要你来写. 文案可以让设计感觉和设计本身一样模板化. 详见下文 \\"设计中的写作\\".\\n\\n## 流程\\n\\n### 1. 写下问题, 定下 N\\n\\n默认 **3 个变体**. 超过 5 个就不再截然不同, 开始变成噪音, 以此为上限.\\n\\n在原型所在位置或文件顶部注释里, 用一行写下计划:\\n\\n> \\"设置页的三个变体, 在既有 `/settings` 路由上通过 `?variant=` 切换.\\"\\n\\n无论用户是否在场, 这行计划都能让他随时反驳.\\n\\n### 2. 为每个变体构思设计计划\\n\\n先校准: 当前 AI 生成的设计聚集在三种长相上 — (1) 暖奶油色背景 (接近 #F4F1EA) 配高对比衬线 display 字体和赤陶色点缀; (2) 近黑背景配单一的酸性绿或朱红亮色点缀; (3) 大报 (broadsheet) 风格版式, 发丝级细分隔线, 零圆角, 报纸般的密集多栏. 三者对某些简报都合法, 但它们是默认值而非选择, 而且不分主题地出现. 简报钉死了视觉方向时, 严格照做 — 简报自己的话永远优先, 包括它点名要这三种长相之一时. 简报留白的轴, 不要花在默认值上. 就像被雇佣的人类设计师, 做擅长的事和把每个项目当作实验与学习的机会之间, 常常需要仔细平衡.\\n\\n分两步走. 第一步, 基于简报为每个变体构思一份简短的设计计划 — 一套紧凑的 token 系统, 含色彩, 字体, 布局和签名元素:\\n\\n- **色彩**: 4–6 个命名 hex 值描述的调色板.\\n- **字体**: 2 个以上角色的字体 (有性格的 display 字体, 克制地使用; 互补的 body 字体; 需要时再为 caption 或数据配一个 utility 字体).\\n- **布局**: 一个布局概念, 用一句话的散文描述和 ASCII 线框图来构思和比较.\\n- **签名元素**: 这个页面将被记住的唯一独特元素, 以恰当的方式体现简报.\\n\\n构建之前, 把计划对照简报再审一遍: 任何部分读起来像你为任何类似页面都会产出的通用默认值时 (可以拿一个类似 prompt 走一遍, 看是否殊途同归), 而不是为这个具体简报做的选择 — 修订它, 并说明改了什么, 为什么. 确认设计计划的相对独特性之后, 才开始写代码.\\n\\n尽量把构思和迭代放在思考里完成, 只在有较高把握能取悦用户时才把想法亮出来.\\n\\n### 3. 生成截然不同的变体\\n\\n严格按修订后的设计计划实现每个变体, 每个颜色和字体决策都从计划中推导. 每个变体要对得起:\\n\\n- 页面的目的和它能访问的数据;\\n- 项目的组件库 / 样式体系 (TailwindCSS, shadcn, MUI, 纯 CSS, 无论什么);\\n- 清晰的导出组件名, 例如 `VariantA`, `VariantB`, `VariantC`.\\n\\n变体之间必须 **结构上不同**: 不同的布局, 不同的信息层级, 不同的主要可供性 (affordance), 而不只是颜色不同. 三个微调的卡片网格不是 UI 原型, 是壁纸. 两个草稿太像时, 带着明确的 \\"不许用卡片网格\\" 式约束重做一个.\\n\\n写代码时注意 CSS selector 的优先级 (specificity) 结构. 很容易生成互相抵消的 CSS class (尤其是 `.section` 这类按类型命中的选择器和 `.cta` 这类按元素命中的选择器叠加时), 在 section 之间的 padding/margin 上经常发生.\\n\\n### 4. 接在一起\\n\\n在路由上创建一个切换组件:\\n\\n```tsx\\n// 伪代码, 按项目框架适配\\nconst variant = searchParams.get(\\"variant\\") ?? \\"A\\";\\nreturn (\\n  <>\\n    {variant === \\"A\\" && <VariantA {...data} />}\\n    {variant === \\"B\\" && <VariantB {...data} />}\\n    {variant === \\"C\\" && <VariantC {...data} />}\\n    <PrototypeSwitcher variants={[\\"A\\", \\"B\\", \\"C\\"]} current={variant} />\\n  </>\\n);\\n```\\n\\n子形态 A (既有页面): 把既有的数据获取全部保留在切换逻辑之上, 只有渲染的子树随变体变化.\\n\\n子形态 B (新建页面): `/prototype/<name>` 下的一次性路由挂载同一个切换器.\\n\\n### 5. 构建浮动切换栏\\n\\n固定在屏幕底部中央的小条, 由三部分组成:\\n\\n- **左箭头**: 循环到上一个变体 (回绕).\\n- **变体标签**: 显示当前变体 key; 变体导出了名字时也显示名字, 例如 `B (Sidebar 布局)`.\\n- **右箭头**: 循环到下一个变体 (回绕).\\n\\n行为:\\n\\n- 点击箭头更新 URL search param (使用框架的 router, 例如 Next 的 `router.replace`, React Router 的 `navigate` 等), 让变体可分享, 刷新后稳定.\\n- 键盘: `←` 和 `→` 方向键同样切换. 焦点在 `<input>`, `<textarea>` 或 `[contenteditable]` 上时不要拦截方向键.\\n- 视觉上与页面区分开 (例如高对比度的胶囊形加柔和阴影), 让人一眼看出它不属于正在被评估的设计.\\n- 在生产构建中隐藏: 用 `process.env.NODE_ENV !== \'production\'` 或等价检查门控, 防止误并原型把切换栏发给用户.\\n\\n切换栏做成单一共享组件, 让两种子形态复用; 放在项目里共享 UI 所在的位置.\\n\\n### 6. 交出去\\n\\n把 URL (以及 `?variant=` 的各个 key) 亮出来. 用户得空就会翻看. 最有意思的反馈通常是 **\\"我想要 B 的 header 配 C 的 sidebar\\"**, 那才是他们真正想要的设计.\\n\\n### 7. 捕获答案并清理\\n\\n某个变体胜出后, 捕获答案 (哪个变体, 为什么), 然后按 [SKILL](SKILL.md) 所述捕获原型. 把胜者折入真实代码, 其余的移到一次性分支, 不进 main:\\n\\n- **子形态 A**: 胜者折入既有页面; 落选变体和切换栏从 main 移除.\\n- **子形态 B**: 胜出变体提升为正式路由; 一次性路由和切换栏从 main 移除.\\n\\n整套变体是 primary source, 所以它落在一次性分支而不是垃圾桶: 留在 main 分支里的变体组件和切换栏腐烂得很快, 还会迷惑下一个读者.\\n\\n## 克制与自我批判\\n\\n把大胆花在一个地方. 让签名元素成为唯一令人记住的东西, 它周围的一切保持安静和克制, 砍掉不服务于简报的装饰. 不冒险本身也是一种风险. 默默达到质量下限而不声张: 响应式直到移动端, 可见的键盘焦点, 尊重 reduced motion. 构建过程中持续批判自己的作品, 环境支持就截图 — 一图抵千 token. 想想 Chanel 的建议: 出门前照照镜子, 摘掉一件配饰. 人类创作者有记忆, 总是尝试新东西; 如果有可以快速记下尝试过什么的地方, 它会在后面的迭代里帮到你.\\n\\n## 设计中的写作\\n\\n文字出现在设计里只为一个原因: 让它更容易被理解, 因而更容易被使用. 文字是设计材料, 不是装饰. 对文案投入与间距和色彩同等的刻意. 动笔之前先问: 这个设计需要说什么, 怎样说才最有助于使用者 navigate 整个体验.\\n\\n从终端用户的屏幕一侧来写. 用人们控制和认知的名字称呼事物, 绝不用系统的构建方式. 人管理的是通知, 不是 webhook 配置. 用平实的话说清某件事做什么, 而不是推销它. 具体永远好过抖机灵.\\n\\n默认使用主动语态. 一个控件要确切说出使用后会发生什么: \\"保存更改\\", 而不是 \\"提交\\". 一个动作在整条流程里保持同一个名字, 写着 \\"发布\\" 的按钮, 产出的 toast 就写 \\"已发布\\". 界面的词汇表是人们在产品里认路的路标, 连贯与一致是人们学会认路的方式.\\n\\n把失败和空态当作给出方向的时刻, 而非渲染情绪. 用界面自己的声音解释出了什么错, 怎么修. 报错不道歉, 也绝不含糊发生了什么. 空屏是一份行动邀请.\\n\\n保持对话式的语域并调好音: 平实的动词, sentence case, 无填充词, 语气匹配品牌与受众. 让每个元素只做一件事: 标签负责标注, 示例负责演示, 没有任何东西悄悄身兼二职.\\n\\n## 反模式\\n\\n- **变体只在颜色或文案上有差异.** 那是微调, 不是原型. 真正的变体在结构上有分歧.\\n- **变体之间共享太多代码.** 共享一个 `<Header>` 没问题; 共享 `<Layout>` 就违背了初衷. 每个变体都应该有扔掉整个布局的自由.\\n- **把变体接到真实 mutation 上.** 只读原型就够了. 变体确实需要变更数据时, 指向 stub: 问题是 \\"这应该长什么样\\", 不是 \\"后端能不能工作\\".\\n- **把原型直接提拔进生产.** 变体代码是在原型约束下写的 (无测试, 错误处理从简). 折入时正式重写.\\n"},{"path":"references/skills/to-story/SKILL.md","content":"---\\nname: to-story\\ndescription: 打磨模糊想法为有序、可验收的用户故事集；当角色、期望收益或用户结果尚不足以安全拆分为 issues 时使用。\\nargument-hint: \\"要把什么想法打磨成用户故事?\\"\\n---\\n\\n# To Story\\n\\n用户故事使用 **3C**：`story.md` 是 card，分轮访谈形成 conversation，可判定验收形成 confirmation。\\n\\n## 1. 定位 Plan\\n\\n完整读取 `<nano-flow-skill-root-dir>/references/DOMAIN.md`，从 `CONTEXT-MAP.md` 定位 owner 域并读取相关 `CONTEXT.md`、ADR 和已有 Plan。新 Plan 创建于：\\n\\n```text\\ndocs/{domain-name}/plans/active/YYYY-MM-DD-中文工作名/story.md\\n```\\n\\n读取 [`TEMPLATE.md`](TEMPLATE.md) 后创建或载入 `story.md`。访谈中的候选决策只进入该文件；领域术语或 ADR 要到 `/quest-with-domain` 获得确认后才改变。\\n\\nowner、Plan 路径、原始想法和所有相关现有产物已确定时，本步骤完成。\\n\\n## 2. 拷问故事地图\\n\\n持续访谈直到与用户达成共同理解。把角色、用户结果、收益、边界、验收与未知项映射为 **design tree**：每个待定决策是一个节点，依赖它的决策位于下游；**frontier** 是前置均已确定、当前无需猜测即可回答的全部节点。\\n\\n按 **rounds** 推进 design tree。每轮询问整个 frontier：问题依次编号，提供 agent 的推荐答案与关键理由，然后等待用户回答。依赖本轮未决答案的问题留到下一轮。使用以下格式：\\n\\n```markdown\\n❓ **Q1 - {问题标题}**：{问题；需要时列出互斥选择}\\n\\n➡️ **建议**：{推荐答案与关键理由}\\n\\n❓ **Q2 - {问题标题}**：{问题；需要时列出互斥选择}\\n\\n➡️ **建议**：{推荐答案与关键理由}\\n```\\n\\n用户的答案会重塑 design tree：用已确定决策推进 frontier，补入新暴露的下游节点，再重新计算下一轮。事实由 agent 通过工作区与工具查明，不把可检索问题交给用户；需要继续探索时委派 subagent，并把进行中的探索视为未决前置，只暂停其下游，当前 frontier 的其余问题照常询问。角色、收益、边界与验收等判断由用户决定并等待其回答。\\n\\n用户确认共同理解前，工作限于取证、访谈与维护 `story.md`；确认后才闭合故事集。\\n\\n每轮把已确认内容就地写入 `story.md`：\\n\\n- 角色进入「角色」。\\n- 完整用户结果进入「故事」，属于同一较大结果时归入 epic，否则保持独立故事。\\n- 新空白进入「迷雾」，澄清后迁移到权威章节。\\n- 使用过的 PRD、spec、ADR、commit 或 diff 进入「上下文」。\\n\\ndesign tree 的每个可达分支都已访问，frontier 为空且没有静默假设；agent 已复述最终故事地图，用户明确确认达成共同理解时，本步骤完成。\\n\\n## 3. 闭合故事集\\n\\n逐项核对原始想法与故事地图：每条故事有唯一且单调递增的 `US-NNN`、明确角色与收益、至少一项可判定验收；迷雾为空或每个保留项都获用户明确接受；每个原始意图都恰好映射到故事或已接受的非范围。\\n\\n从工作区根执行 `node <nano-flow-skill-root-dir>/scripts/check-domain.mjs .`。校验通过且用户确认故事集覆盖原始想法后，本步骤完成。\\n"},{"path":"references/skills/to-story/TEMPLATE.md","content":"# Story 模板\\n\\n属于 epic 的故事使用四级标题；独立故事使用三级标题，不创建空壳 epic。\\n\\n```markdown\\n# {工作名}\\n\\n## 原始想法\\n\\n{用户的最初表述，保留原话}\\n\\n## 角色\\n\\n- **{角色名}**：{是谁、处境与工作方式}\\n\\n## 故事\\n\\n### {Epic 标题}\\n\\n{这组故事共同完成什么}\\n\\n#### US-001 {故事标题}\\n\\n作为 {角色}，我 {想要}，以便 {收益}。\\n\\n- [ ] {可判定的验收结果}\\n\\n### US-002 {独立故事标题}\\n\\n作为 {角色}，我 {想要}，以便 {收益}。\\n\\n- [ ] {可判定的验收结果}\\n\\n## 迷雾\\n\\n{尚未澄清的空白；澄清后迁移到权威章节}\\n\\n## 上下文\\n\\n{通过路径或 URL 引用生成故事时使用的现有产物；域内文档使用相对路径}\\n```\\n\\n模板是最小集合。新增章节必须同时满足：内容无法归入现有章节；缺少它会使执行或验收丢失关键信息。章节标题使用中文。\\n"},{"path":"references/skills/to-workflow/agents/openai.yaml","content":"policy:\\n  allow_implicit_invocation: false\\n"},{"path":"references/skills/to-workflow/SKILL.md","content":"---\\nname: to-workflow\\ndescription: 将一个反复出现的循环固化为可运行的工作流.\\nargument-hint: \\"要把哪个反复出现的循环转化为工作流?\\"\\ndisable-model-invocation: true\\n---\\n\\n# 固化工作流\\n\\n把重复活动定界为一个**循环**, 运行 `/questing` 清空决策前沿, 将其**工作流**固化到拥有该循环的 `.agents/skills/{domain-flow}` skill. 使用 `/writing-for-agents` 编写工作流.\\n\\n## 词汇\\n\\n- **循环** - 反复出现且有可辨认起止边界的活动.\\n- **工作流** - 指导循环每次运行的规格; 可以规定固定步骤, 或提供用于现场派生步骤的上下文, 或混合两者.\\n- **运行契约** - 工作流的触发条件, 输入, 前置条件, 委派边界, 结束边界, 输出和完成证据.\\n- **委派边界** - 自动执行与用户确认的分界; 在授权, 风险和可逆性允许的范围内尽可能右移.\\n- **简报** - 到达委派边界时交给用户的可直接决策摘要, 包含现场事实, 证据, 未决选项和推荐答案.\\n\\n## 步骤\\n\\n1. **取证** 读取 `/nano-flow` 的 `<nano-flow-skill-root-dir>/references/DOMAIN.md` 和工作区根 `CONTEXT-MAP.md`, 定位所触及业务域, 再读取它们的 `CONTEXT.md`, 相关 ADR, 相邻工作流; 检查当前工作中足以证明重复模式的事实. 在领域语言, 既有约束, 相邻工作流和重复证据均已覆盖, 且现场可检索的事实缺口已清空时完成.\\n2. **定界** 从用户指出及证据揭示的候选项中, 提出潜在循环, 让用户选择一个并确定拥有它的业务域. 跨域输入输出作为契约引用, 不复制工作流. 在能够明确说出它的 owner, 触发, 起点, 终点和重复结果, 且用户确认选择时完成.\\n3. **拷问** 以所选循环为根运行 `/questing`; 清空工作流形态, 运行契约, 委派边界及简报, 失败恢复和推荐技能的决策前沿. 在前沿为空且用户确认达成共同理解时完成.\\n4. **落盘** 按已确认决策在 `NNNN-{workflow}/` 中创建, 修订, 合并或删除工作流, 并同步该 domain-flow skill 的 `SKILL.md` 路径-触发条件索引; 每个含义只写在一个权威位置. 不存在对应 domain-flow skill 时, 同时创建其目录与 `SKILL.md`. 在每项决策恰好落到一个现场文件位置, 且没有其他文件重复规格化该循环时完成.\\n5. **读回** 重新读取现场文件, 逐项核对运行契约, 委派边界及简报, 适用的条件分支与失败恢复, 推荐技能, 索引与文件的一致性, 既有文档冲突和下列约束. 在全部决策与约束均有核对结果, 冲突已显式指出, 且已向用户报告文件路径与完成证据时完成.\\n\\n## 输出约束\\n\\n- 一个工作流文件只规格化一个循环; 结构由循环决定.\\n- 触发条件只写在所属 domain-flow skill 的 `SKILL.md` 路径-触发条件索引中, 不在工作流文件内重复.\\n- 工作流包含 `推荐技能` 章节, 说明运行时推荐调用的技能及其时机.\\n- 文件名与正文使用中文; 与 ADR 或既有工作流冲突时显式指出, 不静默覆盖.\\n- 工作流的目录结构如下\\n\\n```text\\n├── CONTEXT-MAP.md\\n├── .agents/skills/{domain-flow}\\n│  ├── SKILL.md\\n│  └── NNNN-{workflow}/\\n│      ├── NNNN-{workflow}.md\\n│      └── ...\\n└── ...\\n```\\n"},{"path":"references/skills/verifying/SKILL.md","content":"---\\nname: verifying\\ndescription: 验证交付主张并形成可复核证据链；当变更需要以门禁证据判定 passed/failed/blocked 结论时使用。\\n---\\n\\n# Verifying\\n\\n交付验证使用一条**证据链**: 交付主张 → 适用门禁 → 实际结果 → 结论. 门禁失败时进入**最小循环**: 失败 → 证据定位 → 最小修复 → 重验.\\n\\n携带 `/dev-gate` context 时, 以已确认的质量门禁作为最小集合, 复用 `/code-test` 仍适用于当前 diff 的证据, 再执行集合中尚未取得可信证据的门禁.\\n\\n## 证据链\\n\\n1. **明确交付主张**: 确定本次要证明的变更, 受影响单元, 外部契约, 消费者和目标环境. 完成标准: 每项交付主张都映射到待验证的影响面, 未知与不在范围内的部分已明确.\\n2. **选择门禁**: 从工作区声明的一系列门禁(例如编程领域的格式检查, 静态检查, 测试, 构建, 运行态确认和消费者路径)中, 选择足以支撑交付主张的最小集合; 已有证据仅在目标, 版本, 配置和前置仍适用时复用. 完成标准: 每项门禁都有事实来源, 执行入口和通过条件.\\n3. **执行门禁**: 按依赖顺序从最早缺少可信证据的门禁开始;完成标准: 每项已选门禁都有实际结果, 仍适用的可信证据或明确的阻塞前置.\\n4. **运行最小循环**: 门禁失败后, 从最早失败门禁取得足够证据, 在任务授权范围内做证据指向的最小修复, 再从最早受影响门禁重验; 仍失败时以新证据进入下一轮. 完成标准: 每轮失败, 修复和重验一一对应, 受影响证据链重新通过或形成明确的 blocked 结论.\\n5. **闭合结论**: 先给交付结论, 再记录各门禁的结果, 关键证据, 未验证范围和剩余风险. 完成标准: 结论限于证据覆盖范围, 另一执行者能够复现失败或阻塞项.\\n\\n## 结论门禁\\n\\n- `passed`: 范围内适用门禁均已实际通过, 或有仍适用于当前目标的可信通过证据; 发生修复时, 只使用修复后的重验结果.\\n- `failed`: 必要前置已满足, 门禁实际结果与交付主张矛盾.\\n- `blocked`: 必要门禁因无法满足的前置而不能得到结果; 记录缺失条件和恢复入口.\\n- 局部门禁通过只支持对应范围的结论; 未验证部分保持显式.\\n"},{"path":"references/skills/writing-for-agents/SKILL-MECHANICS.md","content":"# Skill mechanics\\n\\n[`writing-for-agents`](SKILL.md) 中 skill 特有的 branch: 当文档是 skill 时会发生哪些变化, 包括 frontmatter, invocation choice 和 router skills. 其他所有写法都使用 `SKILL.md` 中的通用 reference.\\n\\n## Invocation\\n\\n两种选择, 在 two loads 之间进行权衡:\\n\\n- **model-invoked** skill 保留 `description`, 因此 agent 可以自主触发它, 其他 skills 也能到达它. 你仍然可以输入它的名称: model-invocation 始终_包含_ user reach. description 只会增加 agent discovery, 从不移除人类入口. description 是 skill 的 top-level context pointer, 被迫始终保持加载. 以永久 context load 换取 discoverability. 内容全是 reference 的 model-invoked skill 也可以作为 shared reference 的一个归属地: 其他 skill 可以调用它, 因此多个 skills 所需的 reference 可以放在一处. 机制: 省略 `disable-model-invocation`, 并编写面向 model, 携带 trigger branches 的 description(`SKILL.md` 中的 pointer-writing rules 完全适用).\\n- **user-invoked** skill 从 agent 的可达范围中移除 description: 只有输入其名称的人类可以调用它, 其他 skill 都无法调用. context load 为零, 但会消耗 cognitive load. 你就是必须记住它存在的索引. 机制: 设置 `disable-model-invocation: true`. `description` 变为面向人类的一行摘要, 移除 trigger lists.\\n\\n只有当 agent 必须自行到达该 skill, 或另一个 skill 必须到达它时, 才选择 model-invocation. 如果它始终只能由人手动触发, 将其设为 user-invoked, 不付出 context load.\\n\\n两个 user-invoked skills 都需要的 shared reference 不能位于其中任何一个内部. 没有 descriptions, 二者都无法触发对方. 将它推到 skill system 外部的普通文件中, 成为任何 skill 都能指向的 external reference.\\n\\n## Splitting by invocation\\n\\nsplitting 的 invocation cut(sequence cut 位于 `SKILL.md` 中): 如果存在应该独立触发 skill 的 distinct leading word, 即你确实会在 prompts 中使用的 trigger word, 或者另一个 skill 必须到达它, 则拆出 model-invoked skill. 你会为新的 always-loaded description 付出 context load, 因此这种独立可达性必须物有所值.\\n\\n## Router skills\\n\\n当 user-invoked skills 增长到超出你的记忆能力时, 使用 **router skill** 治疗堆积的 cognitive load: 一个 user-invoked skill 为其他 skills 命名, 并说明何时到达每一个 skill, 让人类只需记住一个 skill, 而不是多个. 它只能提示, 绝不能触发它们: user-invoked skills 没有 description, 因此除人类外, 没有任何事物能到达它们.\\n"},{"path":"references/skills/writing-for-agents/SKILL.md","content":"---\\nname: writing-for-agents\\ndescription: 优化 agent 指令文档的 context pointers、信息层级、完成标准与 pruning；当创建或编辑 skill、AGENTS.md、CLAUDE.md 或其引用文档时使用。\\n---\\n\\n编写任何供 agent 使用的文档时所参考的资料, 包括 skill, `AGENTS.md` / `CLAUDE.md`, 以及通过 pointer 到达的文档. 它们的包装不同, 写法却相同: 同一组杠杆让每一种文档都可预测, 让 agent 每次运行时采用相同的_流程_, 而不是产出相同的结果.\\n\\n当你编写的文档是 skill 时, 阅读 [`SKILL-MECHANICS.md`](SKILL-MECHANICS.md), 了解 frontmatter, invocation choice 和 router skills.\\n\\n## Context pointers\\n\\n**context pointer** 是 agent context 中的一项 reference. 它为某些 context 之外的材料命名, 并编码到达这些材料的条件. skill 的 description 是 context pointer. `AGENTS.md` 中为某份文档命名的一行也是同一种对象. 决定 agent 何时以及多可靠地到达材料的是 pointer 的_措辞_, 而不是它的目标. 必需的目标如果藏在措辞薄弱的 pointer 后面, 就是 variance bug: 先打磨措辞, 只有打磨失败时才将材料 inline.\\n\\npointer 做两件事: 说明材料是什么, 列出应该触发到达该材料的 **branches**(branch 是文档处理的一种独立情况, 因此不同运行会在其中采用不同路径). 始终加载的 pointer 中的每个词都会在每一轮产生开销, 因此它比正文更需要严格 pruning:\\n\\n- **Front-load the leading word** - pointer 正是在这里发挥触发作用.\\n- **One trigger per branch.** 为同一个 branch 换名的同义词, 只是将一个 branch 写了两遍. 将它们合并, 只保留真正不同的 branches.\\n- **删除正文已经承载的身份信息.**\\n\\n## The two loads\\n\\n你添加的每份文档和每个 pointer 都会消耗以下两种预算之一:\\n\\n- **Context load** - 始终加载的材料在 agent window 上产生的成本: `AGENTS.md` 中的一行, skill description, 任何每轮都位于 context 中的内容. 无论是否触发, 它们都会消耗 tokens 和注意力.\\n- **Cognitive load** - 人类承担的成本: 存在哪些文档, 何时应该使用哪一份. 人类就是索引. 这不是要最小化的成本, 而是人的自主性所需的代价. 在 human judgement 重要的地方投入它, 在不重要的地方消除它.\\n\\n只能通过 pointer 到达的材料, 以 pointer 自身那一行为代价避开 context load. 完全没有 pointer 的材料, 则完全依赖 cognitive load.\\n\\n## Information hierarchy\\n\\n文档由两种可自由混合的内容类型构成: **steps**(agent 按顺序执行的动作)和 **reference**(按需查阅的定义, 规则, 事实). 它可以全部由 steps 构成(一份操作步骤), 全部由 reference 构成(review 的规则, 本 skill), 也可以同时包含两者. 核心决策是每项内容位于 **information hierarchy** 的哪一层. 这架 ladder 按 agent 对材料的即时需要程度排序:\\n\\n1. **In-file step** - 主要层级: agent 按顺序执行的内容.\\n2. **In-file reference** - 按需查阅. 它通常是合理的扁平同级集合(review 的每条规则位于同一 rung), 这是一种良好安排, 不是 smell.\\n3. **Disclosed reference** - 被推入独立文件, 通过 context pointer 到达, 仅在 pointer 触发时加载. 它既可以是同一文件夹中的同级文件, 也可以是位于任何地方, 可被任何文档指向的完全 external reference.\\n\\n向下推得太少, 顶层就会膨胀. 向下推得太多, 就会隐藏 agent 真正需要的材料. 这种张力就是决策的全部.\\n\\n**Progressive disclosure** 是沿 ladder 向下移动, 离开主文件并进入 pointer 之后, 从而保持顶层清晰. 它主要不是 token 优化, 而是保护 hierarchy 的方式. Branching 是最清晰的 disclosure test: 将每个 branch 都需要的内容 inline, 将只有部分 branches 会到达的内容推到 pointer 后面. 当文档包含 steps 时, 本应 disclosed 的 in-file reference 会埋没它们, 使 agent 是否注意到它们变成抛硬币. 这是一个 variance lever, 而不只是可读性杠杆.\\n\\n**Co-location** 是文件内部的配套原则: ladder 决定一项内容_向下放多远_, co-location 决定它到达那里后_与什么放在一起_. 将一个 concept 的 definition, rules 和 caveats 放在同一个标题下, 而不是分散各处, 让读者读取一部分时也会带上它的邻居. 判断方式: 文档读起来应该像为 agent 编写的 documentation. 成组的材料如此, 分散的材料则不然.(它与 duplication 不同: duplication 在两处重复同一个含义, scattering 则将一个含义拆散到多处.)\\n\\n**Sprawl** 是这里的 failure mode: 即使每一行都有效且独一无二, 文档仍然过长. 注意力会被过量内容稀释, 每增加一行, 就多一行需要保持 relevance. 解决方法是 ladder: 将 reference 披露到 pointers 后面, 并按 branch 或 sequence 拆分, 让每条路径只携带自身需要的内容.\\n\\n## Steps and completion criteria\\n\\n每个 step 都以 **completion criterion** 结束, 它是告诉 agent 工作已经完成的条件. 两个属性使其成为杠杆:\\n\\n- **Clarity** - agent 能否区分 done 与 not-done? 模糊的 bound(\\"已经理解\\")会诱发 **premature completion**: step 尚未真正完成便结束, 注意力滑向_完成这件事_. 前方仍然可见的 steps, 即 **post-completion steps**, 提供拉力. criterion 的 clarity 提供阻力. 按顺序防守: **sharpen the bound first**(局部且便宜). 只有当它无法摆脱模糊, _并且_你观察到赶进度时, 才通过拆分 sequence 隐藏后续 steps. 隐藏只有跨越真实 context boundary 时才有效(hand-off 或 subagent dispatch). inline call 会让后续 steps 留在 context 中, 什么也没有清除.\\n- **Demand** - 它要求多少. \\"每个修改过的 model 都已核对\\"会迫使 agent 做彻底的工作, 而\\"生成变更清单\\"不会. Demand 驱动 **legwork**, 即 agent 在工作中进行的挖掘. 它潜藏在措辞中, 而不是被写成独立 step. Demand 也不受 step 限制: \\"每条规则都已应用\\"约束一组扁平 reference, 正如\\"每个 step 都已完成\\"约束一个 sequence. 因此, 完全由 reference 构成的文档仍能带有穷尽性门槛.\\n\\n最强的 criteria 既可检查, 又穷尽无遗.\\n\\n## When to split\\n\\n将一份文档拆成两份会消耗 two loads 中的一种, 因此只有切分物有所值时才拆分:\\n\\n- **By sequence** - 当 post-completion steps 诱使 agent 匆忙完成前方 step 时, 拆分这一串 steps. 不让它们出现在视野中, 会驱动 agent 为当前任务投入更多 legwork. 警惕反向操作: 合并 sequences 会让每个 step 的后续 steps 暴露给前面的内容, 从而诱发 premature completion.\\n- **By invocation** - skill 特有的拆分方式: 参见 [`SKILL-MECHANICS.md`](SKILL-MECHANICS.md).\\n\\n## Leading words\\n\\n**leading word** 是一个已经存在于 model pretraining 中的紧凑概念, agent 在运行文档时用它思考(_lesson_, _fog of war_, _tracer bullets_). 将它作为 token 重复, 而不是作为句子重复, 它就会积累分布式定义, 并通过调动 model 已有的 priors, 用最少的 tokens 锚定整个行为区域. 如果定义清晰, 自创新词也能奏效, 但虚构词无法调动 priors. pretrained word 免费提供的内容, 你必须用定义所需的 tokens 补回. 优先寻找现有词语.\\n\\n它锚定两次. 在正文中锚定_执行_: 每当该词出现, agent 都会采用相同行为. 在扁平 reference 中, 它将注意力聚焦到一类需要寻找的事物上. 在 pointer 中锚定_invocation_: 当同一个词出现在 prompts, docs 和 codebase 中时, agent 会将这种 shared language 与材料关联起来, 从而更可靠地到达材料.\\n\\n寻找使用 leading words 进行 refactor 的机会. 在三个位置逐项写出的 triad, 用一个句子指向一种想法的 pointer, 每一处都在恳求被压缩成单个 token:\\n\\n- \\"快速, 确定, 低开销\\" → _tight_(一个 _tight_ loop).\\n- \\"一个你信得过的 loop\\" → _red_. 模糊门禁变成二元可观察状态(loop 在 bug 上变成 _red_, 或者没有).\\n\\n你赢了两次: tokens 更少, agent 用来挂住思考的钩子更锋利. 假设每份文档都带有可以被 leading words 淘汰的重复表述, 去找出它们.\\n\\n**Negation** 是此杠杆旁边的 failure mode: 通过禁止来引导, 会将被禁止的行为拉入 context, 使它变得_更加_可用, 而不是更少. _不要想一头大象_, 结果满脑子都是大象. negation 是一个弱修饰语, 会被强激活的概念淹没, 所以禁令有一半读起来像执行该行为的 instruction. Prompt **positive**: 陈述目标行为(\\"编写单行 comments\\"), 让被禁止的行为根本不被说出. 只有无法用 positive 方式表达的 hard guardrail, 才值得使用 prohibition. 即使如此, 也要将它与 positive target 配对, 让注意力落在应该做什么上.\\n\\n## Pruning\\n\\n- 将每个含义保存在一个 **single source of truth** 中: 一个权威位置, 使行为变更成为单点编辑. **Duplication** 是在多个位置表达同一个含义, 它会消耗维护成本和 tokens, 并将某个含义在 ladder 上的重要性膨胀到超过它的真实层级.(它是 leading word 的意外反面. leading word 刻意重复 token, 绝不重复含义.)\\n- **environment** 也是 source of truth, 例如 `package.json` scripts, config files, directory layout, `--help` output. 重述这些内容的文档是 **cache**: lookup 的副本. 只有 lookup 成本高昂时, 它的 load 才物有所值. Cache agent 无法通过查找获得的内容: 未记录的约定, 选择背后的原因, config 不会坦白的陷阱. 将单文件, 单命令即可完成的 lookups 留给 environment, 它们在那里不会过时.\\n- 检查每一行的 **relevance**: 它是否仍然关系到文档所做的事情? 一行内容会因为从未关系到任务(纯粹说明, 或本应 disclosed 的 branch), 或随着它描述的行为或世界变化而过时, 从而失去 relevance. 较短的文档更容易保持 relevance. 如果没有 pruning discipline, 默认结局就是 **sediment**: 因为添加令人安心, 删除令人不安, 过时层不断沉积, 直到你必须像钻取岩芯一样穿过它们, 才能找到仍然有效的内容.\\n- 逐句寻找 **no-ops**: model 默认已经遵守的 instruction 会付出 load, 却什么也没说. 判断方式是, 相较于默认行为, 它是否改变行为? 这个判断与 model 相关, 而不是与读者相关. 两个人对某个 no-op 持不同意见, 是因为他们对默认行为持不同意见. 应通过运行文档解决, 而不是通过辩论解决. 如果句子未通过判断, 删除整句, 而不是削减其中的词. 该判断也会评价 leading words: 太弱而无法战胜默认行为的词(agent 已经相当 thorough 时使用 _be thorough_)就是 no-op. 解决方法是使用更强的词(_relentless_), 而不是换一种技巧.\\n"},{"path":"scripts/check-domain.mjs","content":"#!/usr/bin/env node\\n\\nimport fs from \\"node:fs\\";\\nimport path from \\"node:path\\";\\nimport { fileURLToPath } from \\"node:url\\";\\n\\nimport {\\n  deriveSpecStatus,\\n  ISSUE_STATUSES,\\n  parseFrontmatter as parsePlanFrontmatter,\\n  parseIssueDependencies,\\n} from \\"./plan-document.mjs\\";\\n\\nconst LIFECYCLES = new Set([\\"active\\", \\"reference\\", \\"archived\\"]);\\nconst SPEC_STATUSES = new Set([\\"pending\\", \\"in_progress\\", \\"completed\\"]);\\nconst HAN_PATTERN = /\\\\p{Script=Han}/u;\\n\\nconst relativePath = (rootDir, targetPath) =>\\n  path.relative(rootDir, targetPath).replaceAll(\\"\\\\\\\\\\", \\"/\\");\\n\\nconst addError = (errors, rootDir, targetPath, message) => {\\n  errors.push(`${relativePath(rootDir, targetPath)}: ${message}`);\\n};\\n\\nconst readText = (targetPath, errors, rootDir) => {\\n  try {\\n    return fs.readFileSync(targetPath, \\"utf8\\");\\n  } catch (error) {\\n    addError(\\n      errors,\\n      rootDir,\\n      targetPath,\\n      `读取失败: ${error instanceof Error ? error.message : String(error)}`,\\n    );\\n    return null;\\n  }\\n};\\n\\nconst readEntries = (targetPath, errors, rootDir) => {\\n  try {\\n    return fs.readdirSync(targetPath, { withFileTypes: true });\\n  } catch (error) {\\n    addError(\\n      errors,\\n      rootDir,\\n      targetPath,\\n      `枚举失败: ${error instanceof Error ? error.message : String(error)}`,\\n    );\\n    return [];\\n  }\\n};\\n\\nconst countLines = (content) => {\\n  const normalized = content.replaceAll(\\"\\\\r\\\\n\\", \\"\\\\n\\");\\n  return normalized.endsWith(\\"\\\\n\\")\\n    ? normalized.slice(0, -1).split(\\"\\\\n\\").length\\n    : normalized.split(\\"\\\\n\\").length;\\n};\\n\\nconst parseFrontmatter = (content, targetPath, errors, rootDir) => {\\n  const parsed = parsePlanFrontmatter(content);\\n  if (!parsed) {\\n    addError(errors, rootDir, targetPath, \\"缺少 YAML frontmatter\\");\\n    return new Map();\\n  }\\n  return parsed.fields;\\n};\\n\\nconst parseDependencies = (rawValue, targetPath, errors, rootDir) => {\\n  const parsed = parseIssueDependencies(rawValue);\\n  if (parsed.kind === \\"valid\\") return parsed.dependencies;\\n  if (parsed.kind === \\"missing\\") {\\n    addError(errors, rootDir, targetPath, \\"frontmatter 缺少 blocked_by\\");\\n    return [];\\n  }\\n  const detail =\\n    parsed.kind === \\"invalid_json\\" ? parsed.detail : \'必须是两位稳定 ID 的 JSON 数组，例如 [\\"01\\"]\';\\n  addError(errors, rootDir, targetPath, `blocked_by 无效: ${detail}`);\\n  return [];\\n};\\n\\nconst hasSection = (content, heading) => new RegExp(`^## ${heading}$`, \\"m\\").test(content);\\n\\nconst assertChineseDocument = (content, targetPath, errors, rootDir) => {\\n  if (!HAN_PATTERN.test(content)) {\\n    addError(errors, rootDir, targetPath, \\"正文必须包含中文\\");\\n  }\\n};\\n\\nconst isValidDate = (value) => {\\n  const [year, month, day] = value.split(\\"-\\").map(Number);\\n  const date = new Date(Date.UTC(year, month - 1, day));\\n  const normalized = [\\n    date.getUTCFullYear().toString().padStart(4, \\"0\\"),\\n    (date.getUTCMonth() + 1).toString().padStart(2, \\"0\\"),\\n    date.getUTCDate().toString().padStart(2, \\"0\\"),\\n  ].join(\\"-\\");\\n  return normalized === value;\\n};\\n\\nconst parseIssueTable = (specContent) => {\\n  const rows = new Map();\\n  for (const line of specContent.split(/\\\\r?\\\\n/)) {\\n    if (!line.startsWith(\\"|\\")) continue;\\n    const cells = line\\n      .slice(1)\\n      .split(\\"|\\")\\n      .map((cell) => cell.trim());\\n    if (cells.length < 5 || !/^\\\\d{2}$/.test(cells[0]) || !ISSUE_STATUSES.includes(cells[2])) {\\n      continue;\\n    }\\n    const link = cells[1].match(/^\\\\[[^\\\\]]+\\\\]\\\\(([^)]+\\\\.md)\\\\)$/);\\n    if (!link) continue;\\n    rows.set(cells[0], {\\n      dependencies: cells[3].match(/\\\\d{2}/g) ?? [],\\n      fileName: path.basename(link[1]),\\n      status: cells[2],\\n    });\\n  }\\n  return rows;\\n};\\n\\nconst checkDependencyGraph = (issues, planPath, errors, rootDir) => {\\n  const issueById = new Map(issues.map((issue) => [issue.id, issue]));\\n  if (!issues.some((issue) => issue.dependencies.length === 0)) {\\n    addError(errors, rootDir, planPath, \\"Issue 依赖图至少需要一个根节点\\");\\n  }\\n\\n  for (const issue of issues) {\\n    for (const dependency of issue.dependencies) {\\n      const dependencyIssue = issueById.get(dependency);\\n      if (!dependencyIssue) {\\n        addError(errors, rootDir, issue.path, `blocked_by 引用了不存在的 Issue ${dependency}`);\\n        continue;\\n      }\\n      if (Number(dependency) >= Number(issue.id)) {\\n        addError(errors, rootDir, issue.path, `依赖 ${dependency} 必须排在 Issue ${issue.id} 之前`);\\n      }\\n      if (\\n        [\\"in_progress\\", \\"blocked\\", \\"completed\\"].includes(issue.status) &&\\n        dependencyIssue.status !== \\"completed\\"\\n      ) {\\n        addError(\\n          errors,\\n          rootDir,\\n          issue.path,\\n          `${issue.status} Issue 的直接依赖 ${dependency} 尚未 completed`,\\n        );\\n      }\\n    }\\n  }\\n\\n  const visiting = new Set();\\n  const visited = new Set();\\n  const visit = (issue) => {\\n    if (visiting.has(issue.id)) return true;\\n    if (visited.has(issue.id)) return false;\\n    visiting.add(issue.id);\\n    const cyclic = issue.dependencies.some((dependency) => {\\n      const dependencyIssue = issueById.get(dependency);\\n      return dependencyIssue ? visit(dependencyIssue) : false;\\n    });\\n    visiting.delete(issue.id);\\n    visited.add(issue.id);\\n    return cyclic;\\n  };\\n\\n  if (issues.some((issue) => visit(issue))) {\\n    addError(errors, rootDir, planPath, \\"Issue 依赖图存在环\\");\\n  }\\n};\\n\\nconst checkIssue = (issuePath, id, errors, rootDir) => {\\n  const content = readText(issuePath, errors, rootDir);\\n  if (content === null) return null;\\n  assertChineseDocument(content, issuePath, errors, rootDir);\\n  const frontmatter = parseFrontmatter(content, issuePath, errors, rootDir);\\n  const status = frontmatter.get(\\"status\\");\\n  if (!ISSUE_STATUSES.includes(status)) {\\n    addError(errors, rootDir, issuePath, `status 必须是 ${ISSUE_STATUSES.join(\\" | \\")}`);\\n  }\\n  const dependencies = parseDependencies(frontmatter.get(\\"blocked_by\\"), issuePath, errors, rootDir);\\n\\n  for (const heading of [\\"交付\\", \\"范围\\", \\"直接依赖\\", \\"验收\\", \\"上下文\\", \\"下一步\\"]) {\\n    if (!hasSection(content, heading)) {\\n      addError(errors, rootDir, issuePath, `缺少「## ${heading}」章节`);\\n    }\\n  }\\n  if (status === \\"blocked\\" && (!content.includes(\\"障碍\\") || !content.includes(\\"解除条件\\"))) {\\n    addError(errors, rootDir, issuePath, \\"blocked Issue 必须记录障碍和解除条件\\");\\n  }\\n  if (\\n    status === \\"completed\\" &&\\n    (!/^## (交付记录|交付物与证据)$/m.test(content) || !content.includes(\\"证据\\"))\\n  ) {\\n    addError(errors, rootDir, issuePath, \\"completed Issue 必须记录交付物与验证证据\\");\\n  }\\n\\n  return { id, path: issuePath, status, dependencies };\\n};\\n\\nconst checkPlanLayout = (planPath, lifecycle, errors, rootDir) => {\\n  const planName = path.basename(planPath);\\n  const planNameMatch = planName.match(/^(\\\\d{4}-\\\\d{2}-\\\\d{2})-(.+)$/);\\n  if (!planNameMatch || !isValidDate(planNameMatch[1]) || !HAN_PATTERN.test(planNameMatch[2])) {\\n    addError(errors, rootDir, planPath, \\"Plan 目录名必须是 YYYY-MM-DD-中文工作名\\");\\n  }\\n\\n  const entries = readEntries(planPath, errors, rootDir);\\n  for (const entry of entries.filter((item) => item.isDirectory())) {\\n    addError(errors, rootDir, path.join(planPath, entry.name), \\"Plan 目录内不允许嵌套目录\\");\\n  }\\n  const markdownFiles = entries.filter((entry) => entry.isFile() && entry.name.endsWith(\\".md\\"));\\n  const hasStory = markdownFiles.some((entry) => entry.name === \\"story.md\\");\\n  const hasSpec = markdownFiles.some((entry) => entry.name === \\"spec.md\\");\\n  if (!hasStory && !hasSpec) {\\n    addError(errors, rootDir, planPath, \\"Plan 至少需要 story.md 或 spec.md\\");\\n  }\\n  if (!hasSpec && lifecycle !== \\"active\\") {\\n    addError(errors, rootDir, planPath, \\"非 active Plan 必须包含 spec.md\\");\\n  }\\n\\n  for (const entry of markdownFiles) {\\n    const allowed =\\n      [\\"story.md\\", \\"spec.md\\"].includes(entry.name) || /^\\\\d{2}-.+\\\\.md$/.test(entry.name);\\n    if (!allowed) {\\n      addError(\\n        errors,\\n        rootDir,\\n        path.join(planPath, entry.name),\\n        \\"Plan 文件名必须是 story.md、spec.md 或 NN-中文标题.md\\",\\n      );\\n    }\\n    if (![\\"story.md\\", \\"spec.md\\"].includes(entry.name) && !HAN_PATTERN.test(entry.name)) {\\n      addError(errors, rootDir, path.join(planPath, entry.name), \\"Issue 文件名必须包含中文标题\\");\\n    }\\n    const content = readText(path.join(planPath, entry.name), errors, rootDir);\\n    if (content !== null) {\\n      assertChineseDocument(content, path.join(planPath, entry.name), errors, rootDir);\\n    }\\n  }\\n  return { hasSpec, markdownFiles };\\n};\\n\\nconst loadIssues = (planPath, markdownFiles, errors, rootDir) => {\\n  const issueEntries = markdownFiles\\n    .filter((entry) => /^\\\\d{2}-.+\\\\.md$/.test(entry.name))\\n    .sort((left, right) => left.name.localeCompare(right.name, \\"zh-CN\\"));\\n  if (issueEntries.length === 0) {\\n    addError(errors, rootDir, planPath, \\"包含 spec.md 的 Plan 至少需要一个 Issue\\");\\n    return [];\\n  }\\n\\n  const issues = [];\\n  for (const [index, entry] of issueEntries.entries()) {\\n    const id = entry.name.slice(0, 2);\\n    const expectedId = String(index + 1).padStart(2, \\"0\\");\\n    if (id !== expectedId) {\\n      addError(\\n        errors,\\n        rootDir,\\n        path.join(planPath, entry.name),\\n        `Issue 编号必须连续，期望 ${expectedId}`,\\n      );\\n    }\\n    const issue = checkIssue(path.join(planPath, entry.name), id, errors, rootDir);\\n    if (issue) issues.push({ ...issue, fileName: entry.name });\\n  }\\n  return issues;\\n};\\n\\nconst checkIssueTable = (specContent, specPath, issues, errors, rootDir) => {\\n  const issueTable = parseIssueTable(specContent);\\n  for (const issue of issues) {\\n    const row = issueTable.get(issue.id);\\n    if (!row) {\\n      addError(errors, rootDir, specPath, `Issue 表缺少 ${issue.id}`);\\n      continue;\\n    }\\n    if (row.fileName !== issue.fileName) {\\n      addError(errors, rootDir, specPath, `Issue ${issue.id} 链接应指向 ${issue.fileName}`);\\n    }\\n    if (row.status !== issue.status) {\\n      addError(\\n        errors,\\n        rootDir,\\n        specPath,\\n        `Issue ${issue.id} 表格状态 ${row.status} 与 frontmatter ${issue.status} 不一致`,\\n      );\\n    }\\n    if (row.dependencies.join(\\",\\") !== issue.dependencies.join(\\",\\")) {\\n      addError(errors, rootDir, specPath, `Issue ${issue.id} 表格依赖与 blocked_by 不一致`);\\n    }\\n  }\\n  for (const id of issueTable.keys()) {\\n    if (!issues.some((issue) => issue.id === id)) {\\n      addError(errors, rootDir, specPath, `Issue 表引用了不存在的 ${id}`);\\n    }\\n  }\\n};\\n\\nconst checkSpec = (planPath, lifecycle, markdownFiles, errors, rootDir) => {\\n  const specPath = path.join(planPath, \\"spec.md\\");\\n  const specContent = readText(specPath, errors, rootDir);\\n  if (specContent === null) return;\\n  const specFrontmatter = parseFrontmatter(specContent, specPath, errors, rootDir);\\n  for (const heading of [\\n    \\"问题\\",\\n    \\"方案\\",\\n    \\"已排除的备选\\",\\n    \\"实施决策\\",\\n    \\"工作环境\\",\\n    \\"范围\\",\\n    \\"非范围\\",\\n    \\"待定\\",\\n    \\"上下文\\",\\n    \\"Issue\\",\\n  ]) {\\n    if (!hasSection(specContent, heading)) {\\n      addError(errors, rootDir, specPath, `缺少「## ${heading}」章节`);\\n    }\\n  }\\n  const specStatus = specFrontmatter.get(\\"status\\");\\n  if (!SPEC_STATUSES.has(specStatus)) {\\n    addError(errors, rootDir, specPath, `status 必须是 ${[...SPEC_STATUSES].join(\\" | \\")}`);\\n  }\\n  const issues = loadIssues(planPath, markdownFiles, errors, rootDir);\\n  if (issues.length === 0) return;\\n  checkDependencyGraph(issues, planPath, errors, rootDir);\\n  checkIssueTable(specContent, specPath, issues, errors, rootDir);\\n  const expectedSpecStatus = deriveSpecStatus(issues);\\n  if (specStatus !== expectedSpecStatus) {\\n    addError(errors, rootDir, specPath, `聚合状态应为 ${expectedSpecStatus}，实际为 ${specStatus}`);\\n  }\\n  if (lifecycle === \\"reference\\" && expectedSpecStatus !== \\"completed\\") {\\n    addError(errors, rootDir, planPath, \\"reference Plan 的 Issue 必须全部 completed\\");\\n  }\\n};\\n\\nconst checkPlan = (planPath, lifecycle, errors, rootDir) => {\\n  const layout = checkPlanLayout(planPath, lifecycle, errors, rootDir);\\n  if (layout.hasSpec) {\\n    checkSpec(planPath, lifecycle, layout.markdownFiles, errors, rootDir);\\n  }\\n};\\n\\nconst checkPlans = (domainPath, errors, rootDir) => {\\n  const plansPath = path.join(domainPath, \\"plans\\");\\n  if (!fs.existsSync(plansPath)) return;\\n  const lifecycleEntries = readEntries(plansPath, errors, rootDir).filter((entry) =>\\n    entry.isDirectory(),\\n  );\\n  for (const lifecycleEntry of lifecycleEntries) {\\n    if (!LIFECYCLES.has(lifecycleEntry.name)) {\\n      addError(\\n        errors,\\n        rootDir,\\n        path.join(plansPath, lifecycleEntry.name),\\n        \\"Plan 生命周期目录只能是 active、reference 或 archived\\",\\n      );\\n      continue;\\n    }\\n    const lifecyclePath = path.join(plansPath, lifecycleEntry.name);\\n    for (const planEntry of readEntries(lifecyclePath, errors, rootDir).filter((entry) =>\\n      entry.isDirectory(),\\n    )) {\\n      checkPlan(path.join(lifecyclePath, planEntry.name), lifecycleEntry.name, errors, rootDir);\\n    }\\n  }\\n};\\n\\nconst checkAdrs = (domainPath, errors, rootDir) => {\\n  const adrPath = path.join(domainPath, \\"adr\\");\\n  if (!fs.existsSync(adrPath)) return;\\n  const adrFiles = readEntries(adrPath, errors, rootDir)\\n    .filter((entry) => entry.isFile() && entry.name.endsWith(\\".md\\"))\\n    .sort((left, right) => left.name.localeCompare(right.name, \\"zh-CN\\"));\\n  if (adrFiles.length > 89) {\\n    addError(errors, rootDir, adrPath, `ADR 数量 ${adrFiles.length} 超过 89`);\\n  }\\n  for (const [index, adrFile] of adrFiles.entries()) {\\n    const adrFilePath = path.join(adrPath, adrFile.name);\\n    const nameMatch = adrFile.name.match(/^(\\\\d{4})-(.+)\\\\.md$/);\\n    const expectedId = String(index + 1).padStart(4, \\"0\\");\\n    if (!nameMatch || nameMatch[1] !== expectedId || !HAN_PATTERN.test(nameMatch[2])) {\\n      addError(\\n        errors,\\n        rootDir,\\n        adrFilePath,\\n        `ADR 文件名必须按域内连续编号，期望 ${expectedId}-中文决策名.md`,\\n      );\\n    }\\n    const content = readText(adrFilePath, errors, rootDir);\\n    if (content === null) continue;\\n    assertChineseDocument(content, adrFilePath, errors, rootDir);\\n    const lineCount = countLines(content);\\n    if (lineCount > 144) {\\n      addError(errors, rootDir, adrFilePath, `ADR 共 ${lineCount} 行，超过 144`);\\n    }\\n  }\\n};\\n\\nexport const checkDomain = (rootDirectory) => {\\n  const rootDir = path.resolve(rootDirectory);\\n  const errors = [];\\n  const policyPath = path.resolve(\\n    path.dirname(fileURLToPath(import.meta.url)),\\n    \\"..\\",\\n    \\"references\\",\\n    \\"DOMAIN.md\\",\\n  );\\n  if (!fs.existsSync(policyPath)) {\\n    addError(errors, rootDir, policyPath, \\"/nano-flow 缺少 references/DOMAIN.md\\");\\n  }\\n  for (const requiredFile of [\\"CONTEXT-MAP.md\\"]) {\\n    if (!fs.existsSync(path.join(rootDir, requiredFile))) {\\n      addError(errors, rootDir, path.join(rootDir, requiredFile), \\"文件不存在\\");\\n    }\\n  }\\n  const docsPath = path.join(rootDir, \\"docs\\");\\n  if (!fs.existsSync(docsPath)) {\\n    addError(errors, rootDir, docsPath, \\"目录不存在\\");\\n    return errors;\\n  }\\n  const mapPath = path.join(rootDir, \\"CONTEXT-MAP.md\\");\\n  const mapContent = fs.existsSync(mapPath) ? readText(mapPath, errors, rootDir) : null;\\n  const domainEntries = readEntries(docsPath, errors, rootDir).filter((entry) =>\\n    entry.isDirectory(),\\n  );\\n  const domainNames = new Set(domainEntries.map((entry) => entry.name));\\n\\n  for (const domainEntry of domainEntries) {\\n    const domainPath = path.join(docsPath, domainEntry.name);\\n    if (!/^[a-z0-9]+(?:-[a-z0-9]+)*$/.test(domainEntry.name)) {\\n      addError(errors, rootDir, domainPath, \\"domain-name 必须是 kebab-case\\");\\n    }\\n    const contextPath = path.join(domainPath, \\"CONTEXT.md\\");\\n    if (!fs.existsSync(contextPath)) {\\n      addError(errors, rootDir, contextPath, \\"业务域缺少 CONTEXT.md\\");\\n    } else {\\n      const content = readText(contextPath, errors, rootDir);\\n      if (content !== null) {\\n        assertChineseDocument(content, contextPath, errors, rootDir);\\n        const lineCount = countLines(content);\\n        if (lineCount > 610) {\\n          addError(errors, rootDir, contextPath, `CONTEXT.md 共 ${lineCount} 行，超过 610`);\\n        }\\n      }\\n    }\\n    if (mapContent !== null && !mapContent.includes(`(./docs/${domainEntry.name}/CONTEXT.md)`)) {\\n      addError(errors, rootDir, mapPath, `缺少业务域 ${domainEntry.name} 的索引`);\\n    }\\n    checkAdrs(domainPath, errors, rootDir);\\n    checkPlans(domainPath, errors, rootDir);\\n  }\\n\\n  if (mapContent !== null) {\\n    for (const match of mapContent.matchAll(/\\\\(\\\\.\\\\/docs\\\\/([^/]+)\\\\/CONTEXT\\\\.md\\\\)/g)) {\\n      if (!domainNames.has(match[1])) {\\n        addError(errors, rootDir, mapPath, `索引指向不存在的业务域 ${match[1]}`);\\n      }\\n    }\\n  }\\n\\n  return errors;\\n};\\n\\nexport const runDomainCheck = ({\\n  rootDirectory = process.argv[2] ?? process.cwd(),\\n  stderr = (message) => console.error(message),\\n  stdout = (message) => console.log(message),\\n} = {}) => {\\n  const rootDir = path.resolve(rootDirectory);\\n  try {\\n    const errors = checkDomain(rootDir);\\n    if (errors.length > 0) {\\n      stderr(`领域文档校验失败，共 ${errors.length} 项：`);\\n      for (const error of errors) stderr(`- ${error}`);\\n      return 1;\\n    }\\n    stdout(\\"领域文档校验通过。\\");\\n    return 0;\\n  } catch (error) {\\n    stderr(`领域文档校验异常: ${error instanceof Error ? error.stack : String(error)}`);\\n    return 1;\\n  }\\n};\\n\\nconst invokedPath = process.argv[1] ? path.resolve(process.argv[1]) : \\"\\";\\nif (invokedPath === fileURLToPath(import.meta.url)) process.exitCode = runDomainCheck();\\n"},{"path":"scripts/flow.mjs","content":"#!/usr/bin/env node\\n\\nimport fs from \\"node:fs/promises\\";\\nimport path from \\"node:path\\";\\nimport process from \\"node:process\\";\\nimport { randomUUID } from \\"node:crypto\\";\\nimport { fileURLToPath } from \\"node:url\\";\\n\\nimport {\\n  deriveSpecStatus,\\n  ISSUE_STATUSES,\\n  parseFrontmatter as parsePlanFrontmatter,\\n  parseIssueDependencies,\\n} from \\"./plan-document.mjs\\";\\n\\nconst SCHEMA_VERSION = 5;\\nconst DEFAULT_LEASE_SECONDS = 1800;\\nconst DEFAULT_STATE_RETENTION_DAYS = 30;\\nconst LOCK_STALE_MS = 30000;\\nconst LOCK_RETRIES = 50;\\nconst LOCK_RETRY_MS = 100;\\nconst PLAN_PHASES = new Set([\\"planning\\", \\"delivering_direct\\", \\"delivering_issues\\", \\"completed\\"]);\\n\\nconst DELIVERY_FLOW = [\\n  { skill: \\"code-delivery\\", results: [\\"started\\"] },\\n  { action: \\"commit\\", results: [\\"committed\\"] },\\n];\\n\\nconst PLANNING_FLOW = [\\n  { skill: \\"to-story\\", results: [\\"completed\\"] },\\n  { skill: \\"quest-with-domain\\", results: [\\"completed\\"] },\\n  { skill: \\"to-issues\\", results: [\\"completed\\", \\"skipped\\"] },\\n  { skill: \\"dev-gate\\", results: [\\"ready\\"] },\\n];\\n\\nconst ISSUE_FLOW = DELIVERY_FLOW;\\n\\nconst HOOK_SCHEMA_VERSION = 1;\\nconst HOOKS_PATH = fileURLToPath(new URL(\\"../extensions/hooks.json\\", import.meta.url));\\nconst HOOKABLE_SKILLS = new Set(\\n  [...PLANNING_FLOW, ...DELIVERY_FLOW].filter((step) => step.skill).map((step) => step.skill),\\n);\\n\\nconst FLOW_MODES = new Set([\\"auto\\", \\"manual\\"]);\\nconst DEFAULT_FLOW_MODE = \\"manual\\";\\nconst MANUAL_MODE_HOOK =\\n  \\"当前 skill 执行结束后, 询问用户当前 skill 是否完成 + 是否进入下一个 skill, 用户同意后, 执行 flow.mjs, 然后自动调用下一个 skill(无须用户确认).\\";\\nconst AUTO_MODE_HOOK = \\"当前 skill 执行结束后, 无需用户确认, 自动调用下一个 skill\\";\\nconst MODE_HOOK_SKILLS = new Set(\\n  PLANNING_FLOW.filter((step) => step.skill !== \\"dev-gate\\").map((step) => step.skill),\\n);\\n\\nconst ENTRY_CURSORS = {\\n  \\"quest-with-domain\\": 1,\\n  \\"to-story\\": 0,\\n};\\n\\nconst fail = (message) => {\\n  throw new Error(message);\\n};\\n\\nconst sleep = (milliseconds) => new Promise((resolve) => setTimeout(resolve, milliseconds));\\n\\nconst normalizeSkill = (value) => value?.replace(/^\\\\//, \\"\\");\\n\\nconst validateHooks = (config) => {\\n  if (\\n    config === null ||\\n    config.schema_version !== HOOK_SCHEMA_VERSION ||\\n    !Array.isArray(config.hooks)\\n  ) {\\n    fail(\\"Hook 配置格式无效或版本不受支持\\");\\n  }\\n  for (const [index, hook] of config.hooks.entries()) {\\n    if (hook === null || typeof hook !== \\"object\\" || Array.isArray(hook)) {\\n      fail(`Hook ${index + 1} 格式无效`);\\n    }\\n    if (\\n      hook.match !== \\"all\\" &&\\n      (!Array.isArray(hook.match) ||\\n        hook.match.length === 0 ||\\n        hook.match.some((skill) => typeof skill !== \\"string\\" || !HOOKABLE_SKILLS.has(skill)))\\n    ) {\\n      fail(`Hook ${index + 1} 的 match 必须是 \\"all\\" 或主流程 Skill 数组`);\\n    }\\n    if (\\n      typeof hook.message !== \\"string\\" ||\\n      hook.message.trim() === \\"\\" ||\\n      /[\\\\r\\\\n]/u.test(hook.message)\\n    ) {\\n      fail(`Hook ${index + 1} 的 message 必须是非空单行字符串`);\\n    }\\n  }\\n  return config;\\n};\\n\\nconst loadHooks = async (hooksPath = HOOKS_PATH) => {\\n  let content;\\n  try {\\n    content = await fs.readFile(hooksPath, \\"utf8\\");\\n  } catch (error) {\\n    fail(\\n      `读取 Hook 配置 ${hooksPath} 失败: ${error instanceof Error ? error.message : String(error)}`,\\n    );\\n  }\\n  try {\\n    return validateHooks(JSON.parse(content));\\n  } catch (error) {\\n    if (error instanceof SyntaxError) fail(`解析 Hook 配置 ${hooksPath} 失败: ${error.message}`);\\n    throw error;\\n  }\\n};\\n\\nconst messageForSkill = (config, skill, mode) => {\\n  const normalized = normalizeSkill(skill);\\n  const messages = MODE_HOOK_SKILLS.has(normalized)\\n    ? [mode === \\"auto\\" ? AUTO_MODE_HOOK : MANUAL_MODE_HOOK]\\n    : [];\\n  return messages\\n    .concat(\\n      config.hooks\\n        .filter((hook) => hook.match === \\"all\\" || hook.match.includes(normalized))\\n        .map((hook) => hook.message),\\n    )\\n    .join(\\"\\\\n\\");\\n};\\n\\nconst requireOption = (options, name) => {\\n  const value = options[name];\\n  if (typeof value !== \\"string\\" || value.trim() === \\"\\") {\\n    fail(`缺少 --${name}`);\\n  }\\n  return value.trim();\\n};\\n\\nconst optionValues = (options, name) => {\\n  const value = options[name];\\n  if (Array.isArray(value)) return value;\\n  if (typeof value === \\"string\\") return [value];\\n  return [];\\n};\\n\\nconst leaseSeconds = (options) => {\\n  const rawValue = options[\\"lease-seconds\\"];\\n  if (rawValue === undefined) return DEFAULT_LEASE_SECONDS;\\n  const value = Number(rawValue);\\n  if (!Number.isInteger(value) || value < 30 || value > 86400) {\\n    fail(\\"--lease-seconds 必须是 30 到 86400 之间的整数\\");\\n  }\\n  return value;\\n};\\n\\nconst normalizePlanPath = (workspace, planInput) => {\\n  const absolutePath = path.resolve(workspace, planInput);\\n  const relativePath = path.relative(workspace, absolutePath);\\n  if (relativePath.startsWith(\\"..\\") || path.isAbsolute(relativePath)) {\\n    fail(\\"--plan 必须位于工作区内\\");\\n  }\\n  return relativePath === \\"\\" ? \\".\\" : relativePath.replaceAll(\\"\\\\\\\\\\", \\"/\\");\\n};\\n\\nconst calendarDate = (date) =>\\n  [date.getFullYear(), date.getMonth() + 1, date.getDate()]\\n    .map((part, index) => (index === 0 ? String(part) : String(part).padStart(2, \\"0\\")))\\n    .join(\\"-\\");\\n\\nconst statePaths = (workspace, now) => ({\\n  dir: path.join(workspace, \\".flow\\", \\"state\\"),\\n  state: path.join(workspace, \\".flow\\", \\"state\\", `${calendarDate(now)}-state.json`),\\n});\\n\\nconst removeExpiredStates = async (directory, now) => {\\n  const oldestRetainedDate = new Date(now);\\n  oldestRetainedDate.setHours(0, 0, 0, 0);\\n  oldestRetainedDate.setDate(oldestRetainedDate.getDate() - (DEFAULT_STATE_RETENTION_DAYS - 1));\\n  const oldestRetainedPrefix = calendarDate(oldestRetainedDate);\\n  const currentPrefix = calendarDate(now);\\n  try {\\n    const entries = await fs.readdir(directory, { withFileTypes: true });\\n    const expiredStates = entries.filter(\\n      (entry) =>\\n        entry.isFile() &&\\n        /^\\\\d{4}-\\\\d{2}-\\\\d{2}-state\\\\.json$/u.test(entry.name) &&\\n        (entry.name.slice(0, 10) < oldestRetainedPrefix || entry.name.slice(0, 10) > currentPrefix),\\n    );\\n    await Promise.all(expiredStates.map((entry) => fs.unlink(path.join(directory, entry.name))));\\n  } catch (error) {\\n    fail(\\n      `清理 ${directory} 中的过期状态失败: ${error instanceof Error ? error.message : String(error)}`,\\n    );\\n  }\\n};\\n\\nconst emptyState = () => ({\\n  plans: {},\\n  revision: 0,\\n  schema_version: SCHEMA_VERSION,\\n});\\n\\nconst validateState = (state) => {\\n  if (\\n    state === null ||\\n    state.schema_version !== SCHEMA_VERSION ||\\n    !Number.isInteger(state.revision) ||\\n    state.plans === null ||\\n    typeof state.plans !== \\"object\\" ||\\n    Array.isArray(state.plans)\\n  ) {\\n    fail(\\"Flow 状态格式无效或版本不受支持\\");\\n  }\\n  for (const plan of Object.values(state.plans)) {\\n    if (\\n      plan === null ||\\n      !PLAN_PHASES.has(plan.phase) ||\\n      !Number.isInteger(plan.cursor) ||\\n      plan.cursor < 0 ||\\n      !Array.isArray(plan.receipts) ||\\n      plan.issues === null ||\\n      typeof plan.issues !== \\"object\\" ||\\n      Array.isArray(plan.issues) ||\\n      (plan.lease !== null && (typeof plan.lease !== \\"object\\" || Array.isArray(plan.lease))) ||\\n      (plan.mode !== undefined && !FLOW_MODES.has(plan.mode))\\n    ) {\\n      fail(\\"Flow Plan 状态格式无效\\");\\n    }\\n  }\\n  return state;\\n};\\n\\nconst readText = async (targetPath, allowMissing = false) => {\\n  try {\\n    return await fs.readFile(targetPath, \\"utf8\\");\\n  } catch (error) {\\n    if (allowMissing && error?.code === \\"ENOENT\\") return null;\\n    fail(`读取 ${targetPath} 失败: ${error instanceof Error ? error.message : String(error)}`);\\n  }\\n};\\n\\nconst readState = async (statePath) => {\\n  const content = await readText(statePath, true);\\n  if (content === null) return emptyState();\\n  try {\\n    return validateState(JSON.parse(content));\\n  } catch (error) {\\n    if (error instanceof SyntaxError) fail(`解析 ${statePath} 失败: ${error.message}`);\\n    throw error;\\n  }\\n};\\n\\nconst atomicWrite = async (targetPath, content) => {\\n  const temporaryPath = `${targetPath}.${randomUUID()}.tmp`;\\n  let handle;\\n  try {\\n    handle = await fs.open(temporaryPath, \\"wx\\");\\n    await handle.writeFile(content, \\"utf8\\");\\n    await handle.sync();\\n    await handle.close();\\n    handle = null;\\n    await fs.rename(temporaryPath, targetPath);\\n  } catch (error) {\\n    if (handle) await handle.close().catch(() => undefined);\\n    await fs.unlink(temporaryPath).catch(() => undefined);\\n    fail(`写入 ${targetPath} 失败: ${error instanceof Error ? error.message : String(error)}`);\\n  }\\n};\\n\\nconst acquireLock = async (lockPath, now) => {\\n  for (let attempt = 0; attempt < LOCK_RETRIES; attempt += 1) {\\n    const nonce = randomUUID();\\n    try {\\n      const handle = await fs.open(lockPath, \\"wx\\");\\n      await handle.writeFile(\\n        JSON.stringify({ acquired_at: now().toISOString(), nonce, pid: process.pid }),\\n        \\"utf8\\",\\n      );\\n      await handle.sync();\\n      await handle.close();\\n      return nonce;\\n    } catch (error) {\\n      if (error?.code !== \\"EEXIST\\") {\\n        fail(`取得 ${lockPath} 失败: ${error instanceof Error ? error.message : String(error)}`);\\n      }\\n      try {\\n        const stat = await fs.stat(lockPath);\\n        if (now().getTime() - stat.mtimeMs > LOCK_STALE_MS) {\\n          await fs.unlink(lockPath).catch(() => undefined);\\n          continue;\\n        }\\n      } catch (statError) {\\n        if (statError?.code !== \\"ENOENT\\") {\\n          fail(`检查 ${lockPath} 失败: ${statError.message}`);\\n        }\\n      }\\n      await sleep(LOCK_RETRY_MS);\\n    }\\n  }\\n  fail(`等待 ${lockPath} 超时`);\\n};\\n\\nconst releaseLock = async (lockPath, nonce) => {\\n  try {\\n    const content = await fs.readFile(lockPath, \\"utf8\\");\\n    const lock = JSON.parse(content);\\n    if (lock.nonce === nonce) await fs.unlink(lockPath);\\n  } catch (error) {\\n    if (error?.code !== \\"ENOENT\\") {\\n      console.error(`释放 ${lockPath} 失败: ${error.message}`);\\n    }\\n  }\\n};\\n\\nconst withStateTransaction = async (workspace, now, action) => {\\n  const directory = path.join(workspace, \\".flow\\", \\"state\\");\\n  const lockPath = path.join(directory, \\"state.lock\\");\\n  await fs.mkdir(directory, { recursive: true });\\n  const nonce = await acquireLock(lockPath, now);\\n  try {\\n    const transactionTime = now();\\n    const paths = statePaths(workspace, transactionTime);\\n    await removeExpiredStates(paths.dir, transactionTime);\\n    const state = await readState(paths.state);\\n    const transaction = await action(state, transactionTime);\\n    if (transaction.changed) {\\n      state.revision += 1;\\n      await atomicWrite(paths.state, `${JSON.stringify(state, null, 2)}\\\\n`);\\n    }\\n    return { ...transaction.output, revision: state.revision };\\n  } finally {\\n    await releaseLock(lockPath, nonce);\\n  }\\n};\\n\\nconst makeLease = (session, seconds, now) => ({\\n  expires_at: new Date(now.getTime() + seconds * 1000).toISOString(),\\n  owner_session: session,\\n});\\n\\nconst leaseIsActive = (lease, now) =>\\n  lease?.owner_session && Date.parse(lease.expires_at) > now.getTime();\\n\\nconst requireLease = (subject, session, now) => {\\n  if (!leaseIsActive(subject.lease, now)) fail(\\"租约不存在或已经过期\\");\\n  if (subject.lease.owner_session !== session) {\\n    fail(`资源由会话 ${subject.lease.owner_session} 持有`);\\n  }\\n};\\n\\nconst nextStep = (sequence, cursor) => sequence[cursor] ?? null;\\n\\nconst issuesDecision = (plan) =>\\n  plan.receipts.find((receipt) => receipt.step === \\"/to-issues\\")?.result ?? null;\\n\\nconst planSequence = (plan) => {\\n  if (plan.phase === \\"planning\\") return PLANNING_FLOW;\\n  if (plan.phase === \\"delivering_direct\\") return DELIVERY_FLOW;\\n  return null;\\n};\\n\\nconst currentPlanStep = (plan) => {\\n  const sequence = planSequence(plan);\\n  return sequence ? nextStep(sequence, plan.cursor) : null;\\n};\\n\\nconst stepLabel = (step) => (step.skill ? `/${step.skill}` : step.action);\\n\\nconst stepOutput = (step) => {\\n  if (!step) return { next_action: null, next_skill: null };\\n  if (step.skill) return { next_action: null, next_skill: `/${step.skill}` };\\n  return { next_action: step.action, next_skill: null };\\n};\\n\\nconst publicPlan = (plan) => ({\\n  cursor: plan.cursor,\\n  issues: plan.issues,\\n  lease: plan.lease,\\n  phase: plan.phase,\\n  receipts: plan.receipts,\\n});\\n\\nconst parseFrontmatter = (content, targetPath) => {\\n  const parsed = parsePlanFrontmatter(content);\\n  if (!parsed) fail(`${targetPath} 缺少 YAML frontmatter`);\\n  return parsed;\\n};\\n\\nconst replaceFrontmatterField = (content, field, value, targetPath) => {\\n  const parsed = parseFrontmatter(content, targetPath);\\n  const fieldPattern = new RegExp(`^${field}:\\\\\\\\s*.*$`, \\"m\\");\\n  if (!fieldPattern.test(parsed.match[1])) fail(`${targetPath} 缺少 ${field}`);\\n  const frontmatter = parsed.match[1].replace(fieldPattern, `${field}: ${value}`);\\n  return content.replace(parsed.match[0], `---\\\\n${frontmatter}\\\\n---\\\\n`);\\n};\\n\\nconst parseDependencies = (rawValue, targetPath) => {\\n  const parsed = parseIssueDependencies(rawValue);\\n  if (parsed.kind === \\"valid\\") return parsed.dependencies;\\n  if (parsed.kind === \\"invalid_value\\") {\\n    fail(`${targetPath} 的 blocked_by 必须是两位 Issue ID 的 JSON 数组`);\\n  }\\n  fail(`${targetPath} 的 blocked_by 不是有效 JSON`);\\n};\\n\\nconst issueFiles = async (planPath) => {\\n  try {\\n    return (await fs.readdir(planPath, { withFileTypes: true }))\\n      .filter((entry) => entry.isFile() && /^\\\\d{2}-.+\\\\.md$/.test(entry.name))\\n      .sort((left, right) => left.name.localeCompare(right.name, \\"zh-CN\\"));\\n  } catch (error) {\\n    fail(`枚举 Plan ${planPath} 失败: ${error.message}`);\\n  }\\n};\\n\\nconst readIssues = async (planPath) => {\\n  const snapshots = [];\\n  for (const entry of await issueFiles(planPath)) {\\n    const issuePath = path.join(planPath, entry.name);\\n    const content = await readText(issuePath);\\n    const frontmatter = parseFrontmatter(content, issuePath).fields;\\n    const status = frontmatter.get(\\"status\\");\\n    if (!ISSUE_STATUSES.includes(status)) fail(`${issuePath} 的 status 无效`);\\n    snapshots.push({\\n      content,\\n      dependencies: parseDependencies(frontmatter.get(\\"blocked_by\\"), issuePath),\\n      file_name: entry.name,\\n      id: entry.name.slice(0, 2),\\n      path: issuePath,\\n      status,\\n    });\\n  }\\n  if (snapshots.length === 0) fail(`${planPath} 没有 Issue 文件`);\\n  return snapshots;\\n};\\n\\nconst updateSpecView = async (planPath, issues) => {\\n  const specPath = path.join(planPath, \\"spec.md\\");\\n  let content = await readText(specPath);\\n  content = replaceFrontmatterField(content, \\"status\\", deriveSpecStatus(issues), specPath);\\n  for (const issue of issues) {\\n    const rowPattern = new RegExp(\\n      `^(\\\\\\\\|\\\\\\\\s*${issue.id}\\\\\\\\s*\\\\\\\\|\\\\\\\\s*\\\\\\\\[[^\\\\\\\\]]+\\\\\\\\]\\\\\\\\([^)]+\\\\\\\\)\\\\\\\\s*\\\\\\\\|\\\\\\\\s*)(pending|in_progress|blocked|completed)(\\\\\\\\s*\\\\\\\\|.*)$`,\\n      \\"m\\",\\n    );\\n    if (!rowPattern.test(content)) fail(`${specPath} 的 Issue 表缺少 ${issue.id}`);\\n    content = content.replace(rowPattern, `$1${issue.status}$3`);\\n  }\\n  await atomicWrite(specPath, content);\\n};\\n\\nconst transitionIssue = async (planPath, issueId, targetStatus, transformBody) => {\\n  const issues = await readIssues(planPath);\\n  const issue = issues.find((item) => item.id === issueId);\\n  if (!issue) fail(`Plan 中不存在 Issue ${issueId}`);\\n  let content = replaceFrontmatterField(issue.content, \\"status\\", targetStatus, issue.path);\\n  if (transformBody) content = transformBody(content);\\n  await atomicWrite(issue.path, content);\\n  issue.content = content;\\n  issue.status = targetStatus;\\n  await updateSpecView(planPath, issues);\\n  return issue;\\n};\\n\\nconst requireIssueReady = async (planPath, issueId) => {\\n  const issues = await readIssues(planPath);\\n  const issue = issues.find((item) => item.id === issueId);\\n  if (!issue) fail(`Plan 中不存在 Issue ${issueId}`);\\n  if (![\\"pending\\", \\"blocked\\", \\"in_progress\\"].includes(issue.status)) {\\n    fail(`Issue ${issueId} 当前状态 ${issue.status} 不可领取`);\\n  }\\n  for (const dependencyId of issue.dependencies) {\\n    const dependency = issues.find((item) => item.id === dependencyId);\\n    if (dependency?.status !== \\"completed\\") {\\n      fail(`Issue ${issueId} 的直接依赖 ${dependencyId} 尚未 completed`);\\n    }\\n  }\\n  return issue;\\n};\\n\\nconst blockedBody = (content, reason, releaseCondition) => {\\n  const section = `## 阻塞记录\\\\n\\\\n- 障碍: ${reason}\\\\n- 解除条件: ${releaseCondition}\\\\n`;\\n  const pattern = /^## 阻塞记录\\\\r?\\\\n[\\\\s\\\\S]*?(?=^## |(?![\\\\s\\\\S]))/m;\\n  if (pattern.test(content)) return content.replace(pattern, section);\\n  return `${content.trimEnd()}\\\\n\\\\n${section}`;\\n};\\n\\nconst hasDeliveryEvidence = (content) => {\\n  const section = content.match(/^## (?:交付记录|交付物与证据)\\\\r?\\\\n([\\\\s\\\\S]*?)(?=^## |(?![\\\\s\\\\S]))/m);\\n  if (!section) return false;\\n  const body = section[1]\\n    .split(/\\\\r?\\\\n/)\\n    .filter((line) => !/^\\\\s*\\\\{.*\\\\}\\\\s*$/.test(line))\\n    .join(\\"\\\\n\\");\\n  return body.includes(\\"交付物\\") && body.includes(\\"证据\\");\\n};\\n\\nconst recordReceipt = (subject, sequence, options, now) => {\\n  const expected = nextStep(sequence, subject.cursor);\\n  if (!expected) fail(\\"当前流程已经没有待执行 skill\\");\\n  recordStepReceipt(subject, expected, options, now);\\n  subject.cursor += 1;\\n  return nextStep(sequence, subject.cursor);\\n};\\n\\nconst handleInit = (state, workspace, options, now) => {\\n  const planInput = requireOption(options, \\"plan\\");\\n  const planKey = normalizePlanPath(workspace, planInput);\\n  const entry = normalizeSkill(requireOption(options, \\"entry\\"));\\n  const session = requireOption(options, \\"session\\");\\n  if (ENTRY_CURSORS[entry] === undefined) {\\n    fail(\\"--entry 必须是 /to-story | /quest-with-domain\\");\\n  }\\n  if (state.plans[planKey]) fail(`Plan ${planKey} 已经初始化`);\\n  state.plans[planKey] = {\\n    cursor: ENTRY_CURSORS[entry],\\n    issues: {},\\n    lease: makeLease(session, leaseSeconds(options), now),\\n    mode: options.mode ?? DEFAULT_FLOW_MODE,\\n    phase: \\"planning\\",\\n    receipts: [],\\n  };\\n  return {\\n    changed: true,\\n    output: {\\n      ...stepOutput(currentPlanStep(state.plans[planKey])),\\n      phase: \\"planning\\",\\n      plan: planKey,\\n      session,\\n    },\\n  };\\n};\\n\\nconst handleEnterPlan = (state, workspace, options, now) => {\\n  const initiator = normalizeSkill(requireOption(options, \\"skill\\"));\\n  if (initiator !== \\"nano-flow\\") {\\n    fail(\\"--skill 必须是 /nano-flow\\");\\n  }\\n  const entrySkill = normalizeSkill(requireOption(options, \\"entry\\"));\\n  if (ENTRY_CURSORS[entrySkill] === undefined) {\\n    fail(\\"--entry 必须是 /to-story | /quest-with-domain\\");\\n  }\\n  const mode = options.mode?.trim();\\n  if (mode !== undefined && !FLOW_MODES.has(mode)) fail(\\"--mode 必须是 manual | auto\\");\\n  if (options.plan === undefined) fail(`/${entrySkill} 进入流程前必须提供 --plan`);\\n  const planInput = requireOption(options, \\"plan\\");\\n  const planKey = normalizePlanPath(workspace, planInput);\\n  let plan = state.plans[planKey];\\n  if (plan?.phase === \\"completed\\") {\\n    delete state.plans[planKey];\\n    plan = null;\\n  }\\n  if (!plan) {\\n    const session = options.session?.trim() || randomUUID();\\n    return handleInit(\\n      state,\\n      workspace,\\n      { ...options, entry: entrySkill, mode: mode ?? options.mode, plan: planKey, session },\\n      now,\\n    );\\n  }\\n  const expected = currentPlanStep(plan);\\n  if (!expected || expected.skill !== entrySkill) {\\n    fail(\\n      `Plan ${planKey} 当前期望 ${expected ? stepLabel(expected) : \\"无后续 skill\\"}，不能进入 /${entrySkill}`,\\n    );\\n  }\\n  const requestedSession = options.session?.trim();\\n  if (leaseIsActive(plan.lease, now)) {\\n    if (!requestedSession || plan.lease.owner_session !== requestedSession) {\\n      fail(`资源由会话 ${plan.lease.owner_session} 持有`);\\n    }\\n    const modeChanged = mode !== undefined && mode !== plan.mode;\\n    if (modeChanged) plan.mode = mode;\\n    return {\\n      changed: modeChanged,\\n      output: {\\n        ...stepOutput(expected),\\n        phase: plan.phase,\\n        plan: planKey,\\n        session: requestedSession,\\n      },\\n    };\\n  }\\n  const session = requestedSession || randomUUID();\\n  plan.lease = makeLease(session, leaseSeconds(options), now);\\n  if (mode !== undefined) plan.mode = mode;\\n  return {\\n    changed: true,\\n    output: {\\n      ...stepOutput(expected),\\n      phase: plan.phase,\\n      plan: planKey,\\n      session,\\n    },\\n  };\\n};\\n\\nconst recordStepReceipt = (subject, expected, options, now) => {\\n  const actual = expected.skill\\n    ? `/${normalizeSkill(requireOption(options, \\"skill\\"))}`\\n    : requireOption(options, \\"action\\");\\n  if (actual !== stepLabel(expected)) {\\n    fail(`步骤顺序错误: 期望 ${stepLabel(expected)}, 实际 ${actual}`);\\n  }\\n  const result = requireOption(options, \\"result\\");\\n  if (!expected.results.includes(result)) {\\n    fail(`${stepLabel(expected)} 的 result 必须是 ${expected.results.join(\\" | \\")}`);\\n  }\\n  const evidence = optionValues(options, \\"evidence\\").filter(Boolean);\\n  if (evidence.length === 0) fail(`${stepLabel(expected)} 至少需要一个 --evidence`);\\n  subject.receipts.push({\\n    evidence,\\n    kind: expected.skill ? \\"skill\\" : \\"action\\",\\n    reason: options.reason ?? null,\\n    recorded_at: now.toISOString(),\\n    result,\\n    step: stepLabel(expected),\\n  });\\n};\\n\\nconst handleRecordPlan = (state, workspace, options, now) => {\\n  const planKey = normalizePlanPath(workspace, requireOption(options, \\"plan\\"));\\n  const plan = state.plans[planKey];\\n  if (!plan) fail(`Plan ${planKey} 尚未初始化`);\\n  const session = requireOption(options, \\"session\\");\\n  const sequence = planSequence(plan);\\n  if (!sequence) fail(`Plan ${planKey} 当前阶段 ${plan.phase} 不接受 record-plan`);\\n  requireLease(plan, session, now);\\n  let next = recordReceipt(plan, sequence, options, now);\\n  if (next) {\\n    plan.lease = makeLease(session, leaseSeconds(options), now);\\n  } else if (plan.phase === \\"planning\\" && issuesDecision(plan) === \\"skipped\\") {\\n    plan.cursor = 0;\\n    plan.phase = \\"delivering_direct\\";\\n    plan.lease = makeLease(session, leaseSeconds(options), now);\\n    next = currentPlanStep(plan);\\n  } else if (plan.phase === \\"planning\\") {\\n    plan.cursor = 0;\\n    plan.lease = null;\\n    plan.phase = \\"delivering_issues\\";\\n  } else {\\n    plan.lease = null;\\n    plan.phase = \\"completed\\";\\n  }\\n  return {\\n    changed: true,\\n    output: { ...stepOutput(next), phase: plan.phase, plan: planKey },\\n  };\\n};\\n\\nconst handleClaimIssue = async (state, workspace, options, now) => {\\n  const planKey = normalizePlanPath(workspace, requireOption(options, \\"plan\\"));\\n  const issueId = requireOption(options, \\"issue\\");\\n  const requestedSession = options.session?.trim();\\n  if (!/^\\\\d{2}$/.test(issueId)) fail(\\"--issue 必须是两位 Issue ID\\");\\n  const plan = state.plans[planKey];\\n  if (!plan || plan.phase !== \\"delivering_issues\\") {\\n    fail(`Plan ${planKey} 尚未进入 Issue 交付阶段`);\\n  }\\n  const issueState = plan.issues[issueId];\\n  if (issueState?.status === \\"completed\\") fail(`Issue ${issueId} 已完成`);\\n  if (issueState?.status === \\"active\\" && leaseIsActive(issueState.lease, now)) {\\n    if (requestedSession && issueState.lease.owner_session === requestedSession) {\\n      return {\\n        changed: false,\\n        output: {\\n          issue: issueId,\\n          ...stepOutput(nextStep(ISSUE_FLOW, issueState.cursor)),\\n          plan: planKey,\\n          session: requestedSession,\\n        },\\n      };\\n    }\\n    fail(`Issue ${issueId} 由会话 ${issueState.lease.owner_session} 持有`);\\n  }\\n  const session = requestedSession || randomUUID();\\n  const planPath = path.resolve(workspace, planKey);\\n  await requireIssueReady(planPath, issueId);\\n  await transitionIssue(planPath, issueId, \\"in_progress\\");\\n  plan.issues[issueId] = {\\n    cursor: issueState?.cursor ?? 0,\\n    lease: makeLease(session, leaseSeconds(options), now),\\n    receipts: issueState?.receipts ?? [],\\n    status: \\"active\\",\\n  };\\n  const next = nextStep(ISSUE_FLOW, plan.issues[issueId].cursor);\\n  return {\\n    changed: true,\\n    output: { issue: issueId, ...stepOutput(next), plan: planKey, session },\\n  };\\n};\\n\\nconst handleRecordIssue = async (state, workspace, options, now) => {\\n  const planKey = normalizePlanPath(workspace, requireOption(options, \\"plan\\"));\\n  const issueId = requireOption(options, \\"issue\\");\\n  const session = requireOption(options, \\"session\\");\\n  const plan = state.plans[planKey];\\n  const issueState = plan?.issues[issueId];\\n  if (!issueState || issueState.status !== \\"active\\") fail(`Issue ${issueId} 未被领取`);\\n  requireLease(issueState, session, now);\\n  const next = recordReceipt(issueState, ISSUE_FLOW, options, now);\\n  if (next) {\\n    issueState.lease = makeLease(session, leaseSeconds(options), now);\\n  } else {\\n    const planPath = path.resolve(workspace, planKey);\\n    const issue = (await readIssues(planPath)).find((item) => item.id === issueId);\\n    if (!issue || !hasDeliveryEvidence(issue.content)) {\\n      fail(`Issue ${issueId} 完成前必须写入交付物与验证证据`);\\n    }\\n    await transitionIssue(planPath, issueId, \\"completed\\");\\n    issueState.lease = null;\\n    issueState.status = \\"completed\\";\\n    if ((await readIssues(planPath)).every((item) => item.status === \\"completed\\")) {\\n      plan.phase = \\"completed\\";\\n    }\\n  }\\n  return {\\n    changed: true,\\n    output: {\\n      issue: issueId,\\n      ...stepOutput(next),\\n      phase: plan.phase,\\n      plan: planKey,\\n      status: issueState.status,\\n    },\\n  };\\n};\\n\\nconst handleBlockIssue = async (state, workspace, options, now) => {\\n  const planKey = normalizePlanPath(workspace, requireOption(options, \\"plan\\"));\\n  const issueId = requireOption(options, \\"issue\\");\\n  const session = requireOption(options, \\"session\\");\\n  const reason = requireOption(options, \\"reason\\");\\n  const releaseCondition = requireOption(options, \\"release-condition\\");\\n  const issueState = state.plans[planKey]?.issues[issueId];\\n  if (!issueState || issueState.status !== \\"active\\") fail(`Issue ${issueId} 未被领取`);\\n  requireLease(issueState, session, now);\\n  await transitionIssue(path.resolve(workspace, planKey), issueId, \\"blocked\\", (content) =>\\n    blockedBody(content, reason, releaseCondition),\\n  );\\n  issueState.block = { reason, release_condition: releaseCondition };\\n  issueState.lease = null;\\n  issueState.status = \\"blocked\\";\\n  return { changed: true, output: { issue: issueId, plan: planKey, status: \\"blocked\\" } };\\n};\\n\\nconst handleLeaseCommand = (state, workspace, command, options, now) => {\\n  const planKey = normalizePlanPath(workspace, requireOption(options, \\"plan\\"));\\n  const session = requireOption(options, \\"session\\");\\n  const plan = state.plans[planKey];\\n  if (!plan) fail(`Plan ${planKey} 尚未初始化`);\\n  const isIssue = command.endsWith(\\"-issue\\");\\n  const issueId = isIssue ? requireOption(options, \\"issue\\") : null;\\n  const subject = isIssue ? plan.issues[issueId] : plan;\\n  if (!subject) fail(isIssue ? `Issue ${issueId} 没有运行态` : \\"Plan 没有运行态\\");\\n  if (command.startsWith(\\"heartbeat\\")) {\\n    requireLease(subject, session, now);\\n    subject.lease = makeLease(session, leaseSeconds(options), now);\\n  } else if (command.startsWith(\\"release\\")) {\\n    requireLease(subject, session, now);\\n    subject.lease = null;\\n    if (isIssue) subject.status = \\"paused\\";\\n  } else {\\n    if ([\\"delivering_issues\\", \\"completed\\"].includes(plan.phase)) {\\n      fail(\\"已完成的流程不可重新领取\\");\\n    }\\n    if (leaseIsActive(subject.lease, now)) {\\n      fail(`资源由会话 ${subject.lease.owner_session} 持有`);\\n    }\\n    subject.lease = makeLease(session, leaseSeconds(options), now);\\n  }\\n  const next = isIssue ? nextStep(ISSUE_FLOW, subject.cursor) : currentPlanStep(plan);\\n  return {\\n    changed: true,\\n    output: {\\n      issue: issueId,\\n      ...stepOutput(next),\\n      ...(isIssue ? { status: subject.status } : { phase: plan.phase }),\\n      plan: planKey,\\n    },\\n  };\\n};\\n\\nconst handleSyncPlan = async (state, workspace, options) => {\\n  const planKey = normalizePlanPath(workspace, requireOption(options, \\"plan\\"));\\n  const planPath = path.resolve(workspace, planKey);\\n  const issues = await readIssues(planPath);\\n  await updateSpecView(planPath, issues);\\n  const plan = state.plans[planKey];\\n  if (!plan) return { changed: false, output: { plan: planKey, synced: true } };\\n  let changed = false;\\n  for (const issue of issues) {\\n    const runtime = plan.issues[issue.id];\\n    if (issue.status === \\"pending\\" && runtime) {\\n      delete plan.issues[issue.id];\\n      changed = true;\\n    } else if (issue.status === \\"completed\\" && runtime?.status !== \\"completed\\") {\\n      plan.issues[issue.id] = {\\n        cursor: ISSUE_FLOW.length,\\n        lease: null,\\n        receipts: runtime?.receipts ?? [],\\n        status: \\"completed\\",\\n      };\\n      changed = true;\\n    } else if (issue.status === \\"blocked\\" && runtime?.status !== \\"blocked\\") {\\n      plan.issues[issue.id] = {\\n        cursor: runtime?.cursor ?? 0,\\n        lease: null,\\n        receipts: runtime?.receipts ?? [],\\n        status: \\"blocked\\",\\n      };\\n      changed = true;\\n    } else if (issue.status === \\"in_progress\\" && !runtime) {\\n      plan.issues[issue.id] = {\\n        cursor: 0,\\n        lease: null,\\n        receipts: [],\\n        status: \\"paused\\",\\n      };\\n      changed = true;\\n    }\\n  }\\n  if (plan.phase !== \\"planning\\" && issuesDecision(plan) === \\"completed\\") {\\n    const phase = issues.every((issue) => issue.status === \\"completed\\")\\n      ? \\"completed\\"\\n      : \\"delivering_issues\\";\\n    if (plan.phase !== phase) {\\n      plan.phase = phase;\\n      changed = true;\\n    }\\n  }\\n  return { changed, output: { phase: plan.phase, plan: planKey, synced: true } };\\n};\\n\\nconst handleStatus = (state, workspace, options) => {\\n  if (!options.plan) return { changed: false, output: { plans: state.plans } };\\n  const planKey = normalizePlanPath(workspace, options.plan);\\n  const plan = state.plans[planKey];\\n  if (!plan) fail(`Plan ${planKey} 尚未初始化`);\\n  return { changed: false, output: { plan: publicPlan(plan) } };\\n};\\n\\nconst dispatch = async (state, workspace, command, options, now) => {\\n  if (command === \\"enter-plan\\") return handleEnterPlan(state, workspace, options, now);\\n  if (command === \\"record-plan\\") return handleRecordPlan(state, workspace, options, now);\\n  if (command === \\"claim-issue\\") return handleClaimIssue(state, workspace, options, now);\\n  if (command === \\"resume-issue\\") return handleClaimIssue(state, workspace, options, now);\\n  if (command === \\"record-issue\\") return handleRecordIssue(state, workspace, options, now);\\n  if (command === \\"block-issue\\") return handleBlockIssue(state, workspace, options, now);\\n  if (command === \\"status\\") return handleStatus(state, workspace, options);\\n  if (\\n    [\\"heartbeat-plan\\", \\"release-plan\\", \\"claim-plan\\", \\"heartbeat-issue\\", \\"release-issue\\"].includes(\\n      command,\\n    )\\n  ) {\\n    return handleLeaseCommand(state, workspace, command, options, now);\\n  }\\n  if (command === \\"sync-plan\\") return handleSyncPlan(state, workspace, options);\\n  fail(`未知命令 ${command}`);\\n};\\n\\nexport const executeFlow = async ({\\n  command,\\n  hooks,\\n  now = () => new Date(),\\n  options = {},\\n  workspace = process.cwd(),\\n}) => {\\n  const root = path.resolve(workspace);\\n  const hookConfig = hooks === undefined ? await loadHooks() : validateHooks(hooks);\\n  let mode = DEFAULT_FLOW_MODE;\\n  const result = await withStateTransaction(root, now, async (state, transactionTime) => {\\n    const transaction = await dispatch(state, root, command, options, transactionTime);\\n    const planKey = transaction.output?.plan;\\n    if (typeof planKey === \\"string\\") mode = state.plans[planKey]?.mode ?? DEFAULT_FLOW_MODE;\\n    return transaction;\\n  });\\n  if (!result.next_skill) return result;\\n  const message = messageForSkill(hookConfig, result.next_skill, mode);\\n  return message === \\"\\" ? result : { ...result, message };\\n};\\n\\nconst parseCli = (argumentsList) => {\\n  const [command, ...tokens] = argumentsList;\\n  if (!command) return { command: \\"help\\", options: {} };\\n  const options = {};\\n  for (let index = 0; index < tokens.length; index += 1) {\\n    const token = tokens[index];\\n    if (!token.startsWith(\\"--\\")) fail(`无法识别参数 ${token}`);\\n    const name = token.slice(2);\\n    const value = tokens[index + 1];\\n    if (!value || value.startsWith(\\"--\\")) fail(`--${name} 缺少值`);\\n    index += 1;\\n    if (name === \\"evidence\\") {\\n      options.evidence = [...(options.evidence ?? []), value];\\n    } else {\\n      options[name] = value;\\n    }\\n  }\\n  return { command, options };\\n};\\n\\nconst usage = `用法:\\n  flow.mjs enter-plan --plan <path> --skill /nano-flow --entry </to-story|/quest-with-domain> [--session <id>] [--mode <manual|auto>]\\n  flow.mjs record-plan --plan <path> --session <id> (--skill </skill>|--action <action>) --result <result> --evidence <ref>\\n  flow.mjs claim-issue --plan <path> --issue <NN> [--session <id>]\\n  flow.mjs record-issue --plan <path> --issue <NN> --session <id> (--skill </skill>|--action <action>) --result <result> --evidence <ref>\\n  flow.mjs block-issue --plan <path> --issue <NN> --session <id> --reason <text> --release-condition <text>\\n  flow.mjs heartbeat-plan|release-plan|claim-plan --plan <path> --session <id>\\n  flow.mjs heartbeat-issue|release-issue|resume-issue --plan <path> --issue <NN> --session <id>\\n  flow.mjs sync-plan --plan <path>\\n  flow.mjs status [--plan <path>]\\n`;\\n\\nexport const runFlowCli = async ({\\n  argumentsList = process.argv.slice(2),\\n  cwd = process.cwd(),\\n  stderr = (message) => console.error(message),\\n  stdout = (message) => process.stdout.write(message),\\n} = {}) => {\\n  try {\\n    const parsed = parseCli(argumentsList);\\n    if ([\\"help\\", \\"--help\\", \\"-h\\"].includes(parsed.command)) {\\n      stdout(usage);\\n      return 0;\\n    }\\n    const workspace = parsed.options.workspace ?? cwd;\\n    delete parsed.options.workspace;\\n    const result = await executeFlow({ ...parsed, workspace });\\n    stdout(`${JSON.stringify({ success: true, ...result }, null, 2)}\\\\n`);\\n    return 0;\\n  } catch (error) {\\n    stderr(JSON.stringify({ error: error.message, success: false }));\\n    return 1;\\n  }\\n};\\n\\nconst invokedPath = process.argv[1] ? path.resolve(process.argv[1]) : \\"\\";\\nif (invokedPath === fileURLToPath(import.meta.url)) process.exitCode = await runFlowCli();\\n"},{"path":"scripts/plan-document.mjs","content":"export const ISSUE_STATUSES = Object.freeze([\\"pending\\", \\"in_progress\\", \\"blocked\\", \\"completed\\"]);\\n\\nexport const parseFrontmatter = (content) => {\\n  const match = content.match(/^---\\\\r?\\\\n([\\\\s\\\\S]*?)\\\\r?\\\\n---(?:\\\\r?\\\\n|$)/);\\n  if (!match) return null;\\n\\n  const fields = new Map();\\n  for (const line of match[1].split(/\\\\r?\\\\n/)) {\\n    const field = line.match(/^([a-z_]+):(.*)/);\\n    if (field) fields.set(field[1], field[2].trim());\\n  }\\n  return { fields, match };\\n};\\n\\nexport const parseIssueDependencies = (rawValue) => {\\n  if (rawValue === undefined) return { kind: \\"missing\\" };\\n\\n  let value;\\n  try {\\n    value = JSON.parse(rawValue);\\n  } catch (error) {\\n    return {\\n      detail: error instanceof Error ? error.message : String(error),\\n      kind: \\"invalid_json\\",\\n    };\\n  }\\n  if (!Array.isArray(value) || value.some((dependency) => !/^\\\\d{2}$/.test(dependency))) {\\n    return { kind: \\"invalid_value\\" };\\n  }\\n  return { dependencies: value, kind: \\"valid\\" };\\n};\\n\\nexport const deriveSpecStatus = (issues) => {\\n  if (issues.every((issue) => issue.status === \\"pending\\")) return \\"pending\\";\\n  if (issues.every((issue) => issue.status === \\"completed\\")) return \\"completed\\";\\n  return \\"in_progress\\";\\n};\\n"},{"path":"SKILL.md","content":"---\\nname: nano-flow\\ndescription: 路由从想法到交付的 Nano Flow 主流程；当任务需要形成用户故事、打磨领域设计、按需拆分 issues、恢复既有 Flow 或判断下一步 skill 时使用。\\n---\\n\\n# Nano Flow\\n\\n`/nano-flow` 只负责选择主流程的起点和传递 Flow context。每个 skill 的步骤、产物与完成标准由该 skill 单独拥有。\\n\\n## 选择入口\\n\\n先从用户输入和现场材料判断当前位置，只推荐一条入口：\\n\\n| 现场                                         | 入口                                                                 |\\n| -------------------------------------------- | -------------------------------------------------------------------- |\\n| 角色、收益或可验收的用户结果尚未明确         | [`/to-story`](references/skills/to-story/SKILL.md)                   |\\n| 用户结果已明确，需要从领域设计开始继续主流程 | [`/quest-with-domain`](references/skills/quest-with-domain/SKILL.md) |\\n\\n同时按工作性质只推荐一种推进模式：\\n\\n| 工作性质                               | 模式             |\\n| -------------------------------------- | ---------------- |\\n| 改动大、需要用户介入与深入交互         | `manual`（默认） |\\n| 改动小、无须用户确认、可自动修复或实现 | `auto`           |\\n\\n已有 Flow 或 Plan 时不创建新路径；读取 [`FLOW.md`](FLOW.md)，从运行态返回的当前位置恢复。\\n\\n向用户说明推荐入口与模式，以及区分它们与相邻选择的关键事实，并等待明确确认。证据不足时只澄清会改变入口或模式选择的决策。入口与模式唯一且用户已确认时，本步骤完成。\\n\\n## 进入 Flow\\n\\n确认新入口后，完整读取 [`FLOW.md`](FLOW.md)，以 `/nano-flow` 为发起者执行 `enter-plan`，并以 `--mode` 传入已确认的模式。保存返回的 flow context，只调用返回的 `next_skill`；返回 `message` 时，将它原样携带给该 skill。\\n\\n`enter-plan` 成功、返回入口与确认结果一致、完整 context 已传给该入口且没有重复进入 Flow 时，本步骤完成。\\n\\n## 沿运行态推进\\n\\n每次只完整读取并执行当前返回的 skill，并将同次返回的 `message` 原样携带给它。当前 skill 的完成标准成立后，按 [`FLOW.md`](FLOW.md) 登记真实证据，再执行新的 `next_skill` 或 `next_action`。工作区领域文档的定位、布局与生命周期以 [`DOMAIN.md`](references/DOMAIN.md) 为单一事实源，并只在当前 skill 需要时读取。\\n\\n运行态到达终点，或形成带恢复条件的 blocked 结果时，本次路由完成。\\n"}]},{"name":"nano-flow-cli","version":"0.0.2","contentHash":"ff93f2c6d534f1f00c251d9a6fec17803e661c91bcd2c789027b2b0c34adf952","files":[{"path":"references/anki.md","content":"# Anki 路由\\n\\n`nnf anki` 经 AnkiConnect 操作桌面端 Anki。开始前确认 Anki 已启动并安装 AnkiConnect；默认地址是 `http://localhost:8765`，需要覆盖时使用 `--anki-connect <url>`。\\n\\n## 命令族导航\\n\\n| 任务                                       | 路由                                |\\n| ------------------------------------------ | ----------------------------------- |\\n| 牌组列表、统计、创建、移动卡片             | `nnf anki decks`                    |\\n| 笔记新增、批量新增、查询、详情、更新、删除 | `nnf anki notes`                    |\\n| 笔记类型、字段、样式与模板                 | `nnf anki models`                   |\\n| 到期卡片、状态查询、展示与评分             | `nnf anki cards`                    |\\n| 交互复习或同步 AnkiWeb                     | `nnf anki review` / `nnf anki sync` |\\n| 标签增删、替换与清理                       | `nnf anki tags`                     |\\n| 媒体查询、读取、写入与删除                 | `nnf anki media`                    |\\n| 集合统计与复习历史                         | `nnf anki stats`                    |\\n| 编辑、创建和牌组管理 GUI                   | `nnf anki gui`                      |\\n\\n## 工作流\\n\\n### 复习\\n\\n优先运行 `nnf anki review --deck <name> --limit <count> --include-new`。它按序同步、取卡、展示问题、从 stdin 接收 1–4 评分并汇总；输入 `q` 或 EOF 结束。需要单步控制时使用：\\n\\n1. `nnf anki sync`\\n2. `nnf anki cards due --deck <name>`\\n3. `nnf anki cards present --card-id <id>`，确认后加 `--answer`\\n4. 用户确认评分后运行 `nnf anki cards rate --card-id <id> --rating <1-4>`\\n\\n每次评分使用本次查询所得 card ID，并由用户确认评分。会话结束后运行 `nnf anki sync` 完成收尾同步。\\n\\n### 建卡\\n\\n1. `nnf anki models fields --name <model>` 确认字段；第一字段是排序字段且必须非空。\\n2. `nnf anki decks list` 确认牌组，不存在时运行 `nnf anki decks create --name <deck>`。\\n3. 单张使用 `nnf anki notes add --deck <deck> --model <model> --field Front=<text> --field Back=<text>`；批量使用 `nnf anki notes add-batch --deck <deck> --model <model> --input -` 并从 stdin 提供 JSON。\\n4. 需要用户在 Anki 中手动确认时使用 `nnf anki gui add-cards`，并按 leaf help 提供命名 options。\\n\\n新增成功后，以返回的 note ID 运行 `nnf anki notes info --note-id <id>` 复核字段。\\n\\n### 查询后修改\\n\\n先用 `nnf anki notes find --query <anki-query>` 获取 ID，再以 `nnf anki notes info --note-id <id>` 核对目标，最后用 `nnf anki notes update --id <id> --field <name=value>` 修改。每个被修改的 ID 都应来自本次会话的查询结果。\\n\\n## 安全与语义\\n\\n- 永久删除笔记、删除笔记类型字段、清理未使用标签和删除媒体前，先取得用户确认，再按 leaf help 使用 `--yes`。\\n- `nnf anki notes update` 的目标正在 Anki 浏览器中打开时可能静默失败；更新前让用户关闭浏览器或切走，并在结果异常时先检查这一点。\\n- `sync` 依赖桌面端已经登录 AnkiWeb。\\n- `nnf anki gui` 只用于编辑、创建与牌组管理；复习使用 `nnf anki review` 或 `nnf anki cards`。\\n- 统计结果中的 counts 表达今日学习队列，states 表达不受日期和上限影响的真实状态计数。\\n- `nnf anki media store --file <path>` 限制媒体 MIME；URL 默认拒绝私网和环回地址；文件名会防御路径穿越。例外由 `MEDIA_ALLOWED_TYPES`、`MEDIA_IMPORT_DIR`、`MEDIA_ALLOWED_HOSTS` 控制。\\n- `--read-only` 或 `READ_ONLY=true` 阻止集合写入，但保持复习评分与同步能力。\\n"},{"path":"references/self.md","content":"# Self 路由\\n\\n## 命令导航\\n\\n| 任务                           | 路由                                                                         |\\n| ------------------------------ | ---------------------------------------------------------------------------- |\\n| 检查 CLI 版本                  | `nnf --version`                                                              |\\n| 更新 CLI 与已安装的受管 skills | `nnf self update`                                                            |\\n| 查看包内 skills 及安装状态     | `nnf self skill list`                                                        |\\n| 检查一个 skill                 | `nnf self skill check --name <skill>`                                        |\\n| 安装一个或全部 skills          | `nnf self skill install --name <skill>` / `nnf self skill install --all`     |\\n| 更新一个 skill                 | `nnf self skill update --name <skill>`                                       |\\n| 卸载一个或全部 skills          | `nnf self skill uninstall --name <skill>` / `nnf self skill uninstall --all` |\\n\\n`nnf self update --version <semver-or-tag>` 可选择 npm 版本或 tag；未指定时选择最新稳定版。\\n\\n## 目标目录与变更边界\\n\\n- 默认 skill 根目录是当前工作区的 `.agents/skills`，命令在其下管理 `nano-flow` 或 `nano-flow-cli` 文件夹。\\n- 使用 `--target <root>` 指定其他 skill 根目录；传入的是根目录，不是具体 skill 文件夹。\\n- 安装会先移除同名目标再写入包内版本，即使内容没有变化。先运行同参数的 `--dry-run`，核对计划中的 `target`。\\n- 更新与卸载遇到本地修改时保留现场并报错；只有明确要覆盖或删除这些修改时才使用 `--force`。\\n- `nnf self update` 同步 npm 包和已经安装的受管 skills；失败会回滚。可先运行 `nnf self update --dry-run` 查看 CLI 与 skill 的完整计划。\\n\\n完成标准：写操作返回成功后，用 `nnf self skill check --name <skill>` 验证状态；批量操作用 `nnf self skill list` 验证所有目标。\\n"},{"path":"SKILL.md","content":"---\\nname: nano-flow-cli\\ndescription: 使用 nnf CLI 管理 Nano Flow 自身与受管 skills，或通过 AnkiConnect 操作本机 Anki；当任务涉及 CLI 自管理或 Anki 操作时使用。\\n---\\n\\n# Nano Flow CLI\\n\\n`nnf` 将自身管理和 Anki 自动化收口在一个命令入口。根据任务只读取一份路由：\\n\\n- CLI 自管理任务：读取 [self 路由](references/self.md)。\\n- Anki 操作：读取 [Anki 路由](references/anki.md)。\\n\\nNano Flow 的需求打磨、计划与实现流程属于 `nano-flow` 工作流 skill，不通过本 skill 路由。\\n\\n## 共享契约\\n\\n- 现场运行 `nnf --help`、`nnf <group> --help` 或更深层的 `--help`，以 CLI 生成的命令和 option 为事实来源。\\n- 输入只使用命名 option。默认真实执行；在写命令上先用 `--dry-run` 获取计划，确认后移除该 option 执行。\\n- stdout 是成功 JSON，stderr 是错误 JSON；退出码 0 表示成功、1 表示运行时错误、2 表示用法错误。失败时优先按 `hint` 修正前置条件。\\n"}]}]',
) as readonly ManagedSkill[];
