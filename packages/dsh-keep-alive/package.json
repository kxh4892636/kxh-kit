{
  "name": "dsh-keep-alive",
  "version": "0.0.1",
  "description": "Keep DSH running after the terminal closes",
  "license": "MIT",
  "bin": {
    "dsh-alive": "dist/main.mjs"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "type": "module",
  "scripts": {
    "build": "vp pack",
    "check": "vp check",
    "test": "vp pack && vp test",
    "test:coverage": "vp pack && vp test --run --coverage"
  },
  "dependencies": {
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@typescript/native": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "typescript": "catalog:",
    "vite-plus": "catalog:",
    "vitest": "catalog:"
  },
  "engines": {
    "node": ">=24.19.0"
  }
}
